# 🧐 품질검수팀

**가치관**: 오보 1건이 계정을 죽인다. 의심되면 막는다.
**책임**: 발행 전 6항 체크리스트 전항 PASS 판정

## 6항 체크리스트
1. 수치=원본 일치(provenance 대조) 2. 순위·합계 정합 3. 출처 표기
4. 저작권(허브 등록 자산만) 5. 가독성(넘침·겹침·대비) 6. 오해 소지 표현

## 도구 (구현됨/예정)
- **디자인 자동 검수기(구현)**: `packages/renderer` designQa (`pnpm --filter @wirit/renderer qa <content>`) — 정렬·여백·넘침·행겹침·**안쪽 여백 침범(padcross)** 을 렌더 좌표로 검출, error 시 발행 차단 → 5항(가독성) 자동화
- 4항 자동 검사: `scripts/validate-assets.mjs` (CI — 미등록·라이선스 누락 적발)
- 스키마 검증(렌더러 ajv) — 구조 오류 차단
- **출처 정합 원칙(3항)**: 수치의 1차 출처와 카드 표기 출처가 일치해야 함. 예—DART로 수집한 평균연봉 카드에 잡코리아를 출처로 넣으면 오보. 헷갈리는 출처 요청은 확인 후 반영
- (예정 M6) 팩트체크 코드 대조 + (M10) 비전 QA

## 기본 업무 기준 (07-21 확정 · 07-30 갱신)
- ⚠️ **카드 검수는 [docs/CARD_CHECKLIST.md](../../docs/CARD_CHECKLIST.md) 를 단일 창구로 쓴다.**
  §1 = 검수가 막는 것(외우지 않는다) · §2 = 사람이 봐야 하는 것 · §5 = 발행본 md5 회귀
- **designQa 자동 검출 항목의 정본은 코드**(`packages/renderer/src/designQa.ts`)다 — 문서에 목록을 복사해 두지 않는다(4곳에 얼어붙어 서로 다른 시점에 늙었던 문제). 최신 목록은 코드나 `audit-head` 산출로 확인한다.
- **검수 목록에 없는 요소는 안 잡힌다** — 새 글자 요소가 생기면 `designQa` 의 `LEAF` 에 셀렉터를 추가한다
- **새 검수항은 "일부러 어긋나게 되돌려" 실제로 잡히는지 확인한다.** 통과만 보고 넘기면 검사가 켜졌는지 알 수 없다
- **오너가 같은 지적을 두 번 하면 검수 실패로 본다** — 좌표로 잴 수 있는 항목이면 그 자리에서 검수항으로 승격한다
- **출처 정합**: 수치의 1차 출처 = 카드 표기 출처. 개별 미검증 산출치는 "**산출치**"로 표기, `verified=false`는 발행 전 1~2개 원문 대조
- **verified 승격 절차(발행 게이트)**: 헤드라인 수치는 **1차 출처(뉴스·공공데이터·법령·공식 시간표)로 대조** → 일치 확인 시 데이터셋 `verified=true` + 대조 근거·URL을 meta에 기록. 근거 없이는 발행 불가
- **캡션 팩트체크**: 본문뿐 아니라 **캡션의 비유·과장도 사실과 일치**해야(과장 비유가 틀리면 오보·클레임) — 6항 "오해 소지"에 포함
- **사진 라이선스**: 안전 라이선스만(CC/PD·Pexels·Pixabay). **CC BY-SA는 저작자 표기 필수**, Pexels/Pixabay는 표기 불요이나 크레딧 관행 유지
- 오해 소지 표현 차단(예: '같은 노선인데 왜 다르냐' → 급행/도심 지하 구분 한 줄)

## 학습 로그
| 날짜 | 배운 것 | 출처 |
|---|---|---|
| 07-19 | 검수 기준 6항 명문화 — "어떤 기준으로 검수하나"에 대한 답 | 오너 요구 |
| 07-19 | **동일 수치에 다른 순위를 매기지 않는다 → 공동순위**. 순위 완전성 검수에 "동일값=동일순위" 필수항 추가 | 오너 지적(초봉카드 오류) |
| 07-19 | **밴드형 집계 수치(9,500만원대 등)를 정밀값처럼 표기 금지**. 밴드는 밴드로, 정밀 랭킹은 1차 출처 검증 후에만 | 오너 지적 |
| 07-19 | 미검증(verified:false) 집계로는 '정밀 순위' 카드 발행 불가 — 밴드/공동순위·비배포까지만 | 오너 지적 |
| 07-19 | DART 평균연봉 첫 수집: 값이 모두 다르고(동률 없음) 업계 통념과 정성적으로 합치 — 그러나 세션은 웹 열람이 막혀 있어 DART 원문 화면 대조는 못 함. verified 승격은 오너 또는 웹 접근 가능한 절차 필요 | 자기점검 |
| 07-20 | **소수점 열 정렬 검수항 추가**: 우측정렬+tabular-nums만으론 정수 자릿수 다른 값(12.9 vs 3.5)의 소수점이 어긋남 → 정수부 짧은 값을 figure space(U+2007)로 앞 패딩해 소수점·자릿줄 정렬 | 오너 검수 요청(평균연봉 카드 직원수 열) |
| 07-20 | **디자인 검수 자동화 도구 확보**(designQa) — 눈으로 놓치던 셀-vs-텍스트 빈 띠를 좌표로 검출. 검수는 이제 `pnpm --filter @wirit/renderer qa <content>`로 실행하고, error면 발행 차단 | 오너 "자동 검토 능력" |
| 07-20 | **검수 구멍 발견·보강**: 기존 넘침 검사는 카드 '바깥 경계'만 봐서, 카드 밖으론 안 나가지만 안쪽 안전여백(패딩 72px)을 침범해 테두리에 붙는 줄을 못 잡았다("선 넘어감"). → `padcross` 검수항 추가(내용이 innerLeft/innerRight 침범 시 error). 회귀검증: 옛 열폭에서 30px 침범을 검출 | 오너 지적("직원수 선 넘어감, 검수 똑바로") |
| 07-21 | **verified 승격 실사례(지하철 표정속도)**: 세션도 WebSearch·일부 WebFetch로 1차 대조 가능. 헤드라인 서울–청량리 29.3=연합뉴스(2016) 정확일치, 급행 68.6=공식 시간표 오차 내, 서울교통공사 1~8호선=공공데이터포털 공식값 정확일치 → verified=true 승격·근거 URL 기록. 나무위키는 WebFetch 403(2차출처라 우회) | 오너 "검증 이어서" |
| 07-21 | **캡션도 검수 대상** — 카드가 정확해도 캡션 비유가 틀리면 오보. "KTX 저리가라"(68.6km/h) 걸러 "도심 도로보다 빠름"으로. 팩트체크를 캡션까지 확장 | 오너 "안전하게 수정" |
| 07-22 | **이모지·사진 커버도 결정성 검증**: 메달(🥇🥈🥉)·반투명 오버레이·사진 배경이 있어도 이중 렌더 md5 동일(크롬 폰트·이미지 결정적). 부동산 세트(커버·지도·순위) 전부 designQa 넘침·정렬 통과 후 발행 | 부동산 세트 |

| 07-22 | **캡션↔카드 수치 대조 자동화**: 캡션의 억 금액이 카드 수치 풀에 없으면 error(오보). 사람이 놓치기 쉬운 캡션-카드 숫자 불일치를 코드로 차단(예 캡션 70억 vs 카드 71.5억). 캡션 린트(태그5·금지어·기간·출처)도 함께 `reviewCli`로 자동화 | 검수 루프 |
| 07-23 | **폰트 실적용 검증 필수**: @font-face 선언만으로 안심 금지 — 렌더가 폴백됐는데 통과처럼 보일 수 있다. 의심 시 `document.fonts.check`/`status`로 로딩 확인, 육안으로 서체 대조(태백 OTF가 조용히 폴백된 사례). 웨이트 범위 밖 요청은 폴백 이탈 원인 | 태백 도입 |
| 07-23 | **증시 수치 표기 규칙**: 소수1 고정, 부호 포함(±). 차트 라벨은 라인·마커와 겹치지 않게(헤일로/전용밴드). 발행 전 헤드라인 수치(현재·고점)는 KRX/언론 교차확인 후 verified | 국장 카드 |
| 07-25 | **공용 캐시를 읽는 빌더는 지역 코드 정규식으로 반드시 좁힌다** — 경기(41xxx) 캐시를 추가하자 이미 승인된 서울 카드 합계가 1,254→4,378로 뛰고 타 지역이 순위에 섞였다(오보 직전). 필터 후 **해시 동일** 재확인이 완료 조건 | 서울 신고가 카드 오염 사고 |
| 07-25 | **승인된 카드는 매 수정마다 해시 회귀검사**: 공용 템플릿을 건드리면 다른 카드가 조용히 바뀐다. 신규 스타일은 variant 클래스(.sm-c) 안에 격리하고, 승인 카드 md5가 그대로인지 확인한다(기준값 5eaa30d2…) | 템플릿 공유 회귀 |
| 07-25 | **designQa는 flex 컨테이너 넘침을 못 잡는다** — 행 수·폰트·패딩을 바꾸면 Playwright로 `마지막 행 bottom ≤ 본문 bottom` 실측 확인. 16행 재조정 때 2회 초과 발견 | 실측 검증 |
| 07-25 | **키 매핑 불일치 = 조용한 오분류**: 지도 이름(geoName)과 실거래 키(dataKey)가 다르면 610건이 통째로 '미지정'으로 샌다. 집계는 항상 dataKey 기준 | 동탄 오분류 |

| 07-30 | **오너가 두 번 지적한 것은 전부 검수 구멍이었다.** 뱃지 겹침("이건 자동 검수 했어야지")·머리글 열 어긋남(07-20 재발)·지수 뱃지↔막대 겹침·푸터 위 간격 — 넷 다 "카드 밖으로 안 나가는" 불량이라 기존 넘침 검사에 안 걸렸다. → `badgeclip`·`colalign`·`barclip`·`footergap` 신설. **문서에만 적힌 원칙은 지켜지지 않는다** | 오너 "지난 기준 잊어버리고 다시 말하게 한 것들이 많아" |
| 07-30 | **`colalign` 의 정체**: 머리글 행과 데이터 행은 각자 다른 grid 다. `auto` 열을 양쪽에 두면 머리글은 라벨 폭("월세 상승분"), 행은 값 폭("+38만원")으로 계산해 어긋난다. 재는 것은 **글자가 아니라 열 상자** — 상자 안 정렬(가운데/왼쪽/오른쪽)은 별개 결정이다. 되돌려 검증: 15px 어긋남 검출 | 오너 "열 맞춰줘" |
| 07-30 | **`process.exit(0)` 이 중간에 있으면 뒤 단계가 조용히 안 돈다** — `rebuild-cards.mjs` 에 일괄 검수 블록을 붙였는데 한 줄도 실행되지 않았다. "붙였다"와 "돈다"는 다르다 | 자체 발견 |
| 07-30 | **단위 환산 사고는 데이터로 막는다** — 천원→만원을 ÷100 으로 잘못 나눠 송파 월세가 23만원이 됐다. 빌더가 `meta.unit.avgWolse === "천원"` 을 확인하지 않으면 던지게 했다. 수치가 "이상하게 작다/크다"는 감각도 검수 항목이다 | 자체 발견 |
| 07-30 | **최상급 표현은 계산 게이트를 통과해야 나간다** — "역사상 최고"는 `isPeakNow`, "전체"는 `upAll` 이 참일 때만. 손으로 적으면 다음 달 공표분에서 카드가 거짓말을 한다. 제목이 표와 모순되면(과천 −1만원 vs '전체 폭등') 문구가 아니라 **제목을 고친다** | 오너 문구 지시 검토 |
| 07-30 | **오너 메모의 수치도 대조 대상이다** — 오너가 9.6% 라고 적었지만 계산값은 9.2%(6개월 실측 4.6×2)였다. 받아 적으면 그게 오보다. 계산값을 쓰고 차이를 보고한 뒤 확인받는다 | 오너 "9.2%로 진행해줘" |
| 07-26 | **발행 승인 화면은 '나갈 물건'을 보여준다** — 타임라인 텍스트로 짐작하게 하지 않는다. 카드 전 장(캐러셀)+캡션 전문+자동검수 판정을 한 화면에 놓고, 검수가 pass가 아니면 승인 시 한 번 더 확인을 받는다 | 관제탑 대개편 |

**KPI**: 오보 0건 유지, 발행 후 정정 0건 | **자동화**: 4항 R3, 나머지 R0→M6에서 승급

## 학습 로그 — 2026-07-31 정비사업 지도 카드

- **`designQa` 는 좌표를 잰다. 그림과 낱말은 못 잡는다.** 이번에 좌표 검수를 통과하고도
  눈으로 잡은 것들: ① 로고가 브랜드가 아니라 회사 로고였다 ② 제목이 다른 카드의 것이었다
  ③ '압구정 현대'에 힐스테이트 로고가 붙었다. **그림·낱말은 대조표를 렌더해 사람에게 보인다.**
- **성공 로그를 믿지 않는다.** 로고 자동 취득은 '✅ 취득 7건'으로 끝났지만 그중 4건이
  엉뚱한 로고였다. 이름이 맞는 것과 그림이 맞는 것은 다른 검사다.
- **넘침은 사후에 잡지 말고 구조로 막는다.** 지도가 칸을 넘쳐 제목을 덮은 사고는
  `height:auto` 때문이었다. `width/height:100%` + viewBox 로 바꾸면 넘칠 수가 없다.
- **자동화가 조용히 죽는 자리를 막는다.** 워크플로 단계가 실패하면 결과 기록까지 통째로
  건너뛰어 저장소에 아무 흔적이 안 남는다 → 실행 단계에 `continue-on-error`,
  기록 단계에 `if: always()`. 기사 읽어오기·로고 취득 두 곳에서 같은 구멍을 밟았다.
- **확정본은 md5 로 지킨다.** 오너 지시로 확정본을 고쳤으면 `pixel-baselines.json` 과
  `CARD_CHECKLIST §5` 표를 **함께** 갱신하고 `doctor.mjs` 로 대조한다.

- **`brandhead` 검수항 신설(2026-07-31).** 우상단 뱃지 배경·글자 크기·카드 위 패딩을 재서
  공용 규격(base.css)과 대조한다. 켠 뒤 **일부러 어긋나게 되돌려 실제로 잡히는지 확인**했다
  (배경 투명 / 뱃지 40px / 패딩 44px 세 건 모두 검출).
  수준을 error 가 아니라 **warn** 으로 둔 이유: 켠 날 이미 나가 있던 카드
  (index-2026·maprank·estate-cover)가 같은 방식으로 어긋나 있어 error 로 두면
  **손대면 안 되는 발행본** 때문에 내보내기가 통째로 막힌다.
  → 남은 숙제: 그 카드들을 규격에 맞출지 예외로 둘지 오너와 정한다.

### 2026-07-31 — `titlegap` 검수항 신설

오너 지적: "제목과 표 헤드라인이 너무 붙어있어."

**왜 사람 눈이 놓쳤나.** 제목을 키운 것도 나고 그 결과를 본 것도 나인데 못 봤다.
제목만 보면 문제가 없다 — 크고 잘 채워져 있다. 제목**과 그 아래 덩어리를 같이** 봐야 보인다.
그리고 이 간격은 내가 여백을 줄여서 생긴 게 아니다. 제목이 폭에 맞춰 자동으로 커지면
글자가 차지하는 세로가 늘어 아래 여백이 저절로 먹힌다. **원인이 눈에 안 보이는 자리**다.

**조치.** 제목 상자 아래끝과 다음 형제 요소 윗끝을 재서 28px 미만이면 경고한다.
문턱은 지적이 나온 판(20px)과 고친 판(60px) 사이에서 낮게 잡았다 —
여백은 디자인 선택의 폭이 넓어 문턱을 높이면 멀쩡한 카드까지 시끄러워진다.

**켜졌는지 확인함.** 일부러 20px 로 되돌려 경고가 뜨는 것을 보고 60px 로 복구했다.
통과만 보면 검수가 실제로 도는지 알 수 없다.

### 2026-07-31 — `aicard` 검수항 신설 (같은 지적 2회)

오너 지적: "AI 티나지 않게 박스 등 처리해줘."

**이건 문서화 실패다.** 07-30 에 이미 오너가 같은 말을 했고 CEO.md 71행에 원칙까지 적혀 있었다.
그런데 07-31 에 나는 회색 라운드 타일 6칸을 또 만들었다. **적어 둔 것을 내가 안 읽었다.**
CLAUDE.md 학습 프로토콜이 "같은 지적 두 번이면 검수로 승격"이라고 말하는 지점이 정확히 여기다.

**조치.** 큰 라운드(≥20px) + 옅은 회색 채움 덩어리가 3개 이상 반복되면 경고한다.
문턱을 3으로 둔 이유: 하나짜리 강조 상자는 디자인이고, 셋이 나란히 서야 'UI 카드'가 된다.
색이 있는 상자(브랜드 강조)·짙은 띠(잉크)·흰 배경은 제외한다 — 문제는 **무채색 옅은 채움의 반복**이다.

**켜졌는지 확인함.** 오너가 물린 이전 판(라운드 24px 타일 6칸)으로 되돌려 경고가 뜨는 것을 보고
새 판으로 복구했다. 새 판은 0건이다.

**만들면서 깨진 것.** 측정 스크립트는 템플릿 문자열 안에 담겨 브라우저로 넘어간다.
거기 정규식을 쓰면 역슬래시가 한 번 먹혀 패턴이 깨진다(`Unterminated group`). 문자열을 잘라 쓴다.

### 2026-07-31 — 3팀 합동 검수에서 드러난 구멍

오너 지시로 디자인팀·AS팀·기획팀이 record-grid 카드를 각각 검수했다. 셋 다 실제 결함을 찾았다.

**AS팀 — 가장 아픈 지적.** 새 템플릿(record-grid)을 만들면서 `designQa.ts` 의 `LEAF` 목록에
글자 요소를 추가하지 않았다. 그래서 **이 카드의 글자는 처음부터 겹침 검사 밖에 있었다.**
`footerGap` 의 '푸터 위 요소' 목록도 마찬가지여서, 오너가 두 번 말한 "최하단 문구 여백"이
새 카드에는 적용되지 않는 상태였다. 검수가 통과한 게 아니라 **아무것도 안 재고 있었다.**
→ 둘 다 채우고, 일부러 어긋나게 되돌려 실제로 잡히는 것을 확인했다.

**AS팀 — 두 번째.** 오너 지적 3건 중 `aicard` 하나만 코드가 됐고 "숫자는 증거다"는 글로만 남아 있었다.
→ `hierarchy` 검수항 신설(문구 < 수치×1.15 면 경고). 문턱을 '같거나 크다'로 두지 않은 이유:
비슷하면 둘 다 주인공이 되어 위계가 사라진다.

**디자인팀.** 최고점 뱃지가 꼭짓점 직후의 되돌림 구간을 덮고 있었다.
"꼭짓점 아래는 비어 있다"는 가정이 틀린 것이다 — 데이터가 바뀌면 조용히 틀리는 종류의 가정.
→ 뱃지 가로 구간 안의 점들을 훑어 **재서** 밀어낸다. 지도 라벨을 무게중심이 아니라
도형 포함 여부로 판정하는 것과 같은 교훈이다.

**기획팀.** 데이터셋 주석이 이미 없어진 기능(뺄셈으로 만드는 보조 문구)을 설명하고 있었다.
다음 세션이 구현된 줄 알고 지나칠 수 있다 → 주석을 사실과 맞췄다.
그리고 발행 게이트를 다시 짚었다: 이 카드의 '역대 1위'류 문구는 전부 보도 인용이고
데이터셋은 verified=false 다. **KRX 대조 전에는 발행하지 않는다.**

**남은 교훈 한 줄.** 검수는 "통과했다"가 아니라 "무엇을 쟀다"로 읽어야 한다.
목록에서 빠진 요소는 통과한 게 아니라 검사받지 않은 것이다.

### 2026-07-31 — 셀프테스트가 개수를 못박아 수집을 통째로 죽이고 있었다

증상: 오너가 Actions 화면을 보내왔다. `국내 증시 수집` 워크플로가 **15초 만에 실패**.
너무 빨라서 수집(야후 호출)에 도달하지도 못한 것이었다.

원인: `selftest.ts` 가 `Object.keys(DOMAIN_MAP).length === 3` 을 검사하고 있었다.
DOMAIN_MAP 은 **브랜드를 추가할 때마다 자라는 큐레이션 목록**이다. 아파트 브랜드 로고를
넣으면서 항목이 늘었고, 그 순간 셀프테스트가 깨졌다. 워크플로는 수집 **전에** 셀프테스트를
돌리므로, 로고를 추가한 날부터 증시 수집이 한 번도 돌지 못했다.

교훈: **자라는 목록의 개수를 테스트로 못박지 않는다.** 개수는 그 파서의 성질이 아니다.
성질은 "모든 항목이 쓸 수 있는 도메인을 갖는가", "첫 도메인이 서로 겹치지 않는가"다.
개수를 박아 두면 테스트가 기능을 지키는 게 아니라 **변경을 막는 자물쇠**가 된다.
→ 성질 검사 3개로 바꿨다(85 통과 / 0 실패).

한 번 더 아픈 점: 이 실패는 **조용했다.** 수집이 안 되니 데이터셋이 07-22 에 멈췄고,
나는 그걸 "이 세션은 외부망이 막혀서"라고만 설명하고 있었다. 파이프라인이 죽은 것을
데이터가 안 온다는 사실만으로는 구분할 수 없다 — 실패를 알리는 경로가 필요하다.


## 2026-07-31 — "통과했다"가 아니라 "무엇을 쟀나": 이 세션에서 승격한 검사 넷

하루에 네 번, 같은 모양의 실패를 만났다. **검사 목록에 없어서 조용히 지나간 것들**이다.

1. **`audit-head` (신설)** — 머리 규격은 한 장씩 보면 안 보인다. `brandhead` 는 warn 이라
   카드별로 흘러갔고, 그래서 **5개 템플릿·13장**이 투명 뱃지·위 여백 44~52px 로 굴러가고 있었다.
   → 전 카드를 한 표로 찍는 창구를 만들고 §0 에 넣었다. 표로 보면 1초에 보인다.
2. **`clipped` (신설·error)** — 열 폭을 줄이자 "현대14차(203,204,205,206동)"이 "…"로 잘렸다.
   기존 truncate 는 `.rt-name` 만 봤다. → 말줄임 쓰는 칸을 좌표로 잰다. **error 로 둔다** —
   warn 이면 흘러가고, 흘러가면 카드에 "…"가 나간다.
3. **LEAF 확장** — `map-rank`·`metro-2col` 의 글자가 **통째로 겹침 검사 밖**에 있었다.
   제목을 키우는 변경을 하면서 올렸다. 커진 제목이 뱃지와 부딪히는지는 사람 눈이 아니라 좌표가 판정한다.
4. **`badgeclip` 오탐 수정** — 상단 캡션을 비우자 **글자 없는 빈 상자**가 뱃지와 74px 겹친
   것으로 나왔다. Range 가 잡을 글자가 없으면 상자 전체로 재기 때문이다.
   → 글자도 배경도 없는 껍데기는 겹침 대상이 아니다(그림 요소는 유지).

**새 검사를 넣으면 반드시 일부러 어긋나게 되돌려 잡히는지 본다.** 넷 다 그렇게 확인했다.

## 2026-07-31 — 발행본이 조용히 바뀌는 것을 md5 회귀가 잡았다

7월 실거래가 들어오자 「2026년 6월 신고가」 카드의 픽셀이 바뀌었다. 픽셀만이 아니었다 —
신고가 판정이 `날짜 >= 6월 1일` 이라 **7월 거래까지 6월 신고가로 세고 있었다.** 오보다.

원인은 빌더가 캐시 폴더를 **통째로** 읽은 것. → 대상 월을 인자로 받는 빌더는 `ym <= latest` 로 자른다.
**특정 달을 말하는 카드는 그 달보다 뒤의 데이터를 읽으면 안 된다.**

자동 갱신을 켜면 이런 사고가 늘어난다. 그래서 `auto-produce.yml` 에도 같은 검사를 물렸다.

## 2026-08-08 — 시안 선검수(3팀)·캡션 수치 대조 (world-capital·mae-streak)

- **검수를 확정이 아니라 시안 순간으로 앞당긴다.** 시안을 오너에게 보이기 전에 세션이 3렌즈
  (기획=의도·오보위험 / 디자인=렌더 PNG 실측·정렬·브랜드 상속 / 검수=결정성·발행본 픽셀불변·루브릭)를
  스스로 돌리고 '선검수 결과' 한 줄을 붙인다(CARD_CHECKLIST §0.5). 한방: `pnpm --filter @wirit/pipeline review`.
- **뉴스 소재 캡션의 억 단위 금액은 전부 카드에 있어야 한다(caption-number 검수).** 카드가 '실/세대'로
  표기한 값을 캡션에서 '억'으로 병기하거나, 카드에 없는 상충 수치를 넣으면 자동 차단(오보). 상충·미표기 금액은 캡션에서 뺀다.
- **정기물은 데이터 갱신 시 캡션·세트 제목이 조용히 늙는다.** mae-streak 78→79주로 데이터가 갔는데
  캡션·제목이 78주로 남아 있었다 → 발행 전 카드 수치와 캡션을 대조하고, 세트 제목엔 변하는 수치를 넣지 않는다.


## 2026-08-12 — 검사 목록에서 빠진 것은 "통과"가 아니라 "안 잰 것"

`gov-bars@1` 작업에서 두 번 확인됐다.

1. **푸터 띠** — 넘침 검사 목록에 없어서 25px 잘린 카드가 통과했다. `.wirit-footer` 를 넣고
   일부러 넘치게 만들어 3·22·31·50px 에서 각각 잡히는 것을 확인했다.
   (22·31px 구간에서는 워터마크가 침묵한다 — 그 한 줄이 없으면 정확히 그 구간이 뚫린다)
2. **SVG 안 글자** — `designQa` 는 HTML 요소만 잰다. 뱃지 글자가 카드 밖으로 잘려 나갔는데도
   "문제 없음"이었다. **SVG 글자는 빌더가 폭을 계산해 책임진다**로 규칙을 세웠다.

**아직 안 재는 것(다음 세션 숙제)**: 막대 중심 ↔ 인물 칸 중심 어긋남. 이번에 고쳤지만
검수항이 없어 이름이 길어지면 최대 247px 까지 조용히 어긋난다(에이전트 실측).
`colSkew` 처럼 좌표 대조 항으로 승격할 것.

**빌더 검산의 사각지대도 하나 확인했다.** `m2-gov` 는 "막대 합 = 전체 증가"를 매번 검산하는데,
구간이 **연속이기만 하면** 경계가 1년씩 틀려도 합은 항상 맞는다. 손으로 적은 퇴임월은 검산이 못 잡는다.

## 2026-08-14 — "검산이 무엇을 보장하는가"를 적지 않으면 검산은 안심 장치가 된다

청약 카드 2장(상동역 롯데캐슬 · 두산위브더제니스 부천)을 만들며 빌더·배관을 고쳤다.
회귀는 깨끗했지만(확정본 5장 md5 동일 · doctor 픽셀 동일 · audit-head 위반 0),
**켜져 있는 줄 알았던 검사 넷이 실제로는 꺼져 있었다.**

1. **`.dcv-facts .abv` 가 `designQa` `LEAF` 에 없었다.** 규모 우선 판(2026-08-06)의 회색
   머리글("59㎡ · 491세대")이다 — danji 5장(장위·더샵·써밋·충정로·두산)의 그 글자가 통째로
   겹침 검사 **밖**이었다. `margin-top:-70px` 로 일부러 28px 겹치게 했더니 검수는 "문제 없음"이라
   답했고, `LEAF` 에 셀렉터 하나 넣자 error 4건이 떴다. 판을 만들 때부터의 누락이다.
2. **`footergap` 은 danji-cover 에서 한 번도 돈 적이 없다.** '푸터 위 요소' 목록 21개 중
   danji-cover 요소가 **하나도 없었다**(`.db-note` 는 삭제된 danji-brief 것). 오너가 두 번 말한
   "최하단 문구 여백"이 청약 판형 9장에 적용된 적이 없었고, 하필 이번에 그 줄이 두 줄로 늘었다.
3. **공고문 파서의 세대수 합 검산은 '행 정렬'만 본다.** 금액 열을 8px 옆(건축비)으로 옮겨도
   합은 그대로 맞아 ✅ 가 찍혔다 — 84A 가 16.3억에서 9.49억이 됐는데 통과했다.
   세대수가 같은 주택형끼리(1세대짜리 셋, 224세대짜리 둘) **순서를 바꿔도** 통과한다.
   `m2-gov` 의 "구간이 연속이기만 하면 경계가 1년 틀려도 합은 맞는다"와 같은 모양이다.
4. **과잉 경고를 고치려다 거짓 안심을 만들었다.** "분양가는 보도값이다"를 무조건 찍던 줄을
   데이터 기반으로 바꾸면서 조건을 `price.byArea` 유무로만 걸었더니, 분양가를 `byType` 에만
   담은 **발행 보류 카드 3장(장위·드파인·송파)에 "✅ 모두 대조됨"** 이 찍혔다.
   **경고를 좁힐 때는 좁아진 쪽에 무엇이 남는지 세어 본다.**

교훈: **검사는 "통과했다"가 아니라 "무엇을 쟀다"로 읽는다.** 그리고 검산을 새로 만들면
그 검산이 **못 잡는 것**을 같은 자리에 적는다 — 안 적으면 다음 사람이 그것을 보증으로 읽는다.

## 학습 로그 — 2026-08-16 「오늘의 신고가」 판형

**게이트가 초록인데 카드가 깨져 있었다.** `rebuild-cards`·`produce-card` 는
`data/content/<날짜>/` 를 검수하는데, 빌더가 `--publish` 없이 돌면 결과는
`data/out/_spike` 로 간다 → **옛 발행본을 검수하고 "통과"라고 말했다.**
판이 담는 칸을 7px 넘긴 카드가 그렇게 확정 직전까지 갔다.

- 검수 결과를 볼 때 **경로를 같이 읽는다.** "무엇을 검수했는가"가 판정보다 먼저다.
- 카드 만들기 전에 `node scripts/verify-singo-pipe.mjs` — 배관 약속을 파일에서 확인한다.
- 확정 §0 순서도 틀려 있었다: 캡션 서명 검사가 **재생성보다 앞**이라 아무 의미가 없었다
  (몇몇 빌더가 캡션을 다시 쓰며 서명을 날린다). → 재생성 → 반영 → 확인.

**SVG 안 글자는 designQa 가 못 잰다.** 빌더가 계산해 던지게 하고, SVG 가 제 칸을 넘치는지는
`svgspill` 규칙이 잡는다. "문제 없음"이 SVG 안까지 봤다는 뜻은 아니다.

## 학습 로그 — 2026-08-16b 검사 자체를 검사하기

- **늘 빨간불인 검사는 없는 검사다.** 확정 픽셀 대조를 넣자 주간 시세물 3장이 매번 실패로 떴다 —
  자료가 갱신되면 다시 그려지는 게 정상인 카드들이다. 그대로 뒀으면 곧 아무도 안 봤을 것이다.
  → **정기물은 알리기만** 하고, 진짜 약속(비정기물 픽셀)만 실패로 센다.
- **같은 사실이 두 파일에 적혀 있으면 반드시 어긋난다.** `sinbundang-loop` 은 08-06 에 재확정하며
  `pixel-baselines.json` 만 갱신하고 `sets.json` 은 08-01 값을 그대로 뒀다. 열흘 동안 아무도 몰랐고
  새 검사를 넣고서야 드러났다. → 읽는 쪽이 **어느 것이 정본인지** 알게 한다.
- **경고 숫자에 가중치를 섞어 내보내지 않는다.** 점수(곡선×3)를 그대로 "N px² 겹칩니다"로 찍으면
  사람이 실제 겹침으로 읽는다. 정렬에는 가중치를, 사람에게는 **가중치 없는 실측**을.
- 배관 점검에 라벨 회피(⑧)·캡션 규칙(⑨)·확정 픽셀(⑩)이 늘었다.
  ⚠️ **항목 수를 문서에 적지 않는다.** 두 번 적었고 두 번 다 틀렸다(28이라 썼는데 25였고,
  25로 고치자마자 ⑪ 이 붙어 33이 됐다). 세고 싶으면 `node scripts/verify-singo-pipe.mjs` 가
  끝에 찍어 준다 — **숫자는 세는 쪽이 말한다.**

## 학습 로그 — 2026-08-16c 인수인계 감사

**새 세션인 척 감사를 돌렸더니 "조건부 가능"이 나왔다.** 배관은 멀쩡한데 **문서만 읽고는 못 만든다**는 뜻이었다.

- **막던 것**: 세트·빌더 등록 절차가 신고가 문서 어디에도 없어 `produce-card` 가 즉시 죽는다.
  대기열 순서가 거꾸로라(빌더가 `lawd`·`umd` 를 찍어 준다) 첫 단계에서 멈춘다.
  `--kapt` 값을 **찾는 방법**이 없다. Actions 결과를 어디서 보는지(`*-last.md`)가 없다.
- **문서가 코드와 달랐던 것**: 렌더 명령이 존재하지 않는 인자(`--outdir`·`--qa`)를 썼고,
  §0 검사가 "5종"이라 적혔는데 7종이었고, 픽셀 기준값 표가 6장인데 파일엔 9장이었다.
- **가장 큰 것**: 세션이 **제일 먼저 읽는** `STATUS.md` 가 폐기된 제목 규칙을 가르치고 있었다.
  뒤의 문서를 아무리 고쳐도 첫 문서가 틀리면 소용없다.

→ 대책을 "다음엔 잘 쓰자"로 두지 않았다. **`verify-singo-pipe.mjs` ⑪ 이 문서를 읽어 확인한다** —
세트 등록 절차 유무, 없는 렌더 인자, STATUS.md 의 폐기 규칙, 문서에 베껴 적은 md5.

또 하나: **`rebuild-cards.mjs` 를 단독으로 돌리면 일부 캡션의 고정 서명이 날아간다.**
단독 실행 뒤에는 `apply-signature.mjs` 를 이어서 돈다(실측 2건).

## 학습 로그 — 2026-08-16d 검사 순서가 곧 검사다

**`doctor` 도 카드를 다시 만든다.** 진단이 말로 확인하지 않고 실제로 찍어 보기 때문이다.
이걸 몰라서 「`apply-signature` 는 `rebuild-cards` 뒤」라는 규칙만 지키면 되는 줄 알았고,
`confirm.mjs`·체크리스트 §0 둘 다 재생성 직후에 서명을 붙였다. 그 뒤에 오는 doctor 가
도로 날려서 **확정을 돌릴 때마다** `tohuh-rent-map`·`wolse-flip` 두 캡션이 서명을 잃었다.

세 번째로 같은 자리에서 깨진 것이다(08-16 · 08-16c · 08-16d). 매번 원인이 같았다 —
**"재생성하는 것이 하나가 아니었다."** 규칙을 "rebuild 뒤"가 아니라 **"카드를 만드는 것
전부의 뒤"** 로 다시 적었고, 마지막에 `--check` 로 증명하게 했다.

→ 새 검사를 순서에 끼울 때는 **그 단계가 산출물을 다시 만드는지** 먼저 묻는다.
   "검사"라는 이름이 붙었다고 읽기만 하는 것이 아니다.

**여기서 통과해도 배포에서 깨지면 소용없다.** 마감 점검의 관제탑 순서가 `tower-deploy.yml`
과 달라(렌더·`build-archive`·`stage-public-cards` 누락) 스모크가 172/174 로 떨어졌는데,
**코드 문제가 아니라 생성물이 없는 것**이었다. 특히 `stage-public-cards` 는
`build-tower-site` 가 `_site` 를 통째로 지우므로 **반드시 그 뒤**여야 한다.
→ 로컬 검사 순서를 **배포 워크플로와 같게** 맞췄다. 다르면 로컬 초록은 거짓 안심이다.

**남은 것:** 관제탑에 신고가 6장이 결재·업로드 목록 **양쪽에** 떠서 173/174 다.
스모크의 「같은 카드가 두 번 뜨지 않음」이 잡았다. 원인 미규명.
