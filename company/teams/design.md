# 🎨 디자인팀

**가치관**: 동일 입력 = 동일 픽셀. 런타임에 창의성은 없다(창의성은 템플릿 제작 때).
**책임**: 콘텐츠 JSON → 1080×1350 카드 PNG (결정적 렌더)

## 도구 (구현됨)
- 렌더러: `packages/renderer` (Playwright, 스키마 검증, 캐러셀, 해시 동일성 검증됨). 폰트 로딩 후 레이아웃 후처리 훅 `window.__wiritFit` 호출 — 렌더·검수 파이프라인 동일 적용(결정적)
- **순위표 `ranking-table@1` — 확정 레퍼런스: "2026 대기업 평균연봉 Top10"**. 구현·확정된 기능:
  · 제목행 라벨(순위·기업·연봉·직원수)을 내용 열 위 **가운데 정렬**(값·보조 열 밀도별 고정폭 + 중앙정렬)
  · **제목 fit-to-width**: 표의 좌우 끝을 실측해 제목 좌우 끝을 **표와 동일하게 자동 정렬**하고 그 폭에 맞는 최대 폰트로 채움
  · **1·2·3위 금·은·동 메달**(🥇🥈🥉) + 기업명 금/은/동(진한 금속톤·미세 그림자, 가독성 우선) — `medal`/`rankClass` 헬퍼
  · **좌우 대칭 여백**(justify-content:center) + 데이터 행 **flex 세로 꽉 채움**(하단 여백 조절 가능)
  · 직원수 소수점 정렬(figure space U+2007), 공동순위(`rank`) 지원, 로고 3단계 폴백
- **디자인 검수기**: `packages/renderer/src/designQa.ts` + `qaCli.ts` (`pnpm --filter @wirit/renderer qa <content>`) — 렌더 좌표 측정 자동 검출: 숫자열 정렬 어긋남·우측치우침·이름 잘림·요소 넘침·행 겹침·**안쪽 여백(패딩) 침범(padcross)**. error 시 종료코드 1(발행 차단)
- 가동 템플릿: ranking-table(시안A)·market-daily + dummy-card
- 브랜드 토큰: `templates/_shared/base.css` (잉크네이비 팔레트, 폰트 3종 번들)

## 지식 (wirit 룩 — CEO.md §C가 원천)
- 잉크 테두리·다크 푸터(1.5배)·우상단 wirit 뱃지·좌상단 옅은 캡션·중앙 큰 제목 한 줄
- 헤드라인·숫자 Wanted Sans / 본문 Pretendard · 상승 레드/하락 코발트
- 로고 3단계 폴백: 실제 로고 > 브랜드 컬러칩 > 모노그램

## 학습 로그
| 날짜 | 배운 것 | 출처 |
|---|---|---|
| 07-18~19 | 헤더 문법 확정까지 오너 반복 조정 — 여백·정렬 민감도 높음, 시안 비교 제시가 정답 | 오너 |
| 07-19 | ranking-table에 photoStrip(하단 3분할 사진) 추가 — 소수항목 시 행 flex로 중간 채움. 브랜드 워드마크 칩·공급평형 | 오너 |
| 07-20 | 순위표 각 행이 별도 grid라 이름 `1fr`이면 값은 정렬되나 짧은 이름 뒤 중앙 빈 띠 큼(우측치우침). 이름 열을 밀도별 고정폭(--name-w)으로 두면 값 정렬 유지+숫자 중앙 배치 | 오너 지적 |
| 07-20 | **눈검수를 코드검수로 승격** — designQa로 정렬·여백·넘침을 좌표 측정해 자동 검출(회귀검증 통과). 앞으로 카드는 이 검수를 거친다 | 오너 "자동 검토 능력 키워줘" |
| 07-20 | 순위표 재설계: 값·보조 열을 **밀도별 고정폭(--val-w/--sub-w) + 가운데 정렬**로 두면 제목 라벨이 내용 위 정중앙에 온다. 제목 1행(62px)으로 낮추고 표 폰트↑. 1·2·3위 medal/rankClass 헬퍼로 금·은·동 처리 | 오너 지정 |
| 07-20 | **고정폭 열 합이 카드 안전영역(936px)을 넘으면 1fr 여백이 붕괴해 마지막 열이 패딩선을 침범**한다. 열폭/간격(--col-gap) 합을 안전영역 안에 맞추고 designQa로 검증할 것. 열간격도 --col-gap 변수화 | 오너 지적(직원수 선 넘음) |
| 07-20 | 밝은 종이 위 **은색(밝은 회색)은 대비 부족 → 금·은·동 모두 '진한 금속톤'으로 낮추고(#a97b0c/#5b6675/#9a5f27) 미세 그림자로 분리**. 가독성이 색보다 우선 | 오너 "1·2·3위 기업명 가독성 떨어짐, 테두리/그림자?" |
| 07-20 | **좌우 여백 대칭**: 트레일링 1fr 대신 `justify-content:center`로 고정폭 열 묶음을 가운데 정렬 → 좌우 여백 자동 동일, 숫자 블록도 우측으로 더 붙음 | 오너 "좌우 여백 동일하게" |
| 07-20 | **제목 fit-to-width**: 컨테이너 폭에 꽉 차게 폰트를 이분탐색으로 자동 확대(window.__wiritFit 훅, 렌더·검수 모두 폰트 로딩 후 호출). 제목 길이가 달라도 항상 꽉 참. 해시 동일=결정성 유지 | 오너 "제목 더 키워 꽉 채워" |
| 07-20 | **세로 꽉 채움**: 데이터 행 `flex:1 1 0`으로 본문 높이를 균등 분배 → 하단 빈 공간 제거·리듬 안정 | 오너 "아래 빈공간 없이, 안정감" |
| 07-20 | **캡처 대비 배경 워터마크** = 옅은 회색 "wirit." 대각 반복(opacity ~0.03, 내용은 그 위 z-index). 캡처·재업로드 시 출처 표기 유지 | 오너 요청 |
| 07-20 | 급/완은 **컬러 태그**(급=레드 강조·완=회색)로 분리해 시각 구분 | 오너 요청 |
| 07-20 | **표 헤드라인은 항상 내용 열 위 가운데 정렬**(모든 표 공통). 각 행이 별도 grid면 값 열을 고정폭으로 둬야 헤더와 값 x가 정렬됨(2단 표에서 확인) | 오너 "헤드라인 항상 가운데" |
| 07-20 | **2단 정보표(metro-2col)**: 항목 많고 공백 많을 때 좌우 2열로 분할해 촘촘·예쁘게. km/h 등 단위는 셀 안에 작은 회색으로 | 오너 "공백 많아 안예쁘다, 2단으로" |
| 07-20 | **지하철 표기방식 = 공식 노선 심볼 뱃지**(apt_lap 참고). wirit 재해석: 공식 노선색 원형에 번호(대형)/명칭형(신분당·수인분당 2행·GTX) 표기, **밝은 노선색(노랑·베이지·라임)은 진한 글자로 가독성 개선**(원본은 흰 글자로 뭉갬), 미세 그림자로 입체감. `metroBadge` 헬퍼 + `metro-lines.json`으로 재사용 | 오너 "이런 표기 참고, 나만의 센스로" |

**KPI**: 렌더 결정성 100%, 템플릿 재사용률 | **자동화**: R3(렌더), 신규 템플릿 R0
