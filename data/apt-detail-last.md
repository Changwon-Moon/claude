# 단지 주차대수 — 마지막 실행

- 대기열: 25줄
- 결과: **성공**
- ⚠️ 이번 실행은 **국토부에 붙지 않았습니다**(전부 이미 있음) — API 가 살아 있다는 증거로 쓰지 마세요

```
⏭ 이미 있음 — data/datasets/apt-detail/A44340013.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A13686302.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A44033010.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10028021.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10026600.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10023875.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10023329.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10023740.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10023833.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10023991.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10024453.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10024488.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10024831.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10025412.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10025621.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A10027344.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A12285703.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A13922114.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A13984004.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A15005705.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A15380403.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A44033010.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A44347020.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A44347021.json (다시 받으려면 force=1)
⏭ 이미 있음 — data/datasets/apt-detail/A44693813.json (다시 받으려면 force=1)

실제로 받아 본 단지 0곳 · 이미 있어 건너뛴 것 25곳
ⓘ 이번 실행은 **국토부에 한 번도 붙지 않았습니다** — 이 초록불은 API 가 살아 있다는 증거가 아닙니다.
✅ 대기열 전 줄 완료
```
