# 실행 로드맵 (Phase 0 ~ 4)

전체 기간: 약 **12~16주** (1인 운영 + AI 에이전트 기준). 각 Phase는 "이게 되면 다음으로 넘어간다"는 완료 조건(Exit Criteria)을 가진다.

> **실제 작업 진행 방법**(세션을 어떻게 나눠 발주하는지)은 [docs/EXECUTION_PLAN.md](./docs/EXECUTION_PLAN.md) 참고. Phase는 '무엇을'이고, 마일스톤 M0~M12는 '세션 단위로 어떻게'다. 매핑: Phase 1 = M0~M3, Phase 2 = M4~M7, Phase 3 = M8~M10, Phase 4 = M11~M12.

> ⚠️ **2026-07-19 우선순위 재정렬 (운영자 방향)**: 이 시스템의 척추는 **"오늘 뭘 만들지 결정하는 리서치·기획"**이다 — 자세한 방침은 [docs/RESEARCH_WORKFLOW.md](./docs/RESEARCH_WORKFLOW.md)(로드맵보다 우선). 리서치·기획은 **운영자가 직접(R0 수동) 시작**해 기준을 만들고, 그 결정 로그를 연료로 점진 자동화(R1→R3)한다. 아래 M4(자동 수치 수집)·M0/M5 발행부(인스타 자동 발행)는 **정기 고정물(증시 등)에만 먼저 적용하는 부수 트랙**으로 내려간다. 이슈·트렌드성 소재는 R0 수동을 오래 유지한다.

```
Phase 0        Phase 1           Phase 2            Phase 3             Phase 4
분석·기획  →   수동 MVP      →   반자동 파이프라인 →  풀 자동화        →   스케일·학습 루프
(1~2주)        (2~3주)           (3~4주)            (3~4주)             (상시)
"뭘 만들지"    "손으로 10장"     "AI가 만들고        "AI가 다 만들고     "성과 데이터가
                                 내가 마무리"        나는 승인만"        기획을 개선"
```

---

## Phase 0 — 콘텐츠 분석 & 계정 전략 (1~2주)

자동화보다 먼저 **"무엇을 무한 생산할 것인가"**를 확정하는 단계. 여기가 부실하면 뒤의 모든 자동화가 헛돈다.

### 할 일

1. **레퍼런스 역설계**: @flow, APT_LAP 최근 게시물 100~200개를 수집해 스프레드시트로 분류
   - 포맷(랭킹표 / 비교표 / 시계열 차트 / 지도 / 대진표 / 달력 / 단일 빅넘버)
   - 소재 카테고리(부동산 / 증시 / 교육 / 스포츠 / 정책 / 기업 / 밀리터리 등)
   - 반응(좋아요·댓글 수) — 어떤 [포맷 × 소재] 조합이 터지는지 파악
2. **콘텐츠 포맷 택소노미 확정**: 초기 런칭용 **6~8개 포맷** 선정 (→ [docs/TEMPLATES.md](./docs/TEMPLATES.md))
3. **니치 & 톤 결정**: flow(범용 랭킹·비교)형인지 APT_LAP(부동산 특화)형인지, 혹은 다른 니치인지. 특화형이 데이터 소스 자동화가 쉽고 팔로워 충성도가 높다.
4. **데이터 소스 확보 검증**: 선정한 포맷별로 데이터를 **API/크롤링으로 매일 뽑을 수 있는지** 실제로 확인 (→ [docs/DATA_SOURCES.md](./docs/DATA_SOURCES.md))
5. **계정 개설**: 인스타그램 프로페셔널(비즈니스) 계정 전환 — Graph API 발행에 필수. Meta 개발자 앱 등록.
6. **브랜드 아이덴티티**: 로고, 컬러 팔레트, 워터마크, 네이밍. 참고 계정처럼 "한눈에 우리 것"인 시각 언어.

### 완료 조건
- [ ] 포맷 6~8종 + 각 포맷의 데이터 소스가 문서로 확정됨
- [ ] 주간 발행 캘린더 초안 (예: 월-증시요약, 화-랭킹, 수-부동산, …)
- [ ] 인스타 비즈니스 계정 + Meta 앱 생성 완료

---

## Phase 1 — 템플릿 시스템 & 수동 MVP (2~3주)

**자동화 없이** 파이프라인의 핵심 부품(템플릿 렌더러)을 만들고, 손으로 10~20장을 발행해 포맷을 검증하는 단계.

### 할 일

1. **레포 스캐폴딩**: 모노레포 구성 (`packages/renderer`, `packages/pipeline`, `packages/dashboard`, `templates/`, `data/`)
2. **렌더러 구축** (가장 중요한 인프라):
   - HTML/CSS 템플릿 + JSON 데이터 → Playwright 스크린샷 → 1080×1350px PNG
   - 템플릿 1개당: `template.html` + `schema.json`(데이터 계약) + `sample.json`
   - CLI: `render --template ranking-table --data ./data/2026-07-18-topic.json --out ./out/`
3. **초기 템플릿 3종 제작**: 랭킹표, 2사 비교표, 시계열 차트(빅넘버+그래프). 참고 계정 수준의 밀도와 완성도가 나올 때까지 반복.
4. **수동 발행 10~20회**: 데이터는 손으로 수집·JSON 작성 → 렌더 → 인스타 수동 업로드. 이 과정에서 "JSON 스키마가 실무에 맞는지"를 검증한다 — 이 스키마가 이후 모든 에이전트의 출력 계약이 된다.
5. **발행 자동화 검증**: Instagram Graph API로 캐러셀(다중 이미지) 게시 테스트 성공.

### 완료 조건
- [ ] `JSON in → PNG out`이 3개 템플릿에서 안정적으로 동작
- [ ] 실계정에 10개 이상 게시물 발행, 포맷별 초기 반응 데이터 확보
- [ ] Graph API로 프로그래밍 발행 1회 이상 성공

---

## Phase 2 — 리서치·기획 에이전트 도입 (반자동, 3~4주)

"AI가 소재를 찾고 데이터를 만들어오면, 내가 다듬어서 발행"하는 단계. 에이전트 상세 스펙은 [docs/AGENTS.md](./docs/AGENTS.md).

### 할 일

1. **데이터 수집기(Collectors) 구축** — 에이전트가 아닌 **결정적 코드**:
   - 소스별 수집 모듈(공공 API, RSS, 크롤러) + 정규화 → `data/raw/{date}/{source}.json`
   - 매일 새벽 cron 실행, 실패 알림
2. **리서치 에이전트 v1**: 수집된 raw 데이터 + 뉴스 헤드라인을 읽고 "오늘 만들 만한 소재 후보 10개"를 근거·출처와 함께 브리핑
3. **기획 에이전트 v1**: 소재 후보를 [시의성 × 포맷 적합도 × 과거 성과 × 캘린더] 기준으로 스코어링 → 오늘의 콘텐츠 3~5건 기획안(포맷, 제목 시안, 필요한 데이터 명세) 작성
4. **편집 에이전트 v1**: 기획안에 따라 raw 데이터에서 템플릿 JSON을 생성. **수치는 코드 파서가 추출**하고, 에이전트는 제목·라벨·요약문만 작성. 출처 표기 자동 삽입.
5. **승인 인터페이스 v1**: 아침에 Slack/Telegram으로 "오늘의 기획안 + 미리보기 이미지"가 도착 → 운영자가 ✅/✏️/❌ 응답
6. **오케스트레이터 v1**: `수집 → 리서치 → 기획 → [승인 대기] → 편집 → 렌더 → [승인 대기] → 발행` 상태 머신. 실패 시 재시도·알림.

### 완료 조건
- [ ] 아침에 자동 브리핑이 도착하고, 운영자 개입은 승인/수정 지시뿐인 날이 주 3회 이상
- [ ] 소재 발굴부터 발행까지 운영자 실작업 시간 30분/일 이하
- [ ] 수치 오류(오보) 0건 유지 체계: 편집 에이전트 출력에 대한 자동 검증 룰 가동

---

## Phase 3 — 풀 파이프라인 & 승인 대시보드 (3~4주)

"모든 과정이 자동으로 돌고, 운영자는 웹 대시보드에서 감독·승인만" 하는 단계.

### 할 일

1. **디자인 QA 에이전트**: 렌더 결과 스크린샷을 비전 모델로 검사 — 텍스트 넘침, 겹침, 빈 셀, 저대비, 워터마크 누락 감지 → 자동 수정 재시도 or 반려
2. **팩트체크 에이전트**: 편집 결과의 모든 수치를 원본 raw 데이터와 대조(코드 검증) + 상식 범위 체크(LLM). 불일치 시 발행 차단.
3. **운영 대시보드 (웹)**:
   - 오늘의 파이프라인 칸반: 소재후보 → 기획안 → 제작중 → 검수대기 → 예약됨 → 발행됨
   - 게시물 미리보기 + 인라인 수정(제목·문구 직접 편집 → 자동 재렌더)
   - 원클릭 승인/반려/보류, 발행 시각 조정
   - 에이전트 로그·비용 모니터링
4. **발행 스케줄러**: 승인된 콘텐츠를 최적 시간대에 자동 발행, 캡션·해시태그 자동 생성(캡션 에이전트)
5. **콘텐츠 뱅크**: 시의성 없는 "에버그린" 콘텐츠(역대 랭킹, 총정리류)를 미리 만들어 재고로 쌓고, 이슈 없는 날 자동 투입
6. **장애 대응**: 소스 다운·API 한도·렌더 실패 시 폴백 경로(재고 투입, 알림) 정의

### 완료 조건
- [ ] 운영자 개입 없이 "브리핑 → 제작 → 검수대기"까지 매일 자동 도달
- [ ] 하루 1~3건 발행을 대시보드 승인 클릭만으로 유지 (실작업 10분/일)
- [ ] 발행 이력·성과·비용이 대시보드에서 한눈에 보임

---

## Phase 4 — 스케일 & 학습 루프 (상시 운영)

### 할 일

1. **성과 피드백 루프**: Instagram Insights API로 게시물별 도달·저장·공유·팔로우 전환을 수집 → 주간 성과 리포트 에이전트가 분석 → 기획 에이전트의 스코어링 가중치/소재 가이드 자동 갱신
2. **A/B 실험**: 제목 표현, 표지 디자인, 발행 시간대 실험을 파이프라인에 내장
3. **포맷 확장**: 성과 데이터 기반으로 신규 템플릿 추가 (분기 1~2종). 템플릿 추가만으로 콘텐츠 종류가 늘어나는 구조 유지.
4. **멀티 채널**: 동일 JSON → 릴스용 모션그래픽(자동 애니메이션), 스레드/트위터 텍스트 버전, 뉴스레터 재활용
5. **계정 확장**: 니치별 세컨드 계정(부동산 특화, 증시 특화 등)을 동일 파이프라인 + 다른 설정 파일로 운영
6. **수익화**: 협찬 표기 워크플로우, 광고 콘텐츠 템플릿, 애드센스형 랜딩(프로필 링크) 등

### 운영 지표 (북극성)
| 지표 | 목표 |
|---|---|
| 발행 빈도 | 1~3건/일, 결측일 0 |
| 운영자 투입 시간 | ≤ 10분/일 |
| 수치 오보 | 0건 (정정 게시 기준) |
| 저장률(저장/도달) | 상승 추세 유지 — 인포그래픽 계정의 핵심 지표 |
| 콘텐츠 단가 | API+인프라 비용 / 발행 건수 → 지속 하락 |

---

## 우선순위 요약 (뭐부터 만들 것인가)

1. **템플릿 렌더러** — 이것 없이는 아무것도 없다 (Phase 1)
2. **데이터 수집기** — 자동화의 원료 (Phase 2 초입)
3. **기획·편집 에이전트 + 승인 게이트** — 반자동 달성 (Phase 2)
4. **대시보드 + QA 에이전트** — 풀 자동화 (Phase 3)
5. **성과 학습 루프** — 무한 개선 (Phase 4)
