# CLAUDE.md — 위릿 카드 공장 세션 정본

> **이 문서 하나가 세션 규칙의 정본이다.** 저장소를 연 뒤에 필요한 모든 것이 여기 있다.
> 2026-08-30 통합 — 같은 내용이 `CLAUDE.md`·`docs/HANDOFF.md`·`docs/WIRIT_CARDS_SKILL.md`·
> 계정 스킬 **네 곳**에 흩어져 있었고, 그중 계정 스킬은 이미 65줄이 갈라져 있었다.
> 이 공장은 같은 판단이 두 곳에 있으면 갈라진다는 것을 세 번 배웠다
> (마감 절차 4곳·빌더 찾기 3곳·캡션 서명 2곳).
>
> 🗺 **다른 규칙의 정본이 어디인지는 이 문서 §13** 이 가리킨다.
> 📦 **저장소를 아직 안 열었다면** `wirit-cards` 스킬이 먼저다(토큰·clone·푸시 개통).
> 🔒 **세션 종료 절차의 정본은 [`docs/CARD_CHECKLIST.md` §7](docs/CARD_CHECKLIST.md)** — §11 엔 요약만 둔다.

---

## §0. 30초 오리엔테이션

**계정** wirit(@wirit_note) — 부동산·경제·통계 데이터 카드뉴스
**산출물** 1080×1350 PNG + 인스타 캡션. 업로드는 **오너가 직접** 한다(자동 발행은 2026-07-27 폐지)
**오너는 비개발자다** — 모든 대화는 한국어로, 기술 용어는 풀어서 설명한다

**핵심 약속 두 가지 — 이게 깨지면 계정이 죽는다:**

| 약속 | 무슨 뜻인가 |
|---|---|
| **오보 0** | 카드의 모든 수치는 원자료에서 **코드가** 뽑는다. LLM이 숫자를 만들지 않는다 |
| **픽셀 불변** | 발행했거나 오너가 "확정"한 카드는 이후 어떤 수정에도 픽셀이 안 바뀐다 |

이 둘은 취향이 아니라 절대 규칙이다(§8). 나머지는 다 협상 가능하다.

---

## §1. 매 세션 시작 — 이 순서를 지킨다

### 0. `node scripts/doctor.mjs` — 이 환경이 공장인가

```bash
pnpm install --frozen-lockfile     # 처음 한 번
node scripts/doctor.mjs            # 3~5분. 실제로 카드를 만들어 본다
```

진단기는 말로 확인하지 않고 **실제로 만들어 본다**: 빌더 실행 → 카드 JSON → 렌더 →
발행본 md5 대조 → 자동 검수.

| 결과 | 뜻 | 어떻게 |
|---|---|---|
| `✅ 카드를 만들 수 있습니다` | 이 환경은 공장이 맞다 | 정상 작업 시작 |
| `❌ 만들 수 없습니다` | 렌더·검수가 안 돈다 | **카드 픽셀을 건드리지 않는다.** 기준·기획·캡션 같은 문서 작업만 |

> ⚠️ **가장 위험한 상태**: 렌더가 안 되는 환경에서 "설명은 다 되니까" 카드를 고치는 것.
> 그러면 검수가 사람 눈으로 돌아가고, 그게 오보 0이 깨지는 지점이다.
> ❌면 오너에게 **"이 환경에서는 카드 제작을 못 합니다"라고 분명히 말한다.**

**A등급(필수)만 통과하면 카드 제작은 된다.** B등급(API 키)은 *새 데이터 수집*에만 필요하고,
없어도 `data/datasets/` 의 기존 데이터로 카드는 나온다.

**푸시 길이 열려 있는지도 여기서 본다.** 막혀 있으면 **작업을 시작하기 전에** 오너에게 말한다.
다 만들고 나서 말하면 늦다(2026-08-06: 카드를 네 번 고쳐 확정까지 마친 뒤에야 알았고
커밋 6건이 컨테이너에 갇혔다). 막혀 있어도 카드 제작·검수·캡션·PNG 전달·인스타 발행은
전부 된다 — 안 되는 건 저장, 관제탑 화면 갱신, 수집 워크플로 방아쇠뿐이다.

### 1~5. 읽는 순서

1. **`STATUS.md`** — 어디까지 왔고 다음 세션이 뭘 알아야 하는지
2. **`docs/CARD_CHECKLIST.md`** — 카드를 만들거나 고치는 작업이면 **필수**. §2(재발 금지)를 훑는다
   - 🏢 청약·분양이면 **`docs/guides/청약분양-카드-기준.md`** 도 필수(판형 `danji-cover@1` 하나뿐)
   - 🚇 노선 시세면 **`docs/LINE_CARDS.md`**
   - 🏆 신고가면 **`docs/guides/신고가-카드-기준.md`**
3. **`company/CEO.md`** — 오너 판단 원칙 100여 개(전략·콘텐츠·디자인·프로세스)
4. **`docs/DATA_REFRESH.md`** — 정기물이 사람 없이 굴러가는 배관.
   "이건 내가 눌러야 하나?" 싶으면 여기부터 본다(대개 안 눌러도 된다)
5. 계약 문서: `docs/ARCHITECTURE.md` · `TEMPLATES.md` · `DATA_SOURCES.md`
   - 🔌 **새 데이터가 필요하면 `docs/API_CONNECTIONS.md` 를 먼저 읽는다**(§6)

> **이 순서가 왜 강제인가**: 2026-07-30에 오너가 **이미 정해둔 기준을 열 번 넘게 다시 말하게** 됐다.
> 원인은 기준이 없어서가 아니라, 긴 학습 로그에 흩어져 있어 아무도 작업 전에 안 훑어서였다.
> `CARD_CHECKLIST.md` 가 그 사건 때문에 생긴 **단일 창구**다.

### 캡션은 쓰기 전에 과거를 본다

`data/review/captions/` 의 최근 발행본과 `_alt/README.md` 를 읽는다. `_alt/README.md` 는
오너가 **무엇을 잘라냈는지**를 적어 둔 파일이다 — 길게 쓴 뒤 잘리는 것보다
처음부터 그 길이로 쓰는 편이 빠르다.

**캡션의 "확정 뒤"는 오너 승인 순서를 말하는 것이지 파일이 생기는 시점이 아니다.**
`confirm.mjs` 는 캡션 파일이 **없으면 확정을 거부**하고 `produce-card` 도 캡션으로 금액을 대조한다
— **파일은 확정 전에 있어야 한다.** CEO.md §211 이 말하는 것은 "카드 모양이 오너 손을 떠나기 전에
캡션 문장을 다듬느라 시간 쓰지 말라"는 뜻이다. 초안은 빌드 직후 만들고,
**문장 손질만** 확정 뒤로 미룬다.

---

## §2. 이 공장의 구조

```
데이터 수집 → 데이터셋 → 빌더 → 카드 JSON → 렌더 → PNG
 (API·Actions)  (검증됨)   (코드)   (계약)   (Playwright)
                                              ↓
                                        자동 검수(designQa)
                                              ↓
                                    캡션 → 검수 리포트 → 발행 대기
```

| 층 | 어디에 | 무엇을 보장하나 |
|---|---|---|
| **데이터셋** | `data/datasets/*.json` | `meta.verified: true` 인 것만 카드가 될 수 있다. `meta.unit` 으로 단위 확정 |
| **빌더** | `scripts/build-*.mjs` | 수치를 **코드로** 추출. provenance 기록. 가드에 걸리면 던진다 |
| **템플릿** | `templates/{이름}/` | HTML/CSS + `schema.json`(입력 계약) + `config.json`(크기) |
| **렌더러** | `packages/renderer` | Playwright 스크린샷. 동일 입력 = 동일 픽셀 |
| **검수** | `packages/renderer/src/designQa.ts` | 좌표를 **실측**해 겹침·넘침·정렬을 잡는다 |
| **명세** | `data/review/{builders,sets,pixel-baselines}.json` | 무엇을 만들고·무엇을 발행하고·픽셀 기준이 뭔지 |

```
├── CLAUDE.md(세션 정본 + §13 문서 지도) · STATUS.md(지금 상태) · README.md(소개)
├── docs/                # 기준 문서 + guides/(비개발자용) + archive/(사료)
├── packages/            # renderer · collectors · pipeline · dashboard-static
├── templates/           # 디자인 템플릿
├── data/                # datasets / review(명세) / content / out(gitignore)
├── research/            # 소재 결정 로그·아이디어·패턴
└── company/             # CEO.md(판단) + teams/(팀별 방식)
```

**기술 결정 — 이미 확정, 재논의하지 않는다:**
TypeScript + Node.js, pnpm workspace 모노레포 · 렌더링은 HTML/CSS + Playwright 스크린샷 ·
LLM 은 Claude API(LLM 검수가 없어도 코드 검수로 카드는 나온다) ·
정기 수집·재생산은 GitHub Actions cron · **발행은 오너가 직접**.

> **왜 이미지 생성 AI 를 안 쓰나**: 텍스트·숫자 정확도가 보장되지 않고, 매번 결과가 달라
> 브랜드 일관성이 무너지고, 무엇보다 **회귀 검증이 불가능**해진다. HTML/CSS 는 픽셀 단위로 재현된다.
> 초기의 자동 발행·상주 에이전트 비전은 `docs/archive/VISION-automation.md` 에 보존.

---

## §3. 자주 쓰는 명령 (이게 사실상 전부다)

```bash
# 카드 전체 재생성 + 자동 디자인 검수  ← 내보내기 전 필수
node scripts/rebuild-cards.mjs

# 환경 자가진단 + 발행본 픽셀 회귀
node scripts/doctor.mjs

# 관제탑 화면 스모크
node scripts/smoke-tower.mjs

# 관제탑 다시 생성 — 이걸 쓴다(폰트 복사 + _site 조립까지 한 번에)
node scripts/build-tower-site.mjs

# 카드 한 장만 렌더 (경로는 절대경로로!)
pnpm -s --filter @wirit/renderer render --data /절대경로/카드.json --out /출력폴더

# 디자인 검수만
pnpm -s --filter @wirit/renderer qa data/content/{날짜}/{카드}.json

# 발행 전 종합 검수(결정성 + QA + 캡션 린트) → data/review/{label}.json
cd packages/pipeline && pnpm -s review /절대경로/카드.json \
  --caption /절대경로/캡션.txt --label {label} --out /절대경로/data/review

# 자동생성 문서 가지치기(14일 초과) — 워크플로가 매일 자동으로 부른다
node scripts/prune-auto-docs.mjs

# 카드를 PNG 전에 HTML 한 장으로 모아 보기 (오너 2026-08-31 "작업은 html로 먼저")
node scripts/preview-html.mjs --set <세트라벨>

# 배관·문서 상태 검사 — doctor 가 부르지만 따로도 돈다
node scripts/check-doc-links.mjs        # 살아있는 문서의 깨진 링크
node scripts/check-orphan-scripts.mjs   # 아무도 안 부르는 스크립트
node scripts/check-skill-sync.mjs       # 계정 스킬 ↔ 저장소본 갈라짐
node scripts/prune-status.mjs           # STATUS 가 다시 서사 창고가 됐나
node scripts/check-schedule-docs.mjs    # 문서가 없는 예약을 가르치나
```

**`error` 가 하나라도 있으면 내보내지 않는다.** `warn` 은 이유를 대고 넘길 수 있다.

> **갓 clone 한 환경에서 스모크가 떨어질 때**(2026-07-31): `packages/dashboard/fonts/` 와
> `data/out/` 은 gitignore 대상이라 clone 직후엔 없다. 떨어지는 항목은 전부
> "생성물이 아직 없다"이지 코드 문제가 아니다.
> `node scripts/render-sets.mjs` → `build-tower-site.mjs` → `smoke-tower.mjs` 순으로 채운다.

---

## §4. 새 카드 하나 만드는 흐름

> ⚠️ **단계마다 오너 승인을 받는다**(2026-07-31 오너 지적).
> ① 어떤 소재로 갈지 → ② 어떤 형태·제목으로 그릴지 → ③ 제작 → ④ **확정 후** 캡션 손질.
> 한 번에 완성해 넘기면 오너는 되돌릴 수 없는 상태로 받는다.

1. **소재 확정** — `research/ideas.json` 에 있거나 오너가 채팅으로 준다
2. **데이터 확인** — `data/datasets/` 에 있나? 없으면 수집부터(§6). `verified: true` 인가?
3. **템플릿 선택** — `docs/TEMPLATES.md`. 기존 것으로 되면 **새로 만들지 않는다**
4. **빌더 작성** — `scripts/build-{이름}.mjs`
   - 수치는 전부 데이터에서 계산. 손으로 적은 숫자 0개
   - "역사상 최고"·"전체"·"N년 연속" 같은 말은 **계산이 확인했을 때만** 나가게 게이트
   - 단위 환산은 `meta.unit` 확인 후. 아니면 throw
5. **렌더 → 검수** — `designQa` error 0
6. **캡션 생성** — 빌더가 만든다(손으로 적으면 다음 갱신에 카드와 어긋난다)
7. **등록** — `data/review/sets.json`(발행 단위) + `builders.json`(재생산).
   **등록해야 관제탑에 뜬다**
8. **종합 검수** → `data/review/{label}.json` 에 PASS
9. **오너에게 렌더 PNG 전송** — 눈으로 볼 수 있는 형태로
10. 오너가 "확정"하면 → `node scripts/confirm.mjs`

### 확정 ≠ 발행 승인, 그리고 고정물 ≠ 정기물

**고정물**(한 번 만들고 안 바뀜)은 `pixel-baselines.json` 에 md5 를 넣는다.
**정기물**(자료가 갱신되면 다시 그려짐 — 신고가·노선 시세 등)은 **넣지 않는다.**
넣으면 다음 수집일에 doctor 가 통째로 빨간불이 된다. `sets.json` 의 `confirmedMd5` 에만 남긴다.

---

## §5. 트리거 — 오너가 이렇게 말하면

### 🚇 "N호선 / 신분당 역세권 (대장) 아파트 시세"

원커맨드 하나로 리프레시→캡션→빌드→렌더→QA 가 전부 자동으로 돈다.
손으로 숫자를 고치거나 단계를 나눠 묻지 않는다.

```bash
node scripts/line-card.mjs 3호선     # 문장째 넘겨도 됨
```
- 새 달을 포함하려면 `--collect 202608` → push → pull 후 다시 실행
- **아직 없는 노선**은 자동 불가 — `docs/LINE_CARDS.md` §7(큐레이션)을 먼저 밟는다

### 🏢 "OO 단지 청약 카드"

```bash
node scripts/danji-card.mjs "<단지명>" --photo <조감도>
```
조회→데이터셋 항목→사진 설치→크롭 재계산→빌드→렌더→QA 가 한 번에 돈다.
판형은 `danji-cover@1` **하나뿐이고 새 템플릿을 만들지 않는다.**

### 🏆 "오늘의 신고가" (매일 04:55 KST 예약 → 텔레그램 알림 → 카드)

판형은 `singo-record@1` 하나로 고정. **전체 절차는 `docs/guides/신고가-카드-기준.md` §6 에 있다.**

```bash
node scripts/verify-singo-pipe.mjs        # ⓪ 배관 점검 (키·망 불필요)
# ① kaptCode 를 사람이 짚는다 (apt-hhld.json 의 byKapt)
# ② 빌더를 먼저 부딪친다 → 곡선 자료가 없으면 써야 할 대기열 줄을 찍고 멈춘다
# ③ 대기열 3개에 줄을 쓰고 한 번에 푸시 → Actions → pull
#    결과는 Actions 로그가 아니라 data/{singo-history,apt-station,apt-detail}-last.md 에서 본다
# ④ sets.json · builders.json 에 등록  ← 없으면 produce-card 가 즉시 죽는다
node scripts/build-singo-record.mjs --apt "<단지>" --type 84 --kapt <A########> --publish
node scripts/gen-singo-caption.mjs data/content/<날짜>/<카드>.json --out <세트의 caption 값>
node scripts/produce-card.mjs <세트라벨>
```

**자주 밟는 지뢰 셋:**
- `pnpm render` 를 직접 부르지 않는다. 렌더러 CLI 인자는 `--data`/`--out` 둘뿐이라
  옛 문서의 `--outdir`·`--qa` 는 **조용히 무시되고** PNG 가 엉뚱한 곳에 떨어진다
- `--out` 은 세트 라벨이 아니라 세트의 `caption` 값이다. 어긋나면 검수가 캡션을 못 찾는데
  `produce-card` 는 **경고만 찍고 통과**한다
- 곡선(`singo-history`)은 선택이 아니라 **필수**다. 게다가 곡선·역 워크플로에는 cron 이 없어
  **실패하면 사람이 다시 밀어야 한다**

**손으로 하지 않는 것(전부 코드가 한다):**
- **세대수**는 `--kapt` 로 **사람이 짚어야만** 붙는다. 이름 자동 매칭 금지
  (2026-08-13 에 남의 단지 세대수가 알림으로 나갔다)
- **캡션 금액**은 생성기가 카드 `meta` 에서 꺼낸다. 보고 옮겨 적지 않는다
- **가까운 역·주차대수**는 자료가 있을 때만 붙고, 없으면 그 줄이 아예 없다 →
  주차가 안 와도 카드를 낼 수 있다. 나중에 들어오면 픽셀이 바뀌니 **확정을 다시 받는다**
- **고점·저점 라벨 자리는 빌더가 곡선을 피해 고른다.** 빈 자리가 없으면 경고를 찍는다 —
  그 경고가 뜨면 눈으로 본다
- **확정된 캡션은 생성기가 못 덮는다**(`state: 오너 확정` 이면 멈춘다)
- 관측 15개월 미만 / '지난 사이클 고점'이 12개월 이내인 단지는 **빌더가 던진다**

> ⚠️ **육안 검수를 건너뛰지 않는다.** 2026-08-16 에 `produce-card` 가 5장 모두 PASS 를 냈지만,
> 그림을 열어 보고서야 곡선이 라벨을 뚫은 것과 캡션의 시간 역행을 찾았다.
> **PASS 는 "볼 준비가 됐다"는 뜻이지 "됐다"는 뜻이 아니다.**

---

## §6. 데이터 수집 — API 는 이미 연결돼 있다

> 세션이 오너에게 *"KOSIS API 키를 주세요"* 라고 물은 적이 있다. 물을 일이 아니었다 —
> 그 키는 **이미 GitHub Secrets 에 있었고 워크플로가 매일 그걸로 데이터를 받고 있었다**(2026-08-12 오너 지시).

**세션 컨테이너에 키가 없는 것은 정상이다.** 세션은 키를 쥐지 않고 외부망도 막혀 있다.
수집은 **GitHub Actions 가** 하고, 세션은 **대기열 파일에 한 줄 밀어 푸시**해서 그걸 깨운다.

`doctor.mjs` 의 `⚠️ ○○_API_KEY 없음` 은 **"이 컨테이너에 없다"**는 뜻이지
**"저장소에 배관이 없다"**는 뜻이 **아니다.** 이 둘을 섞어 읽은 것이 오판의 원인이었다.

정본은 **`docs/API_CONNECTIONS.md`**, 의심되면 `node scripts/check-secrets.mjs`.

| 무엇 | 대기열(세션이 미는 손잡이) |
|---|---|
| **KOSIS** 인구·세대·이동·고령·출생·사망 | `data/kosis-probe-queue.txt` · `data/population-queue.txt` |
| **국토부 실거래** 매매 / 전월세 | `data/molit-queue.txt` · `data/molit-rent-queue.txt` |
| **청약홈** 분양·무순위 (매일 08:00 KST) | `data/applyhome-queue.txt` |
| **부동산원 R-ONE** 월간 전월세지수 / 주간지수 | `data/reb-weekly-queue.txt` |
| **DART** 평균연봉 · **ECOS** 금리·환율 · **증시** | `data/market-queue.txt` |
| **서울 열린데이터광장** 생활인구 (probe 배관까지만) | `data/seoul-probe-queue.txt` |
| **카카오 지오코딩** · 이미지 · 뉴스/트렌드 RSS | `data/geocode-queue.txt` · `data/photo-batch.tsv` · `research/article-queue.txt` |

> ⚠️ **대기열 파일은 일회성 방아쇠지 설정이 아니다.** 줄을 남겨 두면 예약 실행이 엉뚱한
> 인자로 다시 돈다. 쓰고 나면 비운다.

**KOSIS 에 새 표가 필요하면 — 추측하지 말고 찾는다:**
`kosis-probe-queue.txt` 에 `run <날짜> search=키워드` → 푸시 → `data/kosis-search.md` 에
orgId·tblId 가 적혀 온다 → `sources/kosis.ts` 에 `enabled:false` 로 넣고 다시 푸시(probe) →
`data/kosis-probe.md` 에서 축 확인 → `enabled:true`.
그전까지는 표 ID 를 검색결과 제목만 보고 `추정` 으로 박았고, 그 습관이 사망표 사인축 사고를 낳았다.

> 🔑 **키 이름이 한 글자만 달라도 에러 없이 조용히 건너뛴다.** 2026-07-31 에 알림용 키가
> 등록된 적 없이 몇 주가 지난 것을 발견했다. 스크립트는 키가 없으면 "건너뜁니다" 하고 정상 종료한다.

수집한 데이터는 `verified: false` 로 들어오고, 1차 출처 대조 후에야 `true` 로 올린다.

### 재시도와 포기는 **짝이다** (2026-08-26)

`molit.ts` 에 「연결 실패도 3번 재시도」만 넣었더니 같은 날 오후 곡선 수집기가
**60분 잡 시간을 통째로 태웠다.** 이제 곡선도 「5번 쉬고도 안 열리면 접는다」를 한다.
**한쪽을 세게 하면 반대쪽이 받아 줘야 한다.**

그리고 **두 실패를 같은 것으로 세지 않는다**:
`SERVICETIMEOUT` 은 다시 밀면 오고(기다림), 「호를 못 찾았다」는 다시 밀어도 같다(조사).
08-26 공급면적 4건이 정확히 둘로 갈렸다.

> ⚠️ 같은 실행 안에서 기다려도 **IP 가 안 바뀐다.** 필요한 것은 대기가 아니라 **새 러너**다
> — 그래서 신고가는 `singo-retry.yml` 사다리(+20분·+1시간·+3시간)로 받는다.

### 오너가 기사 링크를 줬을 때

작업 세션은 네이버·다음을 못 읽는다. 하지만 **git push 는 된다** — 푸시를 방아쇠로 쓴다.

```bash
echo "https://…" >> research/article-queue.txt
git commit -am "기사 요청" && git push        # ← 이 푸시가 워크플로를 깨운다
```
결과는 성공·실패와 무관하게 `research/article-fetch-last.md` 에 적힌다. **이 파일이 유일한 눈이다.**

- ❌ **네이버 모바일 기사(`n.news.naver.com`)는 이 통로로 못 읽는다.** 다섯 갈래를 다 시도해
  확인했다(2026-07-31). 선택자나 대기 시간의 문제가 아니라 DOM 접근 자체가 막힌다. 다시 파지 않는다
- ✅ **언론사 자체 도메인은 된다.** 네이버 기사 하단의 "기사원문" 링크가 그것이다 —
  **오너에게 그 링크를 달라고 하는 게 1순위**다
- 그래도 안 되면 오너에게 **화면 캡처**를 받는다. 캡처 한 장이 왕복 다섯 번보다 빠르다

> **오너에게 "GitHub 에서 Actions 를 눌러 주세요"라고 하지 않는다.** 그러면 자동화가 아니다.

---

## §7. 관제탑 (웹 화면)

2026-07-30 오너 승인으로 **탭 5종**만 남았다: 발행 · 소재 · 보관함 · 회사 · 자산.

관제탑이 하는 일은 셋뿐이다:
1. **발행 결재** — 카드 실물·캡션 확인 → `🚀 발행 승인`
2. **발행 기록** — 인스타에 올린 뒤 `✅ 인스타에 올렸습니다`
   (자동 발행을 안 하므로 **오너만 알 수 있는 사실**이다.
   이 버튼이 없어 시스템이 "발행 0건"이라고 거짓 보고한 적이 있다)
3. **소재 조망 · 보관함 열람**

**폐지된 것**: 결정함 · 파이프라인 칸반 · 지시함 · 요청 대장 · 성과 탭.
→ **소재 등록·제작·수정 지시의 단일 입구는 채팅(작업 세션)이다.**

> **조립 ≠ 배포.** Cloudflare 배포는 `tower-deploy.yml` 이 하고 **푸시가 방아쇠**다.
> 올라갔는지는 Actions 실행 기록으로 **본다**(추측해서 보고하지 않는다).

---

## §8. 절대 규칙

| 금지 | 왜 |
|---|---|
| **수치를 LLM 이 만들게 하기** | 오보 0의 근간. 반드시 코드로 추출 + `provenance` |
| **발행본·확정본 픽셀 바꾸기** | 그날 무엇을 발행했는지의 증거가 사라진다. 새 스타일은 **variant 로 격리** |
| **API 키·토큰 커밋** | GitHub Secrets 또는 `.env`(gitignore) |
| **렌더에 타임스탬프·랜덤 넣기** | 결정성이 깨지면 회귀 검증이 불가능 |
| **템플릿 스키마 깨는 변경** | 새 버전으로(`ranking-table@2`). 참조는 항상 버전 포함 |
| **"이 칸"을 손으로 지정** | 데이터가 바뀌면 엉뚱한 칸이 된다. 조건을 **실측**해 코드가 고르게 |
| **오너 메모의 수치를 그대로 받아 적기** | 계산값과 다르면 계산값을 쓰고 차이를 보고(9.6% 요청 → 계산값 9.2%) |
| **정기물에 '조건부 예외' 두기** | "이럴 땐 이렇게"는 결국 **카드마다 모양이 다른 것**으로 나타난다 |
| **검수 '통과'를 그대로 믿기** | 빌더가 `--publish` 없이 돌면 결과가 `data/out/_spike` 로 가는데 확정은 `data/content/` 를 본다 → **옛 판본을 검수하고 통과**한다(2026-08-16 실제 사고) |

- 오너가 직접 해야 하는 작업(계정·키 발급·결제)은 단계별 가이드를 `docs/guides/` 에 쓴다
- 오너에게 선택지를 줄 때는 **렌더된 이미지·실행 결과 등 눈으로 비교 가능한 형태**로

---

## §9. 학습 프로토콜 — 이 회사의 기억

이 저장소는 "회사"다(`company/README.md`). 오너 피드백은 **반드시 기록한다.**

| 종류 | 어디에 |
|---|---|
| 판단·취향·전략 | `company/CEO.md` 판단 원칙 (일회성 지시는 제외, 애매하면 "확인 대기") |
| 특정 팀의 일하는 방식 | `company/teams/{팀}.md` 학습 로그 |
| 소재 선택 이유 | `research/DECISION_LOG.md` |
| 터진 콘텐츠 패턴 | `research/PATTERN_LIBRARY.md` |

**오너가 같은 지적을 두 번 하면 그건 문서화 실패다.** 그때는 세 곳을 함께 고친다:
① `docs/CARD_CHECKLIST.md` §2 에 항목 추가
② 좌표로 잴 수 있으면 **`designQa` 검수항으로 승격**
③ 관련 팀(`design.md`·`qa.md`) 학습 로그

새 검수항을 넣었으면 **일부러 어긋나게 되돌려 실제로 잡히는지 확인한다.**
통과만 보면 검사가 켜졌는지 알 수 없다.

> **검수는 "통과했다"가 아니라 "무엇을 쟀다"로 읽는다.** 검사 목록에서 빠진 것은
> 통과한 게 아니라 **검사받지 않은 것**이다.

### 검사를 새로 걸었으면 세 가지를 확인한다 (2026-08-31)

검사를 만드는 것과 그 검사가 **실제로 막는 것**은 다른 일이다. 하루에 세 번 걸렸다:

1. **깨뜨려 본다.** 일부러 어긋나게 만들고 **빨간불이 실제로 뜨는지** 본다.
   문서 링크·고아·STATUS 검사를 「성공이면 필수, 실패면 경고」로 짰더니
   **깨진 상태에서 필수 목록에서 아예 사라지고** 「✅ 카드를 만들 수 있습니다」가 그대로 떴다.
2. **판정은 종료코드로 받는다.** 출력 문자열로 판정하면 검사기가 자기 출력에 걸려 넘어진다 —
   `prune-status` 가 넘친 블록 목록을 찍었는데 그 제목의 ✅ 를 doctor 가 성공으로 읽었다.
3. **그 검사가 도는 자리에 실제로 방아쇠가 있는지 본다.** 문지기에 「문서 링크」를 걸어 뒀는데
   방아쇠 경로에 `docs/**` 가 없어서 **문서만 고쳐 푸시하면 문지기가 안 떴다.**

그리고 **등급을 고른다**: 세션이 스스로 고칠 수 있으면 **필수**(막는 게 말이 된다),
오너만 고칠 수 있거나 카드 제작과 무관하면 **경고**. 아무거나 필수로 만들면
빨간불이 흔해져 안 읽힌다 — 맞는 것을 매번 지적하면 지적을 안 읽는다.

---

## §10. 세션 일지 — 시작할 때와 끝날 때 한 줄씩

여러 세션이 같은 저장소를 동시에 민다. 서로 뭘 하는지 몰라 같은 소재를 두 번 설계하거나
남이 확정한 카드를 건드린 일이 있었다.

```bash
node scripts/session-log.mjs start "무슨 작업을 하는지 한 줄"
node scripts/session-log.mjs block "저장소 푸시가 막혔다"   # 막히면
node scripts/session-log.mjs done  "카드 4장 확정 · 커밋 완료"
```

`data/sessions/` 에 쌓이고 관제탑 `/sessions.html` 과 `data/sessions/BOARD.md` 에 모여 보인다.
**커밋해야 다른 세션이 본다.**

> 이 일지는 **겹치는 것**을 막는다. 망가지는 것은 문지기(guard)가 막는다 — 다른 문제다.

---

## §11. 세션 종료 전

> 🔒 **정본은 [`docs/CARD_CHECKLIST.md` §7](docs/CARD_CHECKLIST.md) 하나다.** 여기엔 요약만 둔다.
> 마감 절차가 네 군데에 다르게 적혀 있어서 **서명 순서 오류가 한 곳만 고쳐지고 나머지는
> 계속 틀린 것을 가르쳤다**(2026-08-16d). 절차를 바꿀 일이 생기면 **§7 만** 고친다.

```bash
node scripts/close-session.mjs      # 실행은 wirit-close 스킬이 이끈다
```

- [ ] **먼저 쓰고 그 다음에 검사한다** — 뒤집으면 초록불에 안심해서 배운 것을 안 적고 닫는다.
      §9 의 표대로 **성격별로** 나눠 적는다
- [ ] 검사는 **`tower-deploy.yml` 과 같은 순서**로, **서명은 맨 뒤**다 —
      `doctor` 도 카드를 다시 만들며 서명을 날린다
- [ ] **커밋 & 푸시.** 저장소가 **공개**이므로(2026-08-16 전환) 밀기 전에 비밀이 섞였는지 본다
- [ ] 푸시가 정말 막혀 있으면 **패치로 뽑아 오너에게 보낸다**
      (`git format-patch origin/<브랜치>..HEAD --stdout`). 이건 오너가 터미널에서 칠 물건이 아니라
      **다음 세션의 나에게 넘기는 물건**이다. **컨테이너 안에만 있는 커밋은 없는 것과 같다**
- [ ] 클로드 프로젝트에 **인수인계 문서**, 오너에게 **렌더 PNG** 로 결과 전달

---

## §12. 경계선 — 무엇을 어디서

| 층 | 어디서 |
|---|---|
| 정기 데이터 수집·재생산, 관제탑 배포, 완성본 보관 | **GitHub Actions** (키는 Secrets) |
| 카드 제작(빌더·렌더·검수·캡션), 소재 발굴·판단, 기준 갱신 | **작업 세션**(코워크/클로드코드) |
| 승인·인스타 업로드·성과 기입 | **오너** (자동 발행 안 함) |

**자산은 창구가 아니라 저장소에 있다.** 이 저장소를 붙이는 순간 세션 규칙(이 문서)·
제작 기준(`CARD_CHECKLIST.md`)·오너 원칙(`CEO.md`)이 전부 자동으로 상속된다.
창구를 옮기는 건 이사가 아니라 현관문을 하나 더 다는 일이다.

> **"팀"은 상주 봇이 아니라 세션이 작업 전에 읽는 기준 문서 묶음이다.** LLM 을 실제로
> 호출하는 코드는 검수용(`llmReview.ts`) 하나뿐이고, 각 세션이 그때그때 해당 팀의 모자를 쓴다.
> 그래서 창구를 바꿔도 사라질 실행 자산이 없다.

> ⚠️ **렌더 환경은 한 곳으로 고정한다.** 결정적 렌더의 전제가 "같은 기계"다.
> 두 창구에서 번갈아 렌더하면 픽셀 기준선이 갈라진다.

**무인 스케줄은 승인 앞까지만.** 정해진 API 를 정해진 시각에 긁는 것은 Actions,
**판단이 필요한 일**(소재 후보 추리기, 정기물 재생산→결재 대기)은 코워크 스케줄.

---

## §13. 문서 지도 — 어느 규칙의 정본이 어디인가

주제마다 **정본 문서 하나**만 두고 나머지는 그곳을 링크한다. 같은 규칙을 두 곳에 복사하지 않는다.

### 시작·규칙

| 문서 | 무엇의 정본인가 |
|---|---|
| **이 문서** | **세션 규칙 정본.** 환경 자가진단·읽기 순서·공장 구조·자주 쓰는 명령·새 카드 흐름·트리거·데이터 수집·절대 규칙·학습 프로토콜·세션 종료 |
| [STATUS.md](STATUS.md) | **현재 상태.** 지금 확정본·진행 중·다음 할 일 (과거 서사는 `docs/archive/STATUS-history.md`) |

### 카드 만들기

| 문서 | 무엇의 정본인가 |
|---|---|
| [docs/CARD_CHECKLIST.md](docs/CARD_CHECKLIST.md) | **작업·검수 절차, 재발 금지 항목** — 카드를 만들거나 고치면 필수 |
| [docs/TEMPLATES.md](docs/TEMPLATES.md) | **템플릿별 픽셀·필드 계약**(수치 규격의 정본) |
| [docs/BRAND.md](docs/BRAND.md) | **색·타이포·워터마크 슬롯**(브랜드 규격의 정본) |
| [docs/REVIEW_RUBRIC.md](docs/REVIEW_RUBRIC.md) | **검수 판정 등급·LLM 렌즈** (designQa 항목 목록의 정본은 `designQa.ts` 코드) |
| [docs/LINE_CARDS.md](docs/LINE_CARDS.md) | **지하철 노선 시세 카드 시리즈** |
| [docs/CAPTION.md](docs/CAPTION.md) | **캡션 규칙**(포맷·금지어·서명·생성) — 2026-08-31 통합 |
| [docs/guides/프로필-세팅안.md](docs/guides/프로필-세팅안.md) | **인스타·스레드 프로필 운영** |
| [docs/guides/릴스-제작-기준.md](docs/guides/릴스-제작-기준.md) | **릴스 판형·파이프라인** |
| [docs/guides/control-tower-live.md](docs/guides/control-tower-live.md) | **관제탑 운영·라이브 모드** |

### 자산·데이터

| 문서 | 무엇의 정본인가 |
|---|---|
| [docs/ASSET_HUB.md](docs/ASSET_HUB.md) | **자산 등록·카탈로그·라이선스 분류**(라이선스 규칙의 정본) |
| [docs/IMAGE_AUTOMATION.md](docs/IMAGE_AUTOMATION.md) | **자산 자동 취득**(로고·사진, push→Actions) |
| [docs/DATA_SOURCES.md](docs/DATA_SOURCES.md) | **데이터 소스 카탈로그**(무엇이 있고 어떻게 접근하나) |
| [docs/DATA_REFRESH.md](docs/DATA_REFRESH.md) | **정기물 자동 갱신 운영**(무엇을 기계가·무엇을 사람이) |

### 판단·조직·소재

| 문서 | 무엇의 정본인가 |
|---|---|
| [company/CEO.md](company/CEO.md) | **오너 판단 원칙**(전략·콘텐츠·디자인·프로세스) |
| [company/teams/](company/teams/) | **팀별 일하는 방식·도구·학습 역사**(레퍼런스) |
| [docs/AGENTS.md](docs/AGENTS.md) | **조직 개요·6축 소재 채점 기준·검수 6항** |
| [docs/RESEARCH_WORKFLOW.md](docs/RESEARCH_WORKFLOW.md) | **리서치 방향·소재 채점 rubric의 정본** |
| [research/DECISION_LOG.md](research/DECISION_LOG.md) | **소재 결정 로그**(왜 골랐/버렸나 — AI 교보재) |
| [research/PATTERN_LIBRARY.md](research/PATTERN_LIBRARY.md) | **터진 콘텐츠 재사용 공식** |
| [docs/CONTENT_STRATEGY.md](docs/CONTENT_STRATEGY.md) | **콘텐츠 티어 믹스·시리즈·발행 리듬** |

지난 계획 문서(로드맵·발주서·자동 운영 매뉴얼 등)는 [docs/archive/](docs/archive/)에 보관돼 있다.

> ⚠️ **새 문서를 만들면 여기 한 줄을 넣는다.** 안 넣으면 없는 문서다.
> 그리고 **같은 규칙이 두 곳에 생기면 그 자리에서 합친다.** 나중은 없다 —
> 이 공장은 그렇게 다섯 번 갈라졌다(마감 절차 4곳·빌더 찾기 3곳·캡션 서명 2곳·
> 세션 규칙 4곳·캡션 규칙 3곳).

**문서가 아닌 것** — `data/*.md`(배관 상태) · `research/briefs/`·`docs/daily/`(자동생성,
14일 뒤 자동 정리) · `docs/archive/`·`docs/history/`(사료)는 읽는 물건이 아니라 도구다.

---
