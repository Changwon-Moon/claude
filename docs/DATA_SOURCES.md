# 데이터 소스 카탈로그

"무한 생산"의 원료. 원칙: **공식·공공 데이터 우선, API 우선, 크롤링은 최후**. 모든 콘텐츠에 출처를 표기하므로 소스의 신뢰도가 곧 계정의 신뢰도다.

## 1. 금융·증시 (데일리 고정 코너의 원료 — 100% 자동화 가능)

| 소스 | 데이터 | 접근 | 갱신 |
|---|---|---|---|
| 한국은행 ECOS API | 기준금리, 환율, 통화량, 경제심리지수 | 공식 API (무료, 키 발급) | 일/월 |
| KRX 정보데이터시스템 | 코스피/코스닥 지수, 종목 시세, 투자자별 매매동향 | 공식 API/CSV | 일 |
| 미국 증시 (Yahoo Finance, Stooq, Alpha Vantage 등) | 나스닥·S&P500·다우, 빅테크 종목, ETF | 무료 API/라이브러리 | 일 |
| 금융감독원 DART API | 사업보고서(연봉, 직원 수, 근속연수), 재무제표 | 공식 API (무료) | 수시 |
| 네이버금융/인베스팅 | 보조 시세 확인용 | 크롤링 (보조) | 일 |

→ `market-daily` 템플릿과 결합해 **첫 완전 무인 콘텐츠**("간밤의 미국 증시", "오늘의 한국 증시") 구현.

## 2. 부동산 (APT_LAP 니치)

| 소스 | 데이터 | 접근 | 갱신 |
|---|---|---|---|
| 국토교통부 실거래가 공개시스템 API | 아파트 매매/전월세 실거래 (단지·평형·금액) | 공공데이터포털 API (무료) | 일 |
| └ **수집기 구현(P1, 07-21)** | 아파트 매매 상세(getRTMSDataSvcAptTradeDev) 구·월 수집 → `data/datasets/molit/{LAWD}-{YYYYMM}.json` 캐시 | `@wirit/collectors` molitCli + `molit-collect.yml`(dispatch·월5일 cron), `MOLIT_API_KEY` | 월 |
| 청약홈 | 청약 일정, 경쟁률, 분양가 | **API 연결됨**(공공데이터포털 15098547) — `applyhome-collect.yml` 매일 08:00 KST. 신규 APT + 무순위. 키 `DATA_GO_KR_API_KEY` | 일간 |
| KB부동산 데이터허브 | 주간/월간 시세 지수, 지역별 통계 | 공개 통계 다운로드 | 주 |
| 한국부동산원 R-ONE | 지가·주택가격 지수, 거래량 | 공식 API | 주/월 |
| └ **수집기 구현(07-28)** | **전세가격지수·월세가격지수** 25개 구 월별 계열 → `data/datasets/reb-rent-index.json`. 통계표 ID는 하드코딩하지 않고 **이름으로 찾아 산출물에 기록**(개편 대비). 권역 묶음은 저장하지 않고 빌더가 코드로 만든다 | `@wirit/collectors` rebCli + `reb-collect.yml`(dispatch·매월 16일 cron), `RONE_API_KEY` ([발급 안내](./guides/reb-key.md)) | 월 |
| 국토부 보도자료 RSS | 부동산 대책, 정책 발표 | RSS/크롤링 | 수시 |

→ "○○구 동별 대표 APT", "N호선 라인 5년 변화", "월별 청약 빅위크" 같은 시리즈를 **지역/노선/기간 파라미터만 바꿔 무한 변주** 가능 — 참고 계정이 실제로 쓰는 방식.

### 2-1. 실거래 지역코드(LAWD_CD) 운용 — 2026-07-25 보강

- 목록: `packages/collectors/src/data/lawd-seoul.json` · `lawd-gyeonggi.json`(47개). CLI `--region seoul|gyeonggi`
- **코드는 분구·통합으로 죽는다.** 2026-02 화성시 4구 분구로 `41590`(화성시)·`41190`(부천시)이 무효화됐다.
  - 화성 만세 `41591` · 효행 `41593` · 병점 `41595` · **동탄 `41597`** / 부천 원미 `41192` · 소사 `41194` · 오정 `41196`
  - **수집 결과 0건이면 API 장애가 아니라 코드 폐기를 먼저 의심**하고 후보 코드를 실측으로 확정한다.
- 캐시: `data/datasets/molit/{LAWD}-{YYYYMM}.json`. **공용 폴더이므로 빌더는 반드시 지역 정규식으로 좁힌다**
  (`/^11\d{3}-\d{6}\.json$/` 등). 안 하면 데이터가 늘 때 과거 카드가 조용히 오염된다.

### 2-2. 행정경계 GeoJSON

| 파일 | 내용 | 주의 |
|---|---|---|
| `data/geo/seoul-districts.geojson` | 서울 25구 | — |
| `data/geo/korea-municipalities.geojson` | 전국 시군구 | 코드가 **행정구역코드**(서울 11, 경기 **31**) — 법정동코드(경기 41) 아님 |
| `data/geo/korea-submunicipalities.geojson` | 전국 읍면동 | 신설 구 **합성용**(예: 동탄구 = 3124044·3124060~62) |

- 폴리곤은 강하게 단순화돼 있다(서울 1개 구 ≈ 9정점). 이 거친 형태가 오히려 카드 스타일과 맞는다.
- **지리 요소는 손으로 그리지 않는다**: 하천 = 양안 그룹의 공유 정점, 시 경계 = evenodd 클립.

## 3. 통계·사회

| 소스 | 데이터 | 접근 |
|---|---|---|
| KOSIS 국가통계포털 API | 인구, 가구, 고용, 임금, 물가 등 모든 국가승인통계 | 공식 API (무료) |
| 공공데이터포털 (data.go.kr) | 부처별 수만 개 데이터셋 | API/파일 |
| e-나라지표 | 부처 핵심 지표 | 크롤링 |
| 법제처·국회 의안정보 | 법령 개정, 정책 | API |
| 각 부처 보도자료 RSS | 최저임금, 병역, 교육 정책 등 이슈 원천 | RSS |
| 서울교통공사 표정속도(1~8호선) | 노선별 표정속도 공식값 | 공공데이터포털 `data.go.kr/data/15088952` · 서울열린데이터광장 `OA-22492` (파일). 07-21 지하철 운행속도 카드 1차 검증에 사용 — 집계표 수치와 정확 일치 확인 |

## 4. 스포츠·엔터·시즌성

| 소스 | 데이터 | 접근 |
|---|---|---|
| FIFA/AFC, KBO, K리그 공식 기록 | 대진표, 순위, 우승 기록 | 크롤링/공개 데이터 |
| Transfermarkt 등 | 선수 시장가치 | 크롤링 (출처 표기) |
| 시즌 캘린더 (자체 구축) | 월드컵, 수능, 연말정산, 청약 일정 등 | 수동 + 에이전트 유지보수 |

시즌 캘린더는 리서치 에이전트의 입력으로 들어가 "다가오는 이벤트 선제 기획"을 가능하게 한다 (참고 계정의 월드컵 시리즈처럼).

## 5. 뉴스·이슈 감지 (소재 발굴용 — 데이터 원천 아님)

| 소스 | 용도 |
|---|---|
| 주요 언론사/통신사 RSS | 오늘의 이슈 감지 |
| 네이버 뉴스 랭킹, 구글 트렌드 | 대중 관심사 파악 |
| 각 부처·기관 보도자료 | 1차 출처 확인 |

**규칙: 뉴스는 "무엇이 이슈인지"를 알려주는 신호로만 쓰고, 콘텐츠의 수치는 반드시 1차 출처(공공 API·공식 발표)에서 다시 뽑는다.** 언론 보도 수치를 그대로 옮기지 않는다 — 오보 전파 방지 + 저작권 리스크 회피.

## 6. 성과 데이터 (내부)

| 소스 | 데이터 |
|---|---|
| Instagram Graph API — Insights | 게시물별 도달, 노출, 저장, 공유, 프로필 방문, 팔로우 |
| 자체 DB | 발행 이력, 포맷·카테고리 태그, 파이프라인 로그 |

## 수집기 구현 우선순위

1. **P0 (Phase 2 첫 주)**: 미국/한국 증시, ECOS 기준금리·환율 — 데일리 코너 가동
2. **P1**: 국토부 실거래가, 청약홈, 뉴스 RSS — 부동산·이슈 콘텐츠
3. **P2**: KOSIS, DART, 스포츠 — 랭킹·비교 콘텐츠 확장

## 법적·윤리 체크리스트

- [ ] 공공데이터는 이용약관상 출처 표기 조건 확인 (대부분 표기만 하면 상업적 이용 가능)
- [ ] 크롤링 대상은 robots.txt·약관 확인, 요청 속도 제한 준수
- [ ] 기업 로고 사용: 정보 전달 목적의 지명적 사용(nominative use) 범위 유지, 문제 제기 시 즉시 교체 프로세스
- [ ] 유료 데이터(리서치사 리포트 등)는 인용 범위 준수 또는 라이선스 계약
- [ ] 모든 게시물 하단 출처 표기 자동화 (편집 에이전트 필수 필드)

## 🧭 시군구 주민등록 인구 (KOSIS) — 2026-08-03 구현

| 항목 | 값 |
|---|---|
| 소스 | KOSIS 국가통계포털 OpenAPI `101 / DT_1B040A3`(행정구역(시군구)별 성별 인구수) |
| 엔드포인트 | `https://kosis.kr/openapi/Param/statisticsParameterData.do` (인증키 파라미터 `apiKey`) |
| 수집기 | `packages/collectors/src/sources/kosis.ts` + `parse/kosis.ts` + `kosisCli.ts` |
| 워크플로 | `.github/workflows/population-collect.yml` — 매월 3일 09:00 KST + `data/population-queue.txt` 푸시 |
| 키 | `KOSIS_API_KEY` (kosis.kr 오픈API 활용신청 — 즉시 발급·무료) |
| 산출 | `data/datasets/population-latest.json` · `data/datasets/population/{YYYY-MM}.json` |
| verified | **false** — 표 ID·필드 이름을 실제 응답으로 대조하기 전까지는 발행 금지 |

### ⚠️ 왜 행안부 주민등록 오픈API 가 아니라 KOSIS 인가
2026-08-03 에 두 가지를 **실측하고** 갈아탔다. 오너 초안은 "주민등록 먼저"였으나 계산이 달랐다.

1. **코드 체계가 지도와 안 맞는다.** 우리 지도는 통계청 행정구역코드다(동작구 `11200`·부산 `21xxx`·경기 `31xxx`).
   행안부 주민등록은 법정동코드라 동작구가 `11590`, 경기가 `41xxx` 다. 시도 2자리가 서울만 우연히
   같아서 얼핏 붙는 것처럼 보이는데, **시군구부터 완전히 다른 체계**다. 그대로 조인하면 엉뚱한 구에
   색이 칠해진다. KOSIS 는 통계청 체계라 지도에 그대로 붙는다.
2. **규격을 검증할 수 없었다.** `data.go.kr` 상세 화면이 세션에서 안 열려(robots) 행안부 API 의
   요청 파라미터·응답 필드를 한 글자도 확인하지 못했다. KOSIS 는 엔드포인트·파라미터·응답 필드
   (`C1`·`C1_NM`·`PRD_DE`·`DT`·`UNIT_NM`)가 공개 문서로 확인된다.

### 지도 — 시군구 경계는 2026년판을 따로 만든다
기존 `data/geo/korea-municipalities.geojson` 은 **통계청 2013년판**이다. 실측하니
청원군(2014 청주 통합으로 소멸)이 살아 있고, 인천 미추홀구가 아직 '남구'이고, 화성 4구
분구(2026-02)가 없다. 전국 인구 히트맵은 시군구를 **전수로** 칠하는 카드라 이 어긋남이 그대로 보인다.

| 파일 | 무엇 |
|---|---|
| `data/geo/korea-sgg-2026.geojson` | 2026-04-01 기준 **시군구 255개** 경계 |
| `data/geo/sgg-codes.json` | **통계청 코드 ↔ 법정동코드 ↔ 이름** 대조표 |
| `scripts/build-sgg-geo.mjs` | 행정동 경계를 시군구로 합치는 스크립트 |
| `.github/workflows/sgg-geo-refresh.yml` | 행정구역이 바뀌었을 때만 돌린다 |

원본은 `vuski/admdongkor` 의 행정동 경계(SGIS 파생, 상업적 이용 가능·출처 표기 권장).
이 파일 하나가 `adm_cd`(통계청)와 `sgg`(법정동) **두 코드를 다 준다** — 그래서 인구(KOSIS)와
실거래(국토부)를 같은 지도 위에 얹을 수 있다. 카드 푸터 출처: `통계청 SGIS / vuski/admdongkor`.

**합치는 방법**: 폴리곤 합집합(union)을 계산하지 않는다. 같은 시군구 안에서 맞닿은 변은
한쪽에서 a→b, 다른 쪽에서 b→a 로 정확히 한 번씩 나타나므로 **쌍이 되는 변을 지운다.**
원본에 없던 좌표가 하나도 생기지 않아 부동소수점 합집합이 만드는 틈·자기교차가 원천적으로 없다.
남은 변이 닫힌 고리로 안 이어지면 **던진다** — 깨진 도형을 조용히 내보내면 카드에 구멍이 뚫린다.

⚠️ **바깥 고리를 회전 방향으로 판정하지 않는다.** 원본의 바깥 고리는 시계방향이라 부호로 가르면
전부 구멍이 된다(2026-08-03 에 실제로 여기서 한 번 막혔다). **포함 깊이**로 가른다.

### 네 갈래로 넓힌다 — 다만 **검증된 표만 켠다** (2026-08-03 오너 선택)
오너가 인구·세대 / 인구이동 / 연령구조·고령화 / 출생·사망 **네 가지를 모두** 골랐다.
표 ID 는 찾았지만 kosis.kr 본문이 세션에서 안 열려(robots) **항목코드·분류축은 대부분 미확인**이다.

| 지표 | tblId | 주기 | 확신도 | 상태 |
|---|---|---|---|---|
| 인구 | `DT_1B040A3` | 월 | 확실(T20=총인구) | **수집중** |
| 세대수 | `DT_1B040B3` | 월 | 표명확실 | 검증 대기 |
| 인구이동 | `DT_1B26001_A01` | 연 | 표명확실(시군구 5자리 호출 확인) | 검증 대기 |
| 연령(1세별) | `DT_1B04006` | 미확인 | 표명확실 | 검증 대기 |
| 출생 | `DT_1B81A03` | 연 | 표명확실 | 검증 대기 |
| 사망 | `DT_1B34E13` | 연 | 표명확실 | 검증 대기 |

**`enabled: false` 인 표는 정기 수집에 끼지 않는다.** 셀프테스트가 이 규칙을 강제한다
(확신도가 "확실"이 아닌데 켜져 있으면 테스트가 깨진다).

#### 검증은 첫 호출이 대신한다 — `kosis-probe`
`pnpm --filter @wirit/collectors probe-kosis` (또는 `data/kosis-probe-queue.txt` 푸시)가
표마다 **최근 1개 시점만** 받아 `data/kosis-probe.md` 에 적어 커밋한다:
통계표명(TBL_NM) · 항목코드(ITM_ID/ITM_NM) · 분류축(C1~C4) · 시군구 5자리 코드가 실제로 오는가.

⚠️ **왜 이 단계를 건너뛰면 안 되나.** 사망 표(`DT_1B34E13`)에는 '사망원인 50항목' 축이 있다.
'계'를 안 고르면 **특정 사인의 숫자가 총사망자수로 카드에 나간다** — 응답은 정상이고 숫자도
그럴싸해서 눈으로는 안 잡힌다. 인구이동 표도 총전입/총전출/순이동이 섞여 있어 같은 위험이 있다.

#### 파생 지표는 받아 적지 않고 우리가 계산한다
세대당 인구(인구÷세대수) · 순이동(전입−전출) · 고령화율(65세이상÷인구) · 자연증감(출생−사망).
전용 항목이 표에 있더라도 **원자료에서 코드가 다시 계산한다** — 오보 0 원칙이고,
지표표와 원자료가 어긋날 때 어느 쪽이 맞는지 우리가 안다.

#### ⚠️ 아직 표를 못 찾은 것 — 1인가구 비중
오너가 고른 네 갈래 중 **가구·1인가구**는 절반만 채워져 있다.
`DT_1B040B3`(주민등록세대수)로 **세대수·세대당 인구**는 되지만, **1인가구 비중**은
`세대원수별` 축이 있는 다른 표가 필요하다. 공공데이터포털에 파일데이터
「지역별(행정동) 세대원수별 주민등록 세대수」가 있는 것은 확인했으나 **KOSIS tblId 는 못 찾았다.**

**추측으로 tblId 를 적어 넣지 않았다** — 지어낸 표 ID 가 이 배관에서 가장 위험하다.
`kosis-probe` 를 돌릴 때 `DT_1B040B3` 의 분류축에 세대원수가 들어 있는지 먼저 보고,
없으면 그때 표를 다시 찾는다. 세대당 인구(인구÷세대수)만으로도 "1인가구 시대" 서사의
7할은 나오므로 급하지 않다.

---

## KOSIS 표 검증 결과 (2026-08-03, 실측)

키를 받아 6개 표를 실제로 찔러 본 결과다. 전문은 `data/kosis-probe.md`.

| 표 | tblId | 상태 | 확인된 것 |
|---|---|---|---|
| 인구 | `DT_1B040A3` | ✅ 수집중 | `T20=총인구수` · 시군구 277개 · 월간(202606) |
| 세대수 | `DT_1B040B3` | ✅ 수집중 | `T1=세대수` · 시군구 277개 · 월간 |
| 이동 | `DT_1B26001_A01` | ✅ 수집중 | `T25=순이동` · 시군구 254개 · 연간(2025) |
| 출생 | `DT_1B81A03` | ⏸ 보류 | 축 `C2` 확인(`00=총계`) — **코드 체계가 다르다(아래)** |
| 연령 | `DT_1B04006` | ⏸ 막힘 | 4만 셀 한도 초과 |
| 사망 | `DT_1B34E13` | ⏸ 막힘 | 4만 셀 한도 초과 |

### ⚠️ 출생 표는 행정구역 코드 체계가 다르다 — 켜지 않았다
출생 표가 주는 코드는 `11010=종로구 · 11020=중구` 인데,
우리 지도와 인구·세대수 표는 `11110=종로구 · 11140=중구` 다.
**그대로 조인하면 엉뚱한 구에 색이 칠해진다.** 이 배관에서 가장 위험한 사고 유형이라
대조표를 만들기 전까지 `enabled` 를 켜지 않는다.

### 연령·사망 — 4만 셀 한도
KOSIS 는 한 요청당 4만 셀까지만 준다. 연령(1세별 101개)·사망원인(50항목)은
전국 277개 지역 × 축 전체가 한 번에 이를 넘는다. 정기 수집을 하려면
**지역을 나눠 여러 번 불러야** 하고, 그 설계를 하려면 축 코드를 먼저 알아야 한다.
probe 가 종로구 하나로 좁혀 축만 엿보도록 해 뒀다.

### 항목은 표당 하나만 받는다
`parse/kosis.ts` 가 `ITM_ID` 를 구분하지 않는다. 항목을 여러 개 받으면
한 지역·한 시점에 값이 겹쳐 시계열이 **조용히** 망가진다.
그래서 이동 표는 항목 8개 중 `T25=순이동` 하나만 받는다.
전입·전출 분해가 필요하면 파서에 항목 축을 먼저 넣는다.
(selftest 가 이 규칙을 가드로 지킨다 — 켜진 표의 itmId 에 `ALL`·`+` 금지)
