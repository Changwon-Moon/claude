# ⛔ 폐기 — 이 문서를 따라가지 마세요

**2026-08-16 오전에 쓴 인수인계입니다. 같은 날 오후 감사에서 아래 내용이 틀린 것으로 확인됐습니다.**

👉 **최신본: [`claude/위릿-인수인계-2026-08-16b.md`](claude/위릿-인수인계-2026-08-16b.md)**
👉 **판형 정본: 저장소의 `docs/guides/신고가-카드-기준.md`**

## 이 문서의 무엇이 틀렸나 (같은 실수를 다시 밟지 않게 남깁니다)

| 여기 적혀 있던 것 | 실제 |
|---|---|
| 렌더 명령이 `--outdir` · `--qa` 를 쓴다 | 렌더러 CLI 인자는 **`--data` / `--out` 둘뿐**이다. 나머지는 조용히 무시돼 PNG 가 엉뚱한 곳에 떨어진다. **렌더는 `produce-card.mjs` 로만 한다** |
| "① 대기열 3개에 줄 쓰고 푸시" 로 시작 | 새 단지의 `lawd`·`umd` 를 알 방법이 없다. **빌더를 먼저 부딪치면** 써야 할 대기열 줄을 찍고 멈춘다 |
| 배관 점검 "18항목" | 항목 수는 계속 늘어난다. **문서에 적지 않는다** — `node scripts/verify-singo-pipe.mjs` 가 끝에 세어서 찍는다 |
| 광명한진타운 확정 md5 를 본문에 적음 | 그 뒤 재확정으로 값이 바뀌었다. **md5 는 문서에 베껴 적지 않는다** — `data/review/sets.json` 의 `confirmedMd5` 가 정본이다 |
| 세트·빌더 등록 절차 없음 | `data/review/sets.json`·`builders.json` 에 항목이 없으면 `produce-card` 가 **즉시 죽는다**. 절차는 기준 문서 §6 ④ 에 있다 |

이 표 자체가 교훈입니다 — **인수인계 문서에 절차를 베껴 쓰면 저장소보다 먼저 낡습니다.**
그래서 최신본은 "지금 어디까지 왔나"만 적고, 절차는 저장소 정본을 가리킵니다.
