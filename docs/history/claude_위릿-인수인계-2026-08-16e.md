# 위릿 인수인계 — 2026-08-16e (마감 스킬 · 정본 통합 · 공급평수 셈법)

> 최종 커밋 `860c1e7`. 저장소의 `STATUS.md` 가 "지금 상태"의 정본이다.
> **마감 절차의 정본은 이제 `docs/CARD_CHECKLIST.md` §7 하나다.**

## 한 줄

마감 절차를 스킬(`wirit-close`)과 스크립트(`close-session.mjs`)로 묶었고, 그걸 **실제로
돌려 보다가** 저장소의 실제 결함 넷을 찾았다. 그리고 공급평수 셈법을 실측으로 확정했다.

## 1. `wirit-close` 스킬 + `scripts/close-session.mjs`

판단이 필요한 것(무엇을 배웠나·어느 문서인가)은 스킬이, 판단이 없는 것(검사·커밋·푸시)은
스크립트가 맡는다. 스크립트는 **빈칸을 가리킬 뿐 채우지 않는다.**

## 2. 돌려 보고 찾은 결함 넷

| 결함 | 지금은 |
|---|---|
| **`doctor` 가 캡션 서명을 날린다** — 진단이 카드를 다시 만들기 때문. `confirm.mjs` 도 같은 순서라 **확정할 때마다** `tohuh-rent-map`·`wolse-flip` 이 서명을 잃고 있었다 | 서명을 **맨 뒤**로 옮기고 `--check` 로 증명. 일부러 지워 되돌린 뒤 새 순서가 잡는 것까지 확인했다 |
| **로컬 검사 순서가 `tower-deploy.yml` 과 달랐다** — render·`build-archive`·`stage-public-cards` 누락 → 스모크 172/174 | 배포와 같은 순서로 맞췄다. `stage-public-cards` 는 조립이 `_site` 를 지우므로 **반드시 그 뒤** |
| **`CARD_CHECKLIST.md` 에 `## §6` 이 두 개** | 지하철 = §6, 세션 닫기 = §7(파일 맨 뒤) |
| **마감 절차가 네 군데에 다르게** 적혀 있어 오류가 한 곳만 고쳐졌다 | **정본 = §7.** `HANDOFF §10`·`WIRIT_CARDS_SKILL`·스킬은 "여기를 보라"만 |

## 3. ✅ 공급평수 셈법 — 오너 지적이 맞았다

건축물대장 전유공용면적으로 늘푸른벽산 84.92㎡ 호를 정확히 집었고, 규칙이 보였다:

> **공급면적 = 전유 + 「주건축물」 공용**

부속건축물 공용(지하주차장 37.07 · 관리/노인정 · 경비실 · 기계전기실)은 기타공용이라 빠진다.
`mainAtchGbCdNm` **한 칸으로 갈려서 키워드 추측이 필요 없다** — 실제로 탐침의 키워드 후보는
「승강**기계**단」이 `기계` 에 걸리는 오탐을 냈다. **필터를 미리 짜 넣지 않고 줄을 그대로 본 것이 맞았다.**

| 단지 | 전용 | 공급 | 실측 평 | 카드 표기 |
|---|---:|---:|---:|---|
| 늘푸른벽산 | 84.92 | 109.94 | 33.26 | ~~34평~~ → **33평** |
| 래미안크리시엘 | 84.95 | 108.02 | 32.68 | ~~34평~~ → **33평** |
| 광명한진타운 | 84.95 | 110.44 | 33.41 | ~~34평~~ → **33평** |
| 화서역푸르지오 | 59.98 | 85.77 | 25.95 | ~~25평~~ → **26평** |

**아직 안 정한 것 둘 (오너 확인 필요):**
① 지하대피소를 주거공용에 넣을지 — 지금 넣었다. 빼면 31평이 되는데 84타입 공급평으로는 낮다
② **반올림이냐 버림이냐** — 33.26 은 어느 쪽이든 33평이지만 **25.95 는 갈린다**

서초포레스타2·다산롯데캐슬은 표본을 못 잡았다(쪽 상한 8,000줄 또는 지번 불일치) — 다시 받는다.

## 4. 🔴 관제탑 배포가 막혀 있다

`tower-deploy.yml` 이 **스모크 단계에서 실패**한다(173/174). 걸린 검사는
**「같은 카드가 두 번 뜨지 않음」** — 신고가 6장이 **결재 목록과 업로드 목록 양쪽에** 있다.
Actions 는 살아 있고 나머지는 전부 초록이다. **이것만 풀면 배포된다.** 원인 미규명 —
`sets.json` 의 `state` 분류부터 본다.

## 5. 다음 세션이 할 일

1. **관제탑 중복** 풀기 → 배포 복구
2. **공급평수** — 반올림·지하대피소를 오너에게 확인 → `PYEONG_LABEL` 을 실측 수집기로 대체.
   ⚠️ `PYEONG_LABEL` 은 **노선 시세·신고가 지도·월간 정리도 같이 쓴다.** 확정본 픽셀이
   걸린 것이 있는지 먼저 훑는다
3. 신고가 5장은 **평까지 정리된 뒤 한 번에 재확정**(지금 확정하면 또 다시 그린다)
4. **서초포레스타2단지** — 저점 기준선이 라벨 글자를 관통한다(눈으로 확인, 검수는 PASS).
   장애물 목록에 기준선 자신이 빠져 있다
5. 정리가 끝나면 **`apt-area-probe.yml` 의 `*/30` cron 을 지운다** — 1회성 조사다

## 6. 이 세션이 배운 것

**스킬을 만들고 안 돌려보는 것은 검수 PASS 를 믿는 것과 같다.** 넷 다 실제로 돌려서 나왔다.
그리고 **같은 절차가 두 곳에 적혀 있으면 언젠가 갈라진다** — 실제로 갈라져 있었다.
