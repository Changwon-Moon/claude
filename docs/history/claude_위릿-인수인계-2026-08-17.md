# 위릿 인수인계 — 2026-08-17 (신고가 6장 확정 · 관제탑 배포 · 공급평수 실측 전환)

> 최종 커밋 `771381b`. 저장소의 `STATUS.md` 가 "지금 상태"의 정본이다.
> 마감 절차의 정본은 **`docs/CARD_CHECKLIST.md` §7** 하나다(이번에 통합했다).

## 한 줄

주차대수를 못 받던 것에서 시작해 저장소 공개 전환 → **카드의 평이 고정 환산표였다는 사실**
→ 건축물대장 실측 전환 → 신고가 6장 확정 → 관제탑 실배포까지 갔다. 도중에 저장소 결함
다섯 개가 드러났고 전부 고쳤다.

## 1. ✅ 신고가 6장 오너 확정 · 관제탑 결재 대기

| 세트 | 카드 | 공급 | 전용률 |
|---|---|---:|---:|
| `singo-gwangmyeong-hanjin` | 광명한진타운 **33평** 10억 | 110.44㎡ | 76.9% |
| `singo-neulpureun-byuksan` | 늘푸른벽산 **33평** 6.67억 | 109.94㎡ | 77.2% |
| `singo-raemian-crisiel` | 래미안크리시엘 **33평** 12.7억 | 108.02㎡ | 78.6% |
| `singo-hwaseo-prugio` | 화서역푸르지오더에듀포레 **26평** 7.75억 | 85.77㎡ | 69.9% |
| `singo-seocho-foresta2` | 서초포레스타2단지 **38평** 19.9억 | 124.35㎡ | **67.9%** |
| `singo-dasan-lottecastle` | 다산롯데캐슬 **35평** 11.95억 | 114.08㎡ | 74.5% |

확정 md5 는 `sets.json` 의 `confirmedMd5` 가 정본이다(여기 베껴 적지 않는다).

**관제탑 실배포 확인** — `docs/_health/verify-live-last.md` 에 「실제 사이트 확인」 전항 초록.
조립이 아니라 Cloudflare 에 올라간 것을 확인했다. 6장 전부 **결재 대기(stage 4)** 에 캡션과 함께.

## 2. 🏗️ 공급평수 — 고정 환산표를 버리고 실측으로

오너: *"전용 59, 84로만 단지서칭은 하되, 카드를 만들때에는 실제 공급평수를 적어줘야지."*

```
공급면적 = 전유 + 「주건축물」 공용
평       = 공급면적 ÷ 3.305785, 반올림
```

**오너가 네이버부동산 화면으로 대조해 확정했다** — `109.94㎡ (84.92)` · `85㎡ (59)` 와
소수점까지 일치. 옛 고정표(84→34평·59→25평)는 실제로 틀렸다.

- ⚠️ **용도 이름으로 거르지 않는다.** 「승강**기계**단」이 `기계` 에 걸리는 오탐이 났다.
  `mainAtchGbCd`(0=주건축물·1=부속건축물)는 대장이 나눠 둔 칸이라 흔들리지 않는다.
- ⚠️ **지번은 한 곳에서 오지 않는다.** 대장 주소와 실거래 지번이 다른 단지가 있다
  (서초 143 vs **384** / 다산 5869-2 vs **6029**). 둘 다 물어보고 줄이 오는 쪽을 쓴다.
- 새 배관: `sources/supplyArea.ts`(셈법 정본) · `supplyAreaCli.ts` ·
  `data/apt-supply-queue.txt` · `apt-supply-area.yml`(cron `37 */2`).
  **자료 없으면 빌더가 멈춘다** — 고정표로 안 돌아간다.

## 3. 🔴 이번에 드러난 저장소 결함 다섯

| 결함 | 지금은 |
|---|---|
| **`doctor` 가 캡션 서명을 날린다** — 진단이 카드를 다시 만든다. `confirm.mjs` 도 같은 순서라 **확정할 때마다** 캡션 2건이 서명을 잃었다 | 서명을 **맨 뒤**로. `--check` 로 증명. 배관 점검 ⑤ 가 **자리**를 잰다 |
| **증시 수집기가 지난 거래일을 지웠다** — 야후 창이 어떤 날은 지난 날을 빼고 주는데 덮어썼다. 확정 카드의 근거가 사라졌다 | 지워진 4일 복원, 수집기가 **합친다**. 빠질 뻔한 날을 로그로 말한다 |
| **관제탑에 카드가 두 번 떠 배포가 막혔다** — 자정을 넘겨 두 날짜 폴더에 생겼다 | `collectProduced` 가 `setLabel` 별 최신 날짜만 남긴다 |
| **마감 절차가 네 군데에 다르게** 적혀 있어 오류가 한 곳만 고쳐졌다 | 정본 = `CARD_CHECKLIST §7`. 나머지는 "여기를 보라"만 |
| **`CARD_CHECKLIST` 에 `## §6` 이 두 개** | 지하철 = §6, 세션 닫기 = §7 |

새 검사 둘(⑫ 세트 제목의 평 == 카드 / ⑤ 서명 자리)은 **일부러 어긋내 잡히는지 확인**했다.

## 4. 🚪 `wirit-close` 스킬 + `scripts/close-session.mjs`

마감 절차를 스킬(판단)과 스크립트(기계)로 나눴다. 스크립트는 **빈칸을 가리킬 뿐 채우지 않는다.**
검사 순서는 `tower-deploy.yml` 과 같다 — 여기서 통과해도 배포에서 깨지면 소용없다.

⚠️ 마감 점검 ②(배움이 원천에 적혔나)는 **오늘 날짜**로만 잰다. 세션이 자정을 넘기면
어제 적은 것이 안 보여 전부 ⬜ 가 된다. **거짓 경고다** — 막지 않는 warn 이니 날짜를 보고 판단한다.

## 5. 다음 세션이 할 일

1. **서초포레스타2 전용률 67.9% 확인** — 84타입 38평은 흔한 값이 아니다.
   주거공용 39.87㎡ 의 용도별 내역(`parts`)을 못 본 채 확정했다(수집이 parts 를 남기기 전
   판본 + 재수집 두 번 다 국토부 문 닫힘). `data/apt-supply-last.md` 를 보고,
   **기타공용이 주건축물로 잡힌 것이면 값을 고치고 재확정한다.** 대기열에 `force=1` 이 걸려 있다.
2. 오너가 관제탑에서 **발행 승인** → 인스타 업로드(자동 발행은 2026-07-27 폐지, 사람이 한다)
3. ⚠️ `PYEONG_LABEL` 은 **노선 시세·신고가 지도·월간 정리도 같이 쓴다.** 아직 고정표 그대로다 —
   그쪽도 실측으로 옮길지 정해야 한다. 확정본 픽셀이 걸린 것이 있는지 먼저 훑는다
4. 정리가 끝나면 **`apt-area-probe.yml` 의 `*/30` cron 을 지운다** — 1회성 조사였다
5. 서초포레스타2 저점 라벨이 기준선에 걸치는 것(검수는 PASS) — 장애물 목록에 기준선 자신을 넣는다

## 6. 알아 둘 함정

- **공공데이터포털 일일 한도**는 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 로 답한다.
  키 문제처럼 보이지만 아니다. `numOfRows` 를 100→1000 으로 올려 호출을 10배 줄였다.
- **`process.cwd()` 를 쓰지 않는다.** `pnpm --filter` 가 작업 폴더를 패키지로 바꾼다.
- **`PASS` 는 "볼 준비가 됐다"** — 검수 초록인 카드에서 기준선이 라벨을 관통하는 걸 눈으로 찾았다.
