# 「오늘의 신고가」 판형 최종 검수 결과 — 2026-08-16

디자인팀 에이전트를 돌려 `singo-record@1` 을 검수하고, 나온 것을 전부 반영했다.
최종 커밋 `c82b31e`. 카드 `singo-gwangmyeong-hanjin` 은 **오너 확정 대기**.

---

## 1. 차단이었던 것 (게이트가 못 잡고 있었다)

### ⚠️ `svgspill` 7px — 그런데 게이트는 초록이었다
곡선 판이 담는 칸(`.sr-chart` 760px)을 SVG(775px)가 7px 넘고 있었다.
**진짜 문제는 그걸 놓친 게 아니라, 검수가 "통과"라고 말한 것이다.**

`rebuild-cards.mjs` 는 빌더를 다시 돌린 뒤 `data/content/<날짜>/` 를 검수했는데,
`--publish` 없는 빌더는 결과를 `data/out/_spike` 에 떨군다.
→ **옛 발행본을 검수하고 통과라고 말하고 있었다.**

**고침**: 둘 다 있으면 **새로 쓰인 쪽**(mtime 비교)을 검수한다.
이 구멍은 이 판형만의 문제가 아니라 `_spike` 로 만드는 **모든 카드**에 있었다.

> 다음 세션 교훈: `data/content/<날짜>/` 의 PNG 를 보고 "안 바뀌었다"고 판단하지 말 것.
> 지금 만든 것은 `data/out/_spike` 에 있다.

---

## 2. 그림에서 고친 것

| 무엇 | 왜 |
|---|---|
| 관측 점(`.sr-obs`) 제거 | 65개월치 점이 14px 간격으로 5px 곡선 **위에** 찍혀 선에 회색 털이 붙었다. 오늘 점 자리에도 겹쳐 **속을 비운 링 한가운데에 회색 점이 박혔다**(오너 지시와 정반대). 정직함(선은 관측 사이를 이은 것)은 **끊긴 path 규칙 + `meta.curve`** 가 그대로 맡는다 |
| 저점 측정선을 `X1` → **오늘 점 x** | 11px 어긋나 파랑 눈금이 링 옆구리로 삐져나왔다. "오늘 값에서 저점까지"가 그림과 안 맞았다 |
| 판 모서리 10 → **4** | CARD_CHECKLIST "AI티 금지: 각을 세우고 ≤4px". 크게 둥근 옅은 회색 판이 정확히 그 표본이다 |
| "달성!" 서체를 가격과 같은 `--font-num` 으로 | 한 덩어리라고 적어 놓고 서체를 갈랐다 |
| 판 높이 `VB_H` 775 → 756 → **768** | 제원 3줄로 히어로가 커져 넘쳤고, 제원을 작게 하자 다시 자리가 났다 |

---

## 3. 문구·자료에서 고친 것 (오보 위험)

- **"고점대비" → "지난 고점대비"**
  그냥 두면 **역대 고점 대비**로 읽힌다. 이 카드의 고점은 지난 사이클 고점(9억·2021.10)이고,
  직전 최고가는 9.5억(2026-07-14)로 따로 있다.

- **`meta.hhld.note` 에 박혀 있던 "오너가 2026-08-13 확인했다" 제거**
  그 카드에서만 참인 말이 **모든 카드에 붙어** 있었다. 다음 검수가 있지도 않은 확인을
  근거로 삼게 된다. 이제 그 실행의 지번·주소 대조 근거를 적는다.

- **`docs/guides/신고가-알림-기준.md` 의 "2006-01 ~ 현재" → "2020-01 ~ 현재"**
  실제 기준선은 `molit-peak/_progress.json` 의 `from: 202001` 이다.
  **문서를 믿고 누가 "역대"라고 쓰는 순간 그게 오보다.**

---

## 4. 오너가 결정한 것 (검수 의견과 갈렸던 3건)

| 쟁점 | 검수 의견 | **오너 결정** |
|---|---|---|
| 킥커 날짜가 발행일(08.16)인데 실제 계약일은 07.19 | 최대 오해 위험 | **킥커를 계약일로.** `오늘의 10억 클럽 (계약 2026.07.19)` — '계약' 두 글자로 무슨 날짜인지 자체가 말하게 했다 |
| 그래프 안 "10억" 라벨 = 카드에서 세 번째 "10억" | 군더더기, 제거 권고 | **유지.** 점선이 무슨 선인지 말하는 유일한 표시다 |
| 제원 3줄이 가격보다 무거운 덩어리 | 무게를 낮춰라 | **회색·한 단계 작게.** 잉크 800/40px → 회색 700/34px. 정보는 그대로, 무게만 |

---

## 5. 남은 판단거리 (검수가 지적했으나 오너 지시라 손대지 않음)

- **코발트가 한 카드에서 두 뜻**: 제목의 `34평`(평형) / 그래프 파랑(저점 대비).
  BRAND.md §3 은 "한 카드에 강조색은 코발트 1개 축"이다. 지금은 두 축.
  되돌리려면 제목 평형을 회색으로 내리는 쪽이 손해가 적다(그래프의 고점=빨강·저점=파랑 쌍은 못 깬다).
- **제목이 폭을 78%만 채운다**: `__wiritFit` 상한 92px 에 걸려 오른쪽에 203px 공백.
  키우려면 세로가 +28px 필요한데 판이 그만큼 없다. 지금은 **짧은 이름에서 fit 이 아무 일도 안 해**
  단지마다 제목 크기가 달라진다 — 정기물에는 좋지 않다.
- **판 안 워터마크 불투명도 `.9`** 가 BRAND.md 개정문에 안 적혀 있다.
  판 바탕이 잉크 3.5% 라 실제 대비는 슬롯 C 기본값(.16)보다 **오히려 옅다.**
  적어 두지 않으면 다음 세션이 ".26 으로 고칠" 위험이 있다.

---

## 6. 최종 게이트

- 디자인 검수 **error 0**
- 셀프테스트 **313 통과 / 0 실패**
- doctor **필수 31항 통과**
- `audit-head` 위반 0
