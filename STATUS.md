# STATUS — 현재 상태 보드

> 모든 세션은 시작할 때 이 파일을 읽고, 끝낼 때 갱신한다(CLAUDE.md 참조).
> **여기엔 "지금 상태 + 다음 할 일"만 둔다.** 지나간 세션 서사는 `docs/archive/STATUS-history.md`,
> 소재 결정 이력은 `research/DECISION_LOG.md`로 간다. 다시 로그가 쌓이지 않게 한다.

**최종 갱신**: 2026-09-07

> ## ⭐ 2026-09-07 — **폰트 기준이 바뀌었다.** 새 판형은 「제목만 태백체 + 나머지 전부 Pretendard」
>
> ### 새 카드를 만들면 이것부터 한다
> 카드 루트에 **`font-taebaek-pretendard`** 를 붙인다 — `<div class="wirit-card font-taebaek-pretendard ...">`.
> 제목만 태백체이고 **숫자를 포함한 나머지는 전부 Pretendard** 다. **Wanted Sans 는 퇴역했다**
> (로고 뱃지 `.wirit-corner .mark` 에만 남는다 — 그건 폰트가 아니라 CI 다).
> 글꼴로 강조하던 몫은 **색과 크기**가 가져간다.
>
> ### 🔒 옛 카드는 건드리지 않는다
> `:root` 기본값(하이브리드)은 **그대로 뒀다.** `--font-num` 을 갈면 확정본 **51개 세트**와
> 그 변수를 쓰는 **판형 33개**가 다시 그려질 때 픽셀이 전부 바뀐다 — 「발행본·확정본 픽셀 불변」이
> 금지하는 바로 그 일이다. 그래서 기본값이 아니라 **세트를 하나 더** 만들었다.
> **일괄 소급 전환은 하지 않는다.** 새 판형이 쌓이며 자연히 갈아탄다.
>
> ⚠️ **태백체를 숫자에 쓰지 않는다** — tabular(고정폭)가 없어 표·순위에 쓰면 열이 흔들린다.
>
> 정본: [`docs/BRAND.md`](docs/BRAND.md) §4 · 체크리스트: [`docs/CARD_CHECKLIST.md`](docs/CARD_CHECKLIST.md) §2 「폰트」 ·
> 판단: [`company/CEO.md`](company/CEO.md) C 섹션 09-07 · 구현: `templates/_shared/base.css`
>
> ### 진행 중 — 9·7 대책 1주년 카드 (새 폰트 첫 적용 사례)
> 판형 시안 확정(가로 진행바 + 얇은 기둥 6칸 + m2-gov 규격 콜아웃 카드).
> **아직 빌더도 데이터셋도 없다.** 수치(수도권 1~7월 착공 7.8만호)가 언론 보도 경유라
> 국토부 「’26년 7월 주택통계」 PDF·8·13 대책 보도자료로 `verified: true` 를 세워야 제작에 들어간다.

> ## 🟡 2026-09-05 — 신고가 **12장 시안** · 오너 확인 대기 · **아침 개편은 첫날 떨어졌다(고침 완료)**
>
> 인수인계: `claude/위릿-인수인계-2026-09-05-예약.md`
>
> ### 지금 상태
> - `singo-daily-2026-09-05` — **12장 · 검수 대기 · 오너 확인 대기**(확정 아님) · 못 만든 장 0건
> - 관제탑 썸네일 **선행 채움 완료**(`smoke-tower` 353/353) — 승인 나면 확정이 안 막힌다
> - ~~08:40 예약~~(2026-09-05 당시)이 원커맨드를 다시 돌려 **추적 파일 변경 0건** = 07:31 판을
>   바이트 단위로 재현. ⚠️ **그 예약은 지금 없다** — 지금 도는 시각의 정본은
>   `docs/DATA_REFRESH.md` 와 관제탑 「배관」 탭이다
>
> ### 🔴 09-06 아침에 딱 하나만 본다
> `singo-prep` 이 **봇 커밋으로 수집기를 못 깨우고 있었다** — 깃허브는 기본 `GITHUB_TOKEN`
> push 로 새 워크플로 실행을 만들지 않는다. 09-05 에 06:54 대기열이 **40분 정지**했고,
> 07:30 에 사람이 밀어서 풀렸다. 09-04 에 「빨라졌다」로 보인 것도 실제로는 사람의 푸시였다.
> 고침: `singo-prep` 이 수집기 넷을 **`workflow_call` 로 직접 부른다**(+ `audit-all` 문지기 둘).
> **09-06 07:0x 에 사람 없이 수집기가 도는가 — 그것만 확인하면 된다.**
>
> ### 남은 손질
> - 곡선 사다리가 「파일 있음」만 보고 통과시킨다 — **오늘 거래보다 오래됐는가**를 재야 한다
>   (09-05 07:45 에 3건을 손으로 다시 받았다)

> ## ✅ 2026-09-04 — 신고가 **14장 확정** · 곡선 발광 · **아침을 앞당기는 배관 개편**
>
> 인수인계: `claude/위릿-인수인계-2026-09-04.md`
>
> ### 지금 상태
> - `singo-daily-2026-09-04` — 캐러셀 **14장 오너 확정** · ZIP·카톡 문구 전달 · 업로드만 남음
> - 못 만든 1건(신림푸르지오 84)은 **자료 문제가 아니다** — 신고가 11.7억 vs 지난 고점
>   11.6억(2021.09)이 0.9% 차이라 두 기준선이 겹쳐 판형이 거절한다(기준 3%). 다시 받아도 같다.
>
> ### ⏱️ 아침 순서를 바꿨다 — **재료는 사람을 기다리지 않는다**
> 09-04 아침: 07:10 판정 → **07:40 세션** → 07:43 그제야 대기열 푸시 → 08:45 공급면적 →
> 10:55 곡선 → 12:00 카드. **늦은 이유는 예약 시각이 아니라 순서였다.**
> 1. **`singo-prep.yml`** — 판정이 성공하면 `--enqueue-only` 로 재료를 자동으로 걸고 커밋한다.
>    수집기 넷이 **07:12** 에 움직인다. 카드는 만들지 않는다(제작·확정은 그대로 세션과 오너).
> 2. **`singo-history-retry.yml`** — 곡선에도 +2·10·30분 사다리(공급면적엔 있고 곡선엔 없었다).
> 3. 코워크 예약을 **08:40 KST**(cron `40 23 * * *`)로 옮겼다 — 재료가 찬 뒤에 오는 자리.
>
> ### 🔒 되돌리지 말 것
> 1. 🔴 **「과거 달의 값은 변하지 않는다」는 틀렸다.** 실거래 신고는 최대 30일 늦어 지난 달
>    값이 계속 자란다. `monthCacheIo` 가 **최근 3달은 캐시를 안 믿는다**(`RECENT_MONTHS`).
>    이걸 되돌리면 09-04 처럼 카드 7장이 「곡선 최고가 < 신고가」로 막힌다.
>    ⚠️ **대기열의 `force=1` 은 월 캐시를 무르지 않는다** — 그래서 그때 재수집이 안 들었다.
> 2. ⭐ **기다림이 아니라 새 러너다 — 실측 확증.** 곡선이 한 실행에서 3시간에 6곳,
>    새 실행에서 **10분에 10곳**. 공급면적도 19건 전부 실패 후 새 러너에서 채워졌다.
> 3. 🔖 **이미 발행한 이름표는 다시 쓰지 않는다.** 같은 단지가 또 신고가를 쓰면 날짜를 붙여
>    **새 카드**로 만든다(`--slug`). 안 그러면 지난 카드를 덮는다 — 09-04 에 08-26 발행분
>    캡션이 9.4억으로 덮여 인스타와 저장소가 갈렸다. 그 빌더는 `frozen` 으로 굳혔다.
> 4. **이름 규칙은 소급하지 않는다 — 단, 오늘 확정한 세트는 「지나간 것」이 아니다.**
>    사전·「아파트」 손질은 **이 판을 그리는 날보다 먼저 확정된** 세트에만 안 건다.
>    이 단서가 없으면 확정하는 순간 그 세트가 자기 카드의 사전 이름을 스스로 벗는다
>    (09-04 에 「대방대림」이 「대림아파트」로 되돌아갔다 → 확정 md5 와 어긋난다).
> 5. **「아파트」는 카드 제목에서 뺀다**(기준 §13). 사전과 `--name` 은 사람이 정한 이름이라 안 건다.
> 6. **대기열에 이미 있는 줄은 다시 쓰지 않는다.** 사람 없는 예약에서 대기열이 끝없이 부푼다.
> 7. **곡선은 단지마다 커밋한다** — 워크플로 주석의 「잘리면 통째로 0건」은 **틀렸다**.

## 지금 상태 한눈에

- **운영 방식**: 세션이 저장소를 열어 카드를 만들고, **발행은 오너가 직접**(자동 발행 폐지). 정기 데이터 수집·재생산은 GitHub Actions.
- **최근 판형 작업**: `singo-record@1`(오늘의 신고가) **오너 확정 2026-08-16** — 기준은 `docs/guides/신고가-카드-기준.md`.
- **대기 중 판형**: `news-figure@1`(인물·뉴스 카드) 시안 완료 — **오너 선택 대기**. 오너가 고르면 `builders.json`·`sets.json` 등록 + 첫 확정 시 `sample.json` 고정.
- **인구 소재 배관**: 지역 인구 통계 자동 추출 경로와 시군구 2026년판 지도 신설(2026-08-03). KOSIS 기반, 통계청 행정구역코드로 조인.
- **최근 소재**: 등록외국인 국적 지도, 토허제×외국인 매수 등 소재 등록·승격 진행(상세는 `research/DECISION_LOG.md`).
- **신고가 판정 단위(2026-08-19 오너 재확인 · 유지)**: 전용 **59타입(56~62㎡)·84타입(82~85.5㎡)을 각각 한 칸으로 묶는다** — 서브면적(59.65/59.75/59.94)으로 가르지 않는다. 이것이 다른 서비스(실까 등)와 답이 갈리는 지점이라 "왜 우리엔 없나"가 반복해서 들어온다 → **`node scripts/singo-why.mjs <단지명> [--type 59] [--price <만원>]`** 으로 명부·문턱·서브면적별 최고가·알림 이력을 그 자리에서 본다(네트워크 불필요).

## 확정·발행 카드

> 🔒 **정본은 `data/review/sets.json`(confirmed) + `pixel-baselines.json`(md5)이다.**
> 여기 표를 손으로 두지 않는다 — 2026-08-31 에 뺐다. 코드가 이미 정답을 갖고 있는데
> 문서에 사본을 두면 **반드시 갈라진다**(어제 목록엔 이미 없는 카드가 남아 있었다).
> 지금 무엇이 확정됐는지는 관제탑 「보관함」 탭 또는 `node -e` 로 `sets.json` 을 본다.

정기물(실거래·증시·주간지수)은 "같은 데이터면 같은 픽셀"이 약속이라 `pixel-baselines.json` 에
넣지 않는다 — `confirm.mjs` 가 자동으로 가른다(고정물만 픽셀 고정).

## 다음 할 일 / 대기

- `news-figure@1` 판형을 오너가 고르면 등록 + 첫 실제 카드(인물 실물 사진 교체) 확정
- 「오늘의 신고가」 사진 도입 검토(2장 캐러셀: 사진 커버 + 곡선) — **저작권 확인이 먼저**
- 인구 카드 시리즈 전개(자동 추출 배관 활용)
- ✅ **낡은 수집 3개 재개 완료(2026-08-31)** — 인구·증시·ECOS 전부 성공.
  인구는 KOSIS 키 만료로 27일간 조용히 실패 중이었고, 오너가 키를 교체해 복구했다
  (`population/2026-07.json` 수신). 교훈은 `docs/API_CONNECTIONS.md` 맨 위
- ⭐ **「대장 도감」 되살렸다(2026-08-31)** — 1화(강남구) 재생산 확인, `builders.json` 등록 완료.
  **다음: 2화(서초구) 데이터셋을 만들면 시리즈가 이어진다.** 매주 한 구씩 25주 치 콘텐츠
- **분양권 프리미엄 — 남은 것은 `ownershipGbn` 하나다**(2026-09-04 정정).
  「청약홈 15101047 활용신청」은 **오너 숙제가 아니다** — `docs/API_CONNECTIONS.md` §1a-2 가
  「이건 API 가 아니라 CSV 라 승인·키가 아예 필요 없다」로 결론냈다(오너 지적 08-28).
  STATUS 와 인수인계 두 개가 그 뒤로도 옛 숙제를 옮겨 적고 있었다. 실제 남은 확인은
  분양권/입주권 구분 칸이 비는 문제 하나다(`data/silv-probe.md` 분포표로 판정).
- 🔴 **A군(도이체방크 세계 물가 2026) — PDF 원문 대조가 남았다.** 데이터셋
  `data/datasets/world-prices-2026.json` 이 `verified:false` 라 **발행 경로에 올리지 않는다**.
  시안 빌더는 사람이 직접 부르는 도구이고, 출력은 `data/out/_spike/world-price/` 로만 간다:

  ```bash
  node scripts/build-world-price.mjs      # 시안만 — builders.json 에 등록하지 않는다
  ```

  PDF 대조로 `verified:true` 가 되면 그때 `data/content/<날짜>/` 로 옮기고
  `builders.json`·`sets.json` 에 등록한다.
- 🔴 **9·7 대책 1주년 카드 — 데이터가 문이다.** 판형·문구는 오너 확정(새 폰트 세트 첫 적용).
  수치가 언론 보도 경유라 **빌더도 데이터셋도 아직 없다.** 국토부 「’26년 7월 주택통계」
  PDF(korea.kr 8/31 배포)와 8·13 대책 보도자료로 `verified:true` 를 세워야 제작에 들어간다.
  ⚠️ 8·13 「23만호+α」 보도와 총계 147만호(=135+12)가 안 맞는다 — 원문에서 확인할 것
- **금호두산 84** — 공급면적 97.52㎡·전용률 87.2% 경고. 실제 분양 평형을 확인하면
  `--accept-supply-warn` 으로 낼 수 있다 (09-03부터 남은 것)
- 소재 대기열: `research/ideas.json`(관제탑 보드) · `research/DECISION_LOG.md`

## 키 · 정기물 · 배관

> 🔒 **여기 적지 않는다.** 셋 다 정본이 따로 있고, 사본을 두면 갈라진다.

| 알고 싶은 것 | 정본 |
|---|---|
| 어떤 키가 등록돼 있나 | `docs/API_CONNECTIONS.md` · `node scripts/check-secrets.mjs` |
| 무엇이 언제 자동으로 도나 | `docs/DATA_REFRESH.md` · 관제탑 **「배관」 탭** |
| 세션에서 수집을 거는 법 | `CLAUDE.md` §6 (대기열 파일에 한 줄 push) |

> 키는 GitHub Secrets 또는 `.env`(gitignore). **저장소에 커밋 금지.**

---

> 과거 세션 서사: `docs/archive/STATUS-history.md`
> ⚠️ **이 파일에 서사를 쌓지 않는다.** 2026-08-08 에 한 번 비웠는데 3주 만에 578줄이
> 다시 쌓였다. `node scripts/prune-status.mjs` 가 세어서 doctor 가 경고한다.
