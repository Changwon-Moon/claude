# STATUS — 현재 상태 보드

> 모든 세션은 시작할 때 이 파일을 읽고, 끝낼 때 갱신한다(CLAUDE.md 참조).
> **여기엔 "지금 상태 + 다음 할 일"만 둔다.** 지나간 세션 서사는 `docs/archive/STATUS-history.md`,
> 소재 결정 이력은 `research/DECISION_LOG.md`로 간다. 다시 로그가 쌓이지 않게 한다.

**최종 갱신**: 2026-09-02

> ## ✅ 2026-09-02 — 신고가 15장 확정 · **호출 한도 절감 배관 신설** · 명부 오매칭 수리
>
> 인수인계: `claude/위릿-인수인계-2026-09-02.md` · 계획서: `claude/자료-국토부-호출한도-절감계획-2026-09-02.md`
>
> ### 🔒 되돌리지 말 것
> 1. **「구 × 월」 캐시** (`data/datasets/molit-monthly/{구}/{연월}.json`) — 곡선을 **호출 없이** 그린다.
>    곡선 한 번이 약 **1,500회**였다(아침 알림 전체가 122회). 그것 하나가 09-02 하루 예산을 태워
>    낮에 모든 국토부 배관이 막혔다. 정본은 `docs/DATA_REFRESH.md` 맨 아래.
>    · 아침 알림(04:55)이 **이미 받은 응답을 한 번 더 접어** 캐시를 채운다 — **추가 호출 0회**
>    · 곡선은 캐시를 **먼저** 읽고, 없는 달만 물어보고, 받은 김에 캐시에도 담는다
>    · `month-backfill.yml`(매일 06:40)이 과거 빈 칸을 **하루 1,200회씩** 채운다 → 3~4일이면 완료
>    · ⚠️ 칸마다 `scope:"universe"` 를 적는다. 그 표시가 없으면 **읽는 쪽이 안 믿는다** —
>      무엇을 걸렀는지 모르는 캐시는 「거래 없던 달」과 「안 담긴 달」을 같은 얼굴로 만든다(오보)
> 2. **명부 잇기 — 이름과 지번이 서로를 검사한다**(`pickUniverse`). 하계동 284 「청구」가
>    같은 동 「하계학여울청구」(1,476세대)에 부분일치로 붙어 **남의 세대수가 실렸다.**
>    08-13·08-26 에 이은 **세 번째**다 — 원인은 매번 「어느 쪽인지 모르는 채로 붙였다」인데
>    그 원칙이 **지번 쪽에만** 걸려 있었다.
> 3. **곡선은 단지마다 바로 커밋**한다(`singo-history.yml`). 중간에 접혀도 받은 것은 남는다.
> 4. **`--also`** — 늦게 온 자료로 지난날 못 낸 장을 오늘 묶음에 붙인다(`--from` 으로는 안 된다).
> 5. **`explainApiError`** — 「등록되지 않은 서비스키」를 **일일 한도**라고 풀어 말한다.
>    저장소는 08-16d 에 이미 알고 있었는데 문구가 그대로 나가 09-02 에 또 키를 의심했다.
> 6. **넘기는 물건은 「어디에 붙일 것인가」로 형태가 정해진다**(오너 09-02).
>    **ZIP = JPG + 인스타 캡션 둘뿐** · **카톡 캡션 = 채팅 텍스트**(복사해 붙일 것이라 파일이면
>    한 번 더 열어야 한다) · **확정본 md5 목록 = ZIP 밖 `<라벨>-목록.txt`**(우리가 되짚을 때만 쓴다).
>    정본은 `CARD_CHECKLIST` §0-A · `CAPTION.md` §9.
>
> ### 오늘의 결과
> `singo-daily-2026-09-02` **15장 오너 확정** · QA error 0 · JPG ZIP·카톡 캡션 전달.
> 어제 못 낸 2장(우장산·수원하늘채1단지 34평)을 `--also` 로 붙였다.
>
> ### 🔴 남은 것
> - **공급면적 3건** — 두산 34평 18.5억 · 산성역자이푸르지오1단지 25평 13.55억 ·
>   평촌관악타운 34평 10.4억. **네 번 밀어도 `fetch failed`** → 다음 세션이 다시 민다(오면 `--also`)
> - **곡선의 단지 고르기가 신고가 판정보다 느슨하다** — 수지 「현대」가 현대성우와 섞였다.
>   가드가 막았지만 고칠 곳은 분명하다(곡선에도 지번을 넘긴다). `CARD_CHECKLIST` §2 참고
> - ⛔ 영구 제외: 북수원자이렉스비아(관측 3개월) · 힐스테이트용인고진역2단지(8개월)
> - ✅ **`jeonse59-map`·`jeonse84-map` 강조색 빨간불 — 그 세션이 같은 날 고쳤다**(CEO.md ⑯).
>   코로플레스 **면(fill)** 을 검사 대상에서 뺐다(머리말엔 이미 그렇게 적혀 있었고 코드만 달랐다).
>   글자색·테두리는 그대로 본다


> ## ✅ 2026-09-01 — 작업 기준 둘이 굳었다 · 신고가 15장 확정
>
> 인수인계: `claude/위릿-인수인계-2026-09-01.md`
>
> ### 🔒 되돌리지 말 것 — 오너가 못박은 기준 셋
> 1. **넘기는 순서: HTML → 확정 → JPG ZIP** (*"작업은 항상 html로 먼저 만들고 확정 후 jpg로
>    작업해서 zip 파일로 올려주는 것으로 작업 기준 변경해줘"*).
>    정본은 `docs/CARD_CHECKLIST.md` **§0-A**. 마지막 걸음은 `scripts/deliver-set.mjs` 인데
>    **게이트**다 — 확정 안 된 세트를 거절하고, 확정 md5 와 지금 PNG 가 다르면 멈춘다.
>    ⚠️ **PNG 를 버리지 않는다.** 픽셀 증거는 PNG 의 md5 고 JPG 는 전달용 사본이다.
> 2. **중복 문지기가 묶는 자리에서 자동으로 돈다** (*"매 제작 시, 이런 검수 시스템이 자동으로
>    발현되도록"*). `singo-daily-set.mjs` 안. 갈래는 **둘뿐** — 단지·타입·가격이 모두 같으면
>    막고, **나머지는 전부 그냥 통과**해 다른 장과 똑같은 판형으로 간다
>    (*"진짜 중복만 아니면 다 똑같은 템플릿으로 진행해줘"*). 통과는 `--allow-dupe <슬러그>` 로
>    **그 장만** 명시한다. 가짜 중복을 심어 **실제로 막히는 것까지 확인**했다.
> 3. **평수는 건축물대장 원값 그대로** — 시장 통칭에 맞추지 않는다.
>    `docs/guides/신고가-카드-기준.md` **§11**. 08-31 의 대림1 「23평」 미결 사안도 이걸로 닫혔다.
>
> ### 오늘의 결과
> `singo-daily-2026-09-01` **캐러셀 15장 오너 확정** · QA error 0 · 스모크 259/259 ·
> 전수 626항 통과. 신고가 18건 중 3장이 빠졌다:
> - 🔴 **우장산아이파크·이편한세상 34평 16.4억** · **수원하늘채더퍼스트1단지 34평 8.9억** —
>   공급면적을 **네 번 밀어도** `fetch failed`. `apt-supply` 는 08-31 에 cron 을 떼서
>   **푸시가 유일한 손잡이**다 → **다음 세션이 다시 민다.** 오면 내일 캐러셀에 붙인다
> - 🚫 **호계2차현대홈타운 34평 8.1억** — 빌더가 거절(신고가 8.1억 vs 지난 사이클 고점 7.97억,
>   1.6% 차이 · 기준 3%). **자료 문제가 아니라 소재가 판형에 안 맞는 것**이라 다시 시도해도 같다

> ## 🟠 2026-08-28 — 분양권 프리미엄 배관 **두 갈래**, 오너 손 1분 남음
>
> 「27년 이후 입주 분양권 프리미엄」 커뮤니티 캡처를 소재로 받았다. **숫자는 반려**
> (CEO.md 07-19 「미검증 집계로는 정밀 순위 카드를 만들지 않는다」), **소재는 채택**.
> 오너 지시로 **A·B 둘 다** 놓았다 — `docs/API_CONNECTIONS.md` §1a · §1a-2.
>
> | 갈래 | 무엇 | 상태 |
> |---|---|---|
> | **A. 분양권 실거래** | 국토부 15126471 — 얼마에 팔렸나 | ✅ 승인·수집 확인(동탄·평택·영통 171건) |
> | **B. 주택형별 분양가** | 청약홈 15101047 — 프리미엄의 뺄셈 상대 | 🔴 **401 — 활용신청 대기** |
>
> ### 오너가 할 것 (자동승인이라 1분)
> data.go.kr → 「한국부동산원_청약홈_APT 주택형별 분양정보」(**15101047**) → 오픈API 탭 → **활용신청**.
> 키 발급은 필요 없다 — Secrets 의 `DATA_GO_KR_API_KEY` 를 그대로 쓴다.
> ⚠️ 우리가 매일 쓰는 청약홈 조회 서비스(15098547)와 **다른 데이터셋**이라 승인이 따로다.
>
> ### 프리미엄 = 분양권 실거래가 − 같은 타입 분양가
> 코드가 뺀다. 다만 분양가가 **「분양최고금액」**이라 결과는 실제 프리미엄의 **하한**이다 —
> 카드에 그 사실을 적는다(오너 판단 08-26 「최저호가를 쓰는 게 더 강한 말」과 같은 결).
> 그리고 원본이 **연 1회 갱신**이라 2026년 공고의 분양가는 없다.
>
> ### ⚠️ 남은 확인 하나 — `ownershipGbn` 이 비어 있다
> 분양권 첫 수집에서 분양권/입주권 구분 칸이 **평택 171건 전부 공백**이었다. 둘은 분양가
> 기준이 다른 물건이라 섞으면 그 자체가 오보다. 전 지역 재수집의 원값 분포표
> (`data/silv-probe.md`)로 확인한다. 끝내 비어 있으면 **청약홈 공고 목록과의 교집합**으로
> 분양권만 골라낸다(B 갈래가 그 목록을 가져오므로 같이 풀린다).

## 지금 상태 한눈에

- **운영 방식**: 세션이 저장소를 열어 카드를 만들고, **발행은 오너가 직접**(자동 발행 폐지). 정기 데이터 수집·재생산은 GitHub Actions.
- **최근 판형 작업**: `singo-record@1`(오늘의 신고가) **오너 확정 2026-08-16** — 기준은 `docs/guides/신고가-카드-기준.md`.
- **대기 중 판형**: `news-figure@1`(인물·뉴스 카드) 시안 완료 — **오너 선택 대기**. 오너가 고르면 `builders.json`·`sets.json` 등록 + 첫 확정 시 `sample.json` 고정.
- **인구 소재 배관**: 지역 인구 통계 자동 추출 경로와 시군구 2026년판 지도 신설(2026-08-03). KOSIS 기반, 통계청 행정구역코드로 조인.
- **최근 소재**: 등록외국인 국적 지도, 토허제×외국인 매수 등 소재 등록·승격 진행(상세는 `research/DECISION_LOG.md`).
- **신고가 판정 단위(2026-08-19 오너 재확인 · 유지)**: 전용 **59타입(56~62㎡)·84타입(82~85.5㎡)을 각각 한 칸으로 묶는다** — 서브면적(59.65/59.75/59.94)으로 가르지 않는다. 이것이 다른 서비스(실까 등)와 답이 갈리는 지점이라 "왜 우리엔 없나"가 반복해서 들어온다 → **`node scripts/singo-why.mjs <단지명> [--type 59] [--price <만원>]`** 으로 명부·문턱·서브면적별 최고가·알림 이력을 그 자리에서 본다(네트워크 불필요).

## 확정·발행 카드

> 🔒 **정본은 `data/review/sets.json`(confirmed) + `pixel-baselines.json`(md5)이다.**
> 여기 표를 손으로 두지 않는다 — 2026-08-31 에 뺐다. 코드가 이미 정답을 갖고 있는데
> 문서에 사본을 두면 **반드시 갈라진다**(어제 목록엔 이미 없는 카드가 남아 있었다).
> 지금 무엇이 확정됐는지는 관제탑 「보관함」 탭 또는 `node -e` 로 `sets.json` 을 본다.

정기물(실거래·증시·주간지수)은 "같은 데이터면 같은 픽셀"이 약속이라 `pixel-baselines.json` 에
넣지 않는다 — `confirm.mjs` 가 자동으로 가른다(고정물만 픽셀 고정).

## 다음 할 일 / 대기

- `news-figure@1` 판형을 오너가 고르면 등록 + 첫 실제 카드(인물 실물 사진 교체) 확정
- 「오늘의 신고가」 사진 도입 검토(2장 캐러셀: 사진 커버 + 곡선) — **저작권 확인이 먼저**
- 인구 카드 시리즈 전개(자동 추출 배관 활용)
- ✅ **낡은 수집 3개 재개 완료(2026-08-31)** — 인구·증시·ECOS 전부 성공.
  인구는 KOSIS 키 만료로 27일간 조용히 실패 중이었고, 오너가 키를 교체해 복구했다
  (`population/2026-07.json` 수신). 교훈은 `docs/API_CONNECTIONS.md` 맨 위
- ⭐ **「대장 도감」 되살렸다(2026-08-31)** — 1화(강남구) 재생산 확인, `builders.json` 등록 완료.
  **다음: 2화(서초구) 데이터셋을 만들면 시리즈가 이어진다.** 매주 한 구씩 25주 치 콘텐츠
- 소재 대기열: `research/ideas.json`(관제탑 보드) · `research/DECISION_LOG.md`

## 키 · 정기물 · 배관

> 🔒 **여기 적지 않는다.** 셋 다 정본이 따로 있고, 사본을 두면 갈라진다.

| 알고 싶은 것 | 정본 |
|---|---|
| 어떤 키가 등록돼 있나 | `docs/API_CONNECTIONS.md` · `node scripts/check-secrets.mjs` |
| 무엇이 언제 자동으로 도나 | `docs/DATA_REFRESH.md` · 관제탑 **「배관」 탭** |
| 세션에서 수집을 거는 법 | `CLAUDE.md` §6 (대기열 파일에 한 줄 push) |

> 키는 GitHub Secrets 또는 `.env`(gitignore). **저장소에 커밋 금지.**

---

> 과거 세션 서사: `docs/archive/STATUS-history.md`
> ⚠️ **이 파일에 서사를 쌓지 않는다.** 2026-08-08 에 한 번 비웠는데 3주 만에 578줄이
> 다시 쌓였다. `node scripts/prune-status.mjs` 가 세어서 doctor 가 경고한다.
