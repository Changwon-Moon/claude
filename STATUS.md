# STATUS — 프로젝트 진행 상황 보드

> 모든 세션은 시작할 때 이 파일을 읽고, 끝낼 때 갱신한다. (CLAUDE.md 참조)
> 마일스톤 정의와 발주 프롬프트: [docs/EXECUTION_PLAN.md](./docs/EXECUTION_PLAN.md) §4

**최종 갱신**: 2026-08-03

> **08-03 (코워크 세션) 인구 소재 자동 추출 배관 + 시군구 지도 2026년판 신설**
> 오너 "지역별 인구 통계나 특징 소재를 자동으로 추출하고 카드뉴스로 작업하는 세션을 만들자".
> ① **오너 초안을 실측이 뒤집었다.** 처음 고른 경로는 "행안부 주민등록 먼저, KOSIS 나중"이었는데
>    두 가지가 걸렸다. (a) **코드 체계** — 우리 지도는 통계청 행정구역코드(동작구 11200)인데 행안부
>    주민등록은 법정동코드(동작구 11590)다. 시도 2자리가 서울만 우연히 같아 붙는 것처럼 보이지만
>    **시군구부터 다른 체계**라 그대로 조인하면 엉뚱한 구가 칠해진다. (b) **규격 검증** — data.go.kr
>    상세 화면이 세션에서 안 열려 행안부 API 필드를 한 글자도 확인 못 했다. KOSIS 는 공개 문서로 확인된다.
>    → 오너 재결정: **KOSIS 로 전환**(`KOSIS_API_KEY` 신규 필요).
> ② **지도가 2013년판이었다** — 청원군(2014 소멸)이 살아 있고, 인천 미추홀구가 '남구'이고, 화성 4구
>    분구(2026-02)가 없다. 기존 부동산 카드는 서울·경기 일부만 써서 안 드러났지만, **전국 인구 히트맵은
>    시군구를 전수로 칠하는 카드**라 그대로 보인다. → `korea-sgg-2026.geojson`(255개) 신설.
> ③ **합치는 방법 — 좌표를 새로 만들지 않는다**: 폴리곤 union 을 계산하는 대신 **맞닿은 변을 지운다**
>    (같은 경계선이 a→b·b→a 로 한 번씩 나타난다). 원본에 없던 좌표가 0개라 부동소수점 union 이 만드는
>    틈·자기교차가 원천적으로 없다. 닫힌 고리로 안 이어지면 던진다. ⚠️ **바깥 고리를 회전 방향으로
>    판정하면 안 된다** — 원본이 시계방향이라 부호로 가르면 전부 구멍이 된다(여기서 한 번 막혔다).
>    **포함 깊이**로 가른다. 렌더 검증: 255개 시군구·섬·경계 정상.
> ④ **한 파일로 두 코드를 잇는다**: 원본(`vuski/admdongkor`)이 `adm_cd`(통계청)와 `sgg`(법정동)를 다 준다
>    → `data/geo/sgg-codes.json` 대조표. **인구(KOSIS)와 실거래(국토부)를 같은 지도에 얹을 수 있다.**
> ⑤ **소재는 코드가 고른다**(`parse/kosis.ts` `score()`): 문턱 돌파·붕괴(5만~100만) / N개월 연속 증감 /
>    1년 증감률 전국 상·하위 TOP10. 가점은 지역·규모. 문턱 45점·상위 8건만 보드에. **잘라낸 개수를
>    알림에 적는다**(조용히 자르면 "이번 달은 이게 다"로 읽힌다). ⚠️ 연속 개월에서 **값이 같은 달은
>    연속을 끊는다** — 이어 세면 개월 수가 부풀려져 그대로 오보다(셀프테스트로 고정).
> ⑥ **조용한 실패를 세 곳에서 막았다**: (a) 키 없으면 워크플로 중단 (b) KOSIS 필드 이름이 바뀌면 파서가
>    던진다(빈 결과는 "그 달에 인구가 없었다"와 구분이 안 된다) (c) 셀프테스트는 수집 **뒤에** 돈다.
>    추가로 **지도 조인을 매 수집마다 검사**해 미매칭 시군구를 찍는다.
> ⑦ **`--dry` 배관 전 구간 확인**(세션 외부망 차단): 시군구 255곳·26개 시점 → **지도 조인 255/255,
>    미매칭 0건** → 소재 151건 → 상위 8건 보드 등록 → 알림 문구 생성. 합성 데이터가 실수로 보드에
>    올라가지 않게 `meta.dry` 가면 등록기가 **exit 1** 로 막는 것도 확인했다(산출도 `_dry/` 로 격리).
> ⑧ 셀프테스트 **160 통과**(인구 40항 신규). 렌더 파일은 **하나도 안 건드렸다**(git status 로 확인).
> ⑩ **자동화 수위 재조정 — 보드 자동 등록을 껐다.** 오너 재선택: "데이터만 자동, 소재는 내가 고른다."
>    청약홈 공고는 **사건**(마감이 있고 무엇이 소재인지 자명)이지만 인구는 **상태**(매달 조금씩 움직이고
>    어느 각도로 자를지가 곧 기획)라 성질이 다르다. → 수집기는 `research/ideas.json` 을 건드리지 않고
>    신호를 점수순으로 정리한 **`data/population-signals.md`** 를 커밋 + 텔레그램 알림만 한다.
>    자동 등록은 `--register`(대기열 `register=1`)로 남겨 뒀다 — 나중에 한 줄로 켜진다.
>    CEO.md 에 "자동화 수위는 배관이 아니라 소재의 성질이 정한다" 원칙으로 기록.
> ⑨ **네 갈래로 넓혔다(오너가 넷 다 선택)** — 인구·세대 / 인구이동 / 연령·고령화 / 출생·사망.
>    표 ID 는 찾았으나 kosis.kr 본문이 안 열려 **항목코드·분류축이 대부분 미확인**이라,
>    `enabled` 플래그로 **인구(확실)만 켜고 나머지 5개는 검증 대기**로 뒀다. 셀프테스트가
>    "확신도 '확실'이 아닌데 켜져 있으면 실패"를 강제한다. 검증은 `kosis-probe` 워크플로가
>    표마다 최근 1개 시점만 받아 `data/kosis-probe.md` 에 통계표명·항목코드·분류축을 적어 커밋한다.
>    ⚠️ 이 단계를 건너뛰면 안 되는 이유: 사망 표엔 '사망원인 50항목' 축이 있어 '계'를 안 고르면
>    **특정 사인의 숫자가 총사망자수로 나간다** — 응답도 숫자도 그럴싸해 눈으로는 안 잡힌다.
> ⑪ **오너 최종 확인(08-03)**: 통계 4갈래(인구·세대 / 인구이동 / 연령·고령화 / 가구·1인가구) ·
>    전국 시군구 · "데이터만 자동, 소재는 내가 고름" — **셋 다 이미 구축된 것과 일치**해 추가 변경 없음.
>    다만 **1인가구 비중은 표를 못 찾았다**(`세대원수별` 축이 있는 KOSIS 표 미확인).
>    **추측 tblId 를 적지 않았다** — probe 때 `DT_1B040B3` 분류축에 세대원수가 있는지 먼저 본다.
>    세대당 인구(인구÷세대수)로 서사의 7할은 나오므로 급하지 않다.
> ⑫ **워크플로가 조용히 안 돌던 이유(08-03 발견)**: `kosis-probe.yml` 과 `auto-produce.yml` 의
>    YAML 이 깨져 있었다. `run: |` 블록 안 여러 줄 셸 문자열의 **이어지는 줄을 1열에 붙여 써서**
>    블록이 끊겼다. GitHub 은 깨진 워크플로를 **등록조차 하지 않아 실행 목록에 뜨지 않는다** —
>    빨간불조차 없다. 실패보다 나쁘다. 실패는 보이지만 이건 안 보인다.
>    → 둘 다 `printf` 로 고치고, `scripts/lint-workflows.mjs` 를 만들어 `doctor` 가 상시 검사한다.
>    (KOSIS_API_KEY 는 오너가 등록 완료. 키 문제가 아니었다.)
> ⚠️ **남은 것**: (a) 오너가 **kosis.kr 에서 `KOSIS_API_KEY` 발급 → Secrets 등록**해야 첫 실제 수집이 돈다.
>    (b) 데이터셋 `verified:false` — 표 ID·필드 이름을 실제 응답으로 대조하기 전까지 **발행 금지**.
>    (c) 인구 카드 템플릿·빌더는 아직 없다(소재가 먼저 올라온 뒤 오너가 고르면 만든다).
> ℹ️ `sinbundang-map` designQa error 6건 · `sinbundang-loop` 픽셀 불일치는 **07-31 시안의 기존 건**으로
>    08-02 오너 판정("다른 세션 작업이니 손대지 않는다")에 따라 그대로 뒀다.


> **08-02 (청약홈) 배관 실전 가동 — 대기열 푸시 방아쇠 + 첫 실제 수집 3회**
> ① **오너가 Actions 탭을 열지 않는다**: `push: paths: [data/applyhome-queue.txt]` 방아쇠를 달았다. 세션은 `api.github.com` 이 막혀 [Run workflow] 를 못 누르지만 **푸시는 된다** — molit-queue·market-queue 와 같은 방식(docs/DATA_REFRESH.md §5). **앞으로 새 수집 워크플로는 대기열 방아쇠를 처음부터 단다.**
> ② **세션의 눈 `data/applyhome-last.md`**: 성공·실패 무관하게 실행 조건·로그·등록 결과를 적어 커밋한다. 세션은 Actions 로그를 못 보므로 이게 유일한 창구다(`article-fetch-last.md` 와 같은 방식).
> ③ **첫 실제 수집 성공**(2026-08-02): APT 분양 **2,836건** · 무순위 **1,647건** 수신 → 최근 7일·접수중 14건 → 문턱 45점 넘은 9건이 소재 보드에. 필드 이름·키 모두 실물에서 확인됐다(그 전까지는 공개 스펙 기준 추정이었다).
> ④ **첫 실행이 잡아낸 문제 — 블록 도배**: 상위 5칸을 「더샵 송도그란테르」가 G5-1·3·4·5·11블록으로 채웠다. `mergeBlocks()` 로 같은 종류·지역·접수마감일이면 합친다(150가구·88점 한 줄). **합치기는 점수 매기기 앞**에 온다. '안산고잔2차'의 2차는 단지명 일부라 건드리지 않는다.
> ⑤ **두 번째 문제 — 보드가 하루 만에 낡는다**: 등록만 하고 갱신을 안 하면 D-N 이 굳는다(어제 D-3 이 오늘도 D-3). 이번 수집에 있는 공고는 title·why·deadline 을 덮어쓴다. **오너가 손댄 줄은 절대 안 덮는다.**
> ⑥ 셀프테스트 **120 통과**(청약홈 35항). 검증: 리서치로 사람이 찾았던 「한강 푸르지오 리버프론트」가 자동 수집에도 65점으로 올라왔다.
> ⚠️ **오너 판정**: 신분당 노선 카드(`sinbundang-loop`)의 doctor 픽셀 불일치는 **정기물이 아니고 다른 세션 작업이라 손대지 않는다**. `pixel-baselines.json` 에서 빼지 말 것.

> **08-02 노선 시세 시리즈 — 원커맨드 자동화 + 4~8호선 대규모 편집 + 스킬/기준 학습 반영**
> ① **원커맨드**: `node scripts/line-card.mjs <노선>` 하나로 리프레시→캡션→빌드→렌더→designQa 자동. 각 pick 에 molit 원본키(umd·srcApt)를 심어(`enrich-line-src.mjs`) 리프레시 **자동매칭 100%**. CLAUDE.md 트리거절 + LINE_CARDS §9.
> ② **4~8호선 편집**(오너 지시): 역 추가/삭제/이름·단지 변경. 8호선 +별내·다산·구리·천호, 7호선 +도봉산·노원·사가정·철산·광명사거리·온수·부천시청, 6호선 +응암·불광·연신내·창신(약수→청구·봉화산→신내), 5호선 +영등포시장·서대문·청구(단지 6건 교체), 4호선 +안산중앙. 상봉 KTX·공덕 5·경의·공항 등 환승뱃지 누락 재확인, **GTX 뱃지 축소**(레일 겹침 방지).
> ③ **데이터 충돌 처리(오보 0)**: 오너 지정 단지 중 국토부 84㎡ 매매 실거래 없는 6건 → 오너 확인 후 실전용 병기(SUB: 청화 전용106·하이페리온 전용119·신금호 전용60·목동 전용101) 또는 역 84㎡ 대장 대체. `meta.flags` 기록.
> ④ **학습 반영**: CEO.md(08-02 원칙), CARD_CHECKLIST §6, LINE_CARDS §4·5·9, WIRIT_CARDS_SKILL.md(노선 트리거·읽기목록) 갱신. 경기 신규시 202607 수집 대기 중(도착 시 자동 갱신 예약).
> ⑤ **검증**: 전 9종 designQa error 0.

> **08-01 (3) 청약홈 API 일일 수집 배관 — 무순위(줍줍)·신규분양 소재 자동 발굴**
> 오너 "청약홈 api 연결돼 있으니 하루 한 번 자동으로 돌려서 줍줍·신규분양 소재 뽑아줘".
> ① **어디서 도나**: 세션이 아니라 **GitHub Actions**(`applyhome-collect.yml`, 매일 08:00 KST). 세션은 외부망이 막혀 있다 — 2026-08-01 실측 `apis.data.go.kr` HTTP 000. 세션은 창을 닫으면 사라지므로 "매일"은 달력이 지킨다.
> ② **범위(오너 선택)**: APT 신규 분양 + 무순위·잔여세대 두 갈래 / **전국 수집 + 수도권 가점** / 자동화는 **소재 등록 + 알림까지**(카드 제작은 오너 판단 — CEO 07-26 '3번만 개입').
> ③ **만든 것**: `packages/collectors/src/{sources,parse}/applyhome.ts` · `applyhomeCli.ts`(`--dry` 지원) · `scripts/register-applyhome-ideas.mjs` · 워크플로 1개. 셀프테스트 **26항 추가(총 111 통과)**.
> ④ **점수 규칙은 전부 코드다**(`score()`): 지역·규모·무순위 여부·마감 임박도·브랜드 보유·상한제. 45점 이상만 `research/ideas.json` 에 오른다. LLM 이 고르지 않는다.
> ⑤ **조용한 실패를 세 곳에서 막았다**: (a) 키 없으면 워크플로 **중단**(07-31 알림키 사고) (b) 응답 **필드 이름이 바뀌면 파서가 던진다**(빈 결과는 '공고 없음'과 구분이 안 된다) (c) **셀프테스트는 수집 뒤에** 돈다(07-31 증시 수집이 테스트 실패로 며칠 죽었던 사고).
> ⑥ **보드가 쌓이지 않게**: 같은 공고 재등록 금지 · **접수 끝났고 오너 미선택인 자동 소재는 삭제**(오너가 손댄 건 보존) · 알림은 새 게 있을 때만.
> ⚠️ **아직 확인 못 한 것**: 실제 응답을 한 번도 못 봤다(외부망 차단). 필드 이름은 공개 스펙 기준이고 표본은 픽스처다. 배관은 `--dry` 로 세션에서 전 구간 확인했다(줍줍 95점·상동역 53점·지방소형 14점 → 문턱 45로 2건 등록, 마감 지난 뒤 재실행 시 1건 자동 정리). **`DATA_GO_KR_API_KEY` 가 Secrets 에 실제로 있는지, 청약홈 오퍼레이션이 활용신청 승인됐는지는 세션이 볼 수 없다** — 첫 실행 결과로만 알 수 있다.

> **08-01 (2) 상동역 롯데캐슬 시그니처 카드 — 템플릿 재사용 첫 사례**
> 오너 "최근 분양한 상동역 롯데캐슬로도 하나" → **템플릿·빌더를 그대로 두고 데이터만 추가**했다. 롯데건설 사업개요+한국경제·뉴시스(6.30) 기준 부천 상동 옛 홈플러스 부지·지하8~지상49층 7개동·**1,859가구 전량 일반분양**·전용 84~192㎡ 12개 타입(84㎡ 1,370가구 73.7%). **분양가·청약일정·입주예정은 1차 출처를 못 잡아 '미고지'로 적었다**(모집공고 7/30·특공 8/10 설은 소셜 게시물 한 건뿐이라 쓰지 않음).
> 빌더에 늘어난 안전장치(다음 단지에 자동 적용): ① `mainArea()` 가 **주택형별 세대수 합 ≠ 총 세대수면 던진다**(1,370+308+178+3=1,859 검산 통과) ② 면적대 5개 이상이면 범위 표기로 축약 ③ `site`(부지 내력)가 있으면 제목이 그것으로 간다 ④ 날짜 없으면 '미고지'(칸은 안 지운다) ⑤ 푸터 출처는 매체 하나만.
> 검증: designQa error 0(3장) · rebuild-cards error 0 · doctor 발행·확정본 **7장 픽셀 불변** · audit-head 위반 0 · smoke **110/110**.

> **08-01 (1) 코워크 세션 — 신규 템플릿 `danji-brief@1`(청약단지 브리핑) 시안 2장**
> ① **오너 주문**: 레퍼런스 3장(@theflow.daily 목동재건축·대치선경, @apt_lap 공릉 행복주택)을 주며 "위릿카드 청약단지 소개 템플릿을 제대로 만들어보자". 앞선 대화에서 최근 이슈 분양단지 리서치를 먼저 했다.
> ② **형태는 물어서 정했다**(CEO 07-31 "단계마다 끊어 묻는다"): 사진 비중 → **상단 사진 + 하단 정보표** / 시점 → **분양 예정 + 청약 결과 둘 다** / 고정 항목 → 단지명·시공사·위치, 총 세대수·일반분양·최고층, 평형·분양가, 청약 일정.
> ③ **한 템플릿 두 변형**: `presale`·`result` 는 **자리가 같고 뜻만 다르다**. 숫자칸 셋 중 앞의 둘은 공유, 세 번째만 최고층 ↔ 1순위 경쟁률. 칸의 뜻은 데이터가 정하고 템플릿은 배치만 한다 → 변형이 늘어도 템플릿은 안 는다.
> ④ **오보 0 장치**: 경쟁률을 받아 적지 않고 `접수÷모집` 으로 **다시 계산**해 보도값과 1.0 넘게 벌어지면 던진다. 일반분양 총량(특공+1순위)도 코드가 더한다. **'역대 최고'는 근거를 적은 `record` 필드가 있을 때만** 나간다(우리 데이터에 과거 경쟁률이 없어 코드가 확인 못 한다). 제목의 "인천 최대어"는 확인 불가라 뺐다.
> ⑤ **공급평형을 못 쓴 이유**: 전용→공급 환산은 단지마다 다른 관행값이라 계산이 아니다. 모집공고가 안 밝힌 단지에서 34평이라 적으면 지어낸 숫자다 → 전용㎡ 표기 + 표 아래 단서. **원칙(㎡ 대신 평형)과 오보 0이 부딪히면 오보 0이 이긴다.**
> ⑥ **검수 등록 4곳**(LEAF·colalign·clipped·footerGap) + **일부러 어긋나게 되돌려 실제로 잡히는지 확인**: 머리글 grid 를 다르게 → colalign 4건, 하단 문구 아래 여백 0 → footergap, 단지명 78px → textclip 2건. 통과만 보면 검사가 켜졌는지 알 수 없다.
> ⑦ **검증**: designQa error 0(2장) · rebuild-cards error 0 · render-sets 16/16 · smoke-tower **110/110** · doctor **발행·확정본 7장 픽셀 불변** · audit-head 위반 0장.
> ⚠️ **남은 것**: (a) **조감도를 한 장도 안 들였다** — 이 판형의 병목은 레이아웃이 아니라 사진 조달이다. 시안은 오픈라이선스 대체 사진에 "시안용 대체 이미지"라고 카드에 밝혀 뒀다. (b) 데이터셋 `bunyang-danji-2026.json` 은 **verified:false**(청약홈 모집공고문 미대조) → 발행 금지. (c) 오너 확정 전이라 `sets.json`·`builders.json`·`pixel-baselines.json` 어디에도 안 올렸다.

> **07-31 (3) 코워크 세션 — 신규 템플릿 `sinbundang-map` + 신분당선 대장아파트 지도 카드(시안)**
> ① **소재**: 오너 요청 — 신분당선 역세권 '대장아파트'를 지도 인포그래픽으로. 1·2차 리서치에서 대장 선정 기준이 "가장 비싼 단지"였던 것을 오너가 지적 → **역근접·인지도·거래량·가격리딩·세대수 5기준 종합**으로 재선정(신논현 아크로힐스논현→서초푸르지오써밋, 판교 그랑블→봇들마을, 동천 이스트팰리스→동천센트럴자이 등). 신사·양재시민의숲·광교(종점)는 초역세권 대단지 부재로 표기.
> ② **신규 템플릿 `sinbundang-map@1`**(노선도형 지도): 세로 노선 레일(신분당 공식색 #D4003B)+정거장 점+역명/단지/대표평형 시세. 페이퍼·잉크 톤, 괘선(표티 억제, AI카드 금지 톤 준수). **카드 위 패딩 72px·코너 뱃지 공용 규격 그대로 둠**(brandhead 회귀 방지 — 07-31 map-board 사고 반복 안 함).
> ③ **오보 0**: 모든 수치는 `data/datasets/sinbundang-daejang-2026.json`(provenance·**verified:false**)에서 코드가 읽는다. 빌더 손수치 0개. 데이터는 웹 리서치 종합(호갱노노·리치고·KB·집품·실거래 기사·나무위키) 근사치 → **발행 전 국토부 실거래가 재대조 필요**(세트 note 명시).
> ④ **검수 배관**: 새 템플릿 글자요소(`.sbm-stn`·`.sbm-danji`·`.sbm-price .v/.sz`·`.sbm-note`)를 `designQa.ts` LEAF·footerGap 목록에 등록 → **실제로 겹침을 잰다**(등록 과정에서 시세↔평형 2px 겹침·세로 오버플로를 검수가 잡아 수정함 — 검사가 살아있음을 확인). record-grid·metro 사고 교훈 준수.
> ⑤ **등록**: `builders.json`+`sets.json`(시안, verified:false note)+`captions/sinbundang-map.txt`(태그5·금지어0·기간표기·'억'수치 카드대조 통과). 평면 슬러그(`data/content/{date}/sinbundang-map.json`)로 세트 인식.
> ⑥ **검증**: rebuild-cards designQa **error 0**, render-sets 16/16, smoke-tower **110/110**, doctor **발행본 6장 픽셀 불변**(픽셀 회귀 통과 — 공용파일 안 건드림). 오너에게 렌더 PNG 전송, **확정 대기**.
> ⚠️ 남은 것: 오너 확정 시 → verified 승격(국토부 대조)·`pixel-baselines.json` md5 추가·산출물 `data/content/{날짜}/`로 이동. titlegap 22px warn은 발행 카드들과 동급(행 10개 수용 위해 유지).

> **07-31 (2) 코워크 세션 — 커버 없이 재업로드 3건 + 지하철 카드 배관 구멍 발견**
> ① **오너 결정**: 「서울 최고가 25평(59㎡)」·「34평(84㎡)」·「출근길 지하철 속도」 3건을 **커버 없이, 3개 별도 게시물로 재업로드**. `sets.json` 의 `cards` 에서 커버 슬러그만 제거(`estate-cover-59`·`estate-cover-84`·`metro-cover-photo`), **커버 빌더는 `builders.json` 에 그대로 남겼다** — 되돌릴 수 있어야 하므로. 본문 카드 픽셀은 손대지 않았다(doctor 픽셀 회귀 24/24 통과).
> ② **구멍 발견 — 지하철 카드는 갓 clone 한 환경에서 아예 생성되지 않았다.** `scripts/build-metro-speed-card.mjs` 가 **`builders.json` 에 등록돼 있지 않았고**, `metro-speed` 세트는 `caption: ""` 였다. `smoke-tower` 의 「세트↔빌더 정합」 검사는 **캡션이 있는 세트만** 보기 때문에(`if (!hasCap) continue`) 이 세트는 검사를 통째로 건너뛰었다 — **통과한 게 아니라 검사받지 않은 것**. → 빌더 등록 + `data/review/captions/metro-speed.txt` 신규 작성 + `sets.json` 캡션 연결로 검사 대상에 올렸다. 재발 방지 항목을 `CARD_CHECKLIST.md` §2 「세트 등록 — 캡션이 없으면 검사도 없다」에 추가.
> ③ **검수**: 3세트 모두 결정성 ✓ · designQa **error 0**. warn 은 전부 `brandhead`(뱃지 배경·위 여백 50~52px)인데 **07-21 에 이미 발행된 카드의 픽셀**이라 픽셀 불변 원칙에 따라 고치지 않았다 — 다음 신규 카드부터 공용 규격(72px·잉크네이비 판)을 지킨다. 스모크 **108/108** 통과.
> ④ **캡션**: `metro-speed.txt` 신규(커버가 지고 있던 후킹 "1등과 꼴등이 같은 노선"을 캡션 첫 단락으로 이관). 부동산 2건 캡션은 원래 커버를 참조하지 않아 그대로 사용.
> ⑤ **세션 부트스트랩 개선**: 코워크 컨테이너의 `GH_TOKEN` 은 `proxy-injected` 자리표시자이고 이 저장소는 세션에 붙지 않아 403 이 난다(`add_repo` 도구 없음). → 오너가 **클로드 프로젝트 「위릿노트」의 `[Fine-grained tokens].txt`** 에 토큰을 두기로 결정했고, `wirit-cards` 스킬 1단계를 "오너에게 묻지 말고 프로젝트 문서에서 읽는다"로 고쳐 재패키징해 전달했다. **새 창에서 토큰을 다시 붙여넣을 필요가 없다.**
> ⚠️ 남은 것: 지하철 카드는 커버를 빼면 **1장짜리 단일 이미지 게시물**이 된다(캐러셀 아님) — 오너 확인 필요.

> **07-31 (3) 오너 디자인 지시 3건 반영 — 제목 확대 · 지도 워터마크 · 지하철 워터마크 정리**
> ① **map-rank 제목을 칸에 꽉 채운다**(오너 "각각 제목 더 키워서 꽉 채워주고"). 고정 62px 를 버리고 제목 칸을 486→556px 로 넓힌 뒤 `__wiritFit` 이 **네 줄 중 가장 긴 줄이 칸을 채우는 크기**를 이분탐색한다(약 92px). 엠블럼·평형 뱃지는 px→em 으로 바꿔 제목과 **함께** 커진다. 지도는 그만큼 작아졌다(430→360px) — 제목 우선이라는 오너 지시에 따른 의도된 교환.
> ② **지도 좌상단 빈 사분면에 워드마크**(`.mr-wm`) — 서울 도형이 닿지 않는 자리라 데이터를 가리지 않는다(§2 "데이터 위에는 아무것도 얹지 않는다" 준수).
> ③ **metro-2col 워터마크 260개 → 2개**(오너 "수도없이 들어간 워터마크 싹 지우고 깔끔하게 2개만"). 대각 반복 타일을 버리고 큰 워드마크 좌상·우하 2개로. 스크립트가 `"wirit. ".repeat(260)` 하던 코드도 제거.
> ④ **검사 목록 확장** — `map-rank`·`metro-2col` 의 글자가 `designQa` LEAF 에 **하나도 없었다**(두 템플릿 전체가 겹침 검사 밖). 제목을 키우는 변경과 함께 올렸다: `.mr-title .tx`·`.mr-pyeong b`·표 셀·`.m2-seg`·`.m2-val`. 넣고 나서 **일부러 뱃지를 왼쪽으로 밀어** 실제로 `textclip` 이 잡히는지 확인했다(잡힘 → 원복).
> ⑤ **함정 3건 기록**(CARD_CHECKLIST §4): (a) 템플릿 좌표는 **CSS px**(1080×1350)이지 PNG 픽셀(2160×2700)이 아니다 — 워터마크 하나가 카드 밖으로 잘려 사라졌다. (b) `designQa` 의 Range 측정은 **절대배치 자식을 부모 Range 에 포함**해 줄 상자를 부풀린다 → 글자를 `.tx` 로 감싸 해결. (c) `.m2-wm span` 같은 후손 셀렉터는 안쪽 `.dot` 까지 절대배치로 만든다 → `> span`.
> **검증**: designQa **error 0** · 스모크 **108/108** · doctor **필수 24항**(다른 발행본 픽셀 회귀 없음 — 두 템플릿은 이 3건 외에 쓰는 카드가 없다).

> **07-31 (4) 워터마크 3차 조정** — 오너 "점까지 회색으로, 더 연하게. 최고가 4장은 표 안에".
> ① **마침표까지 같은 회색**(`.dot { color: inherit }`) — 코발트 점은 이 농도에서 혼자 튄다. 농도도 낮춤(map-rank 0.17→0.045, metro 0.06→0.045).
> ② **map-rank 워터마크를 지도 좌상단 → 순위표 한가운데로 이동**(지도는 비웠다). 크게 1개, 대각선.
> ③ ⚠️ **워터마크를 `<div>` 로 넣었더니 `.mr-row:nth-child(even)` 셈이 한 칸 밀려 짝수행 음영이 통째로 뒤집혔다.** `::before` 가상요소로 바꿔 해결 — 가상요소는 nth-child 에 세지 않는다. 픽셀 샘플링으로 이전 판본과 밴딩이 같은지 대조 확인.
> ④ ⚠️ **회전(`rotate`) 뒤에는 `left: 20px` 도 잘린다** — 모서리가 바깥으로 돌아 나간다. `left/top: 50%` + `translate(-50%,-50%)` 로 중앙 기준 배치. 지하철 카드도 좌우 여백 8→44px.
> **검증**: designQa error 0 · 스모크 108/108 · doctor 필수 24항.

> **07-31 (5) 지하철 표 — 구간 머리글 좌측 정렬 + 구간명 노선색**
> ① **'구간' 머리글을 왼쪽으로**(패딩 8px = `.m2-seg` 와 동일) — 데이터가 왼쪽인데 머리글만 가운데라 떠 보였다. §2 "머리글 정렬은 데이터에 맞춘다".
> ② **구간명을 노선색으로 칠한다** — 새 헬퍼 `metroInk`(`renderHtml.ts`). 색은 뱃지와 **같은 카탈로그**(`templates/_shared/metro-lines.json`)에서 오고 템플릿에 손으로 적지 않는다.
> ③ **"밝으면 테두리"를 눈이 아니라 계산으로 가른다** — 종이 바탕(#FAFAF8) 대비 WCAG 3.2 미만이면 같은 색을 0.45배로 깎아 테두리를 두른다. `paint-order: stroke fill` 이라 테두리가 글자 뒤로 깔려 획이 안 뭉개진다. 24개 노선 중 **11개가 자동으로 테두리 대상**(수인분당 #FABE00 대비 1.62, 경의중앙 1.97, 서해 2.01 …). 노선이 추가돼도 자동으로 맞는다.
> ④ ⚠️ **테두리 방식은 실패했다** — 오너 "글씨가 안예쁘다, 테두리도 이상해". 한글 27px 에서 테두리가 획을 뚱뚱하게 만들어 스티커처럼 보였고, **테두리 있는 줄만 굵어져 표가 들쭉날쭉**해졌다. → **테두리를 걷어내고 색 자체를 낮췄다**: HSL 로 바꿔 색상·채도는 두고 **밝기만** 이분탐색으로 내려 대비 4.2 를 맞춘다(`inkFor()`). 굵기는 모든 줄 동일 800. 24개 노선 중 15개가 자동 조정(수인분당 #FABE00 → #977300, 대비 1.62 → 4.21). 재발 방지는 `CARD_CHECKLIST.md` §2 색 절에 기록.
> **검증**: designQa error 0 · 스모크 110/110 · doctor 필수 24항. (다른 세션의 `sinbundang-map` 카드도 이 세션에서 병합 — 검수 항목 충돌 없음)

> **07-31 (6) 머리 규격 전수 정리** — 오너 "위릿뱃지와 카드 흰색테두리 등 기준에 안 맞는 거 전체 수정".
> ① **전수 점검 창구 신설**: `packages/renderer/src/auditHead.ts`(`pnpm --filter @wirit/renderer audit-head`). 카드 전체를 렌더해 **위 여백 · 뱃지 판 배경 · 뱃지 글자 크기 · 최외곽 흰 테두리**를 한 표로 찍는다. `designQa` 의 `brandhead` 는 warn 이라 한 장씩 볼 때 흘러갔고, **그래서 5개 템플릿·13장이 어긋난 채 굴러가고 있었다** — 한 장씩 보면 안 보이고 표로 보면 1초에 보인다. `CARD_CHECKLIST.md` §0 에 편입.
> ② **되돌린 템플릿 4종**: `map-rank`·`metro-2col`·`index-2026`(위 여백 50~52px·투명 뱃지·글자 40px → 공용 72px·잉크네이비 판·36px), `singoga-map`(기본 44px → 72px). 모두 `.wirit-corner` 재정의를 **삭제**해 base.css 를 그대로 상속하게 했다.
> ③ **흰 테두리(3px + 안쪽 잉크 12px)는 25장 전부 정상**이었다 — 어긋난 건 뱃지·위 여백뿐.
> ④ **예외 2종을 표에 명시**: (a) 표지형 전면 사진 카드(estate-cover·metro-cover-photo) — 사진 위에서 잉크네이비 판이 안 보여 흰 판·패딩 0 이 정상, (b) `.sm-c`(compact) 92px — 제목이 뱃지에 붙지 않게 **더 준** 값이라 위반이 아니다. 판정도 `padTop >= 72` 로 바꿔 "넓은 건 통과"하게 했다.
> ⑤ 부작용 1건 수정: map-rank 위 여백이 22px 늘자 표가 짧아져 **1~3위 메달이 위아래 행과 겹쳤다**(designQa 가 잡음). `.mr-top` 높이 452 → 430 으로 잃은 만큼 돌려줬다.
> **검증**: designQa error 0 · 스모크 110/110 · **doctor 픽셀 회귀 6장 전부 동일**(발행·확정본은 하나도 안 바뀌었다 — 고친 템플릿을 쓰는 baselined 카드가 없다) · audit-head **위반 0장**.

> **07-31 (7) 최고가 4장 7월 반영 + 정기물 자동 갱신 배관 완성**
> ① **출처에서 '서울시 행정경계' 제거**(오너 지시) — 행정경계는 지도를 그리는 도형일 뿐 표의 숫자가 나온 곳이 아니다.
> ② **집계 창을 '그해 1월~최신월 누적'으로**(오너 결정). 기존 `slice(-6)` 은 7월이 들어오면 1월을 밀어내 **구에 따라 최고가가 내려갔다**. 2026.1~07월 기준으로 재생산 — 용산 44.2→46.0억, 양천 목동센트럴푸르지오 24→목동신시가지14 24.5억, 중랑 15.2→15.3억, 노원 14.2→14.3억. 1~3위·25위는 그대로.
> ③ **"7월분 신고 진행 중" 자동 표기** — 최신월이 신고기한(계약 후 30일) 안이면 출처에 붙는다. 판정은 빌드 날짜와 최신월의 차이로 하므로 결정적이다. 캡션에도 같은 단서를 넣었다.
> ④ **세션은 Run workflow 버튼을 못 누른다**(api.github.com 프록시 403 — 오너 토큰으로도 동일). → `molit-collect.yml` 에 **대기열 파일 푸시 트리거**를 붙였다(`data/molit-queue.txt`, kr-market 과 같은 방식). 실제로 이 방식으로 7월 데이터를 받아왔다.
> ⑤ **빠져 있던 고리를 이었다 — `auto-produce.yml`**: `data/datasets/**` 가 바뀌면 영향받는 세트를 골라 재생산·검수·머리규격 점검·커밋·관제탑 배포·알림까지 자동. 그전에는 수집기가 돌아도 **카드는 아무도 다시 만들지 않았다**(오너가 [제작 실행]을 눌러야 했다).
> ⑥ **의존 관계는 손으로 안 적는다** — `scripts/affected-sets.mjs` 가 빌더 스크립트 소스에서 `data/datasets/…` 경로를 뽑아 대조한다. `builders.json` 에 `deps` 칸을 두면 반드시 낡는다.
> ⑦ **수집 주기를 신고기한에 맞췄다** — 매월 5일·20일, 정기 실행은 항상 `--force`(캐시가 있으면 건너뛰므로 force 없이는 늦은 신고가 영영 반영 안 된다), 기본 대상 서울 25개구 전체.
> ⑧ **업무 기준 문서화**: `docs/DATA_REFRESH.md` — 무엇을 기계에 맡기고 무엇을 사람에게 남기는지, 배관도, 고장 판별법. `HANDOFF.md` §2 읽는 순서에 편입.
> **기계의 끝은 "결재 대기에 올려두는 것"까지다** — 발행은 오너가 직접 한다(07-27 결정) 그대로.

> **07-31 (8) 최고가 4장 문안·레이아웃 정리 (오너 5건)**
> ① **출처를 짧게** — `국토부 실거래가 · 2026.01~07월 기준`. 월은 두 자리로 0을 채운다. "7월분 신고 진행 중" 단서는 **캡션이 진다**(카드 출처 줄은 짧게).
> ② **상단 회색 캡션 삭제 + 기준 신설** — 제목과 출처로 충분하면 안 쓴다(`CARD_CHECKLIST` §2 「상단 회색 캡션」). 제목이 "전용면적 59㎡ 최고가 APT"라 하고 기간은 출처에 있는데 위에 또 얹으면 같은 말을 두 번 하는 것이다. **캡션은 제목이 말하지 않는 것**(측정 정의 등)이 있을 때만 — 지하철 카드의 "표정속도 = …"는 남긴다.
> ③ **표 열 균형** — `78/172/1fr/168 · gap12 · pad14` → `88/182/1fr/170 · gap20 · pad18`. 좌측 세 열이 붙어 있고 오른쪽에만 구멍이 나던 것을 폭 전체로 퍼뜨렸다.
> ④ **제목 문안 변경** — `서울 / 중상급지·중하급지 [약 N평] / 전용면적 N㎡ / 최고가 APT`. 평형 뱃지를 절대배치에서 **2번째 줄 인라인**으로 옮기고(줄과 함께 움직여 어긋날 여지가 없다), 크기 0.645→0.55em, 상하 대칭 패딩 + `align-items:center` 로 글자를 알약 한가운데에.
> ⑤ ⚠️ **조용히 잘리던 글자를 검사로 승격** — 열 폭을 줄이자 "현대14차(203,204,205,206동)"이 "…"로 잘렸는데, 기존 truncate 검사는 `.rt-name` 만 봐서 아무도 재지 않았다. `clipped` 검사 신설(**error**) — `.mr-apt`·`.mr-gu`·`.m2-seg`·`.rt-name`·`.sm-gu` 를 좌표로 잰다. 일부러 칸을 좁혀 실제로 잡히는 것 확인했다.
> ⑥ **빈 `.wirit-topcap` 오탐 수정** — 캡션을 안 쓰자 빈 상자가 남았고, Range 가 잡을 글자가 없어 상자 전체로 재는 바람에 뱃지와 74px 겹친 것으로 나왔다. 글자 없는 껍데기는 겹침 대상에서 제외(그림 요소는 유지). 템플릿도 캡션이 없으면 아예 안 그리고 그 32px 를 아래 여백으로 돌려준다.
> **검증**: designQa error 0 · 스모크 110/110 · audit-head 위반 0장 · doctor 픽셀 회귀 6장 동일.

> **07-31 (9) 되찾은 자리를 그림에 쓰기 + 급지 문구 색 결정**
> ① **블록을 위로**(margin 46→34) — 한계는 **우상단 뱃지 아래끝**(72+34=106 vs 뱃지 102)이다. 더 올리면 지도 오른쪽 위가 뱃지와 부딪힌다.
> ② **제목 85.1 → 88.7px, 지도 358 → 368px.** 폭은 제로섬이라(제목·지도가 한 행을 나눠 쓴다) 군더더기를 줄여 양쪽에 나눴다 — 평형 뱃지를 0.55→0.5em 으로 줄여 **2번째 줄이 '가장 긴 줄' 자리를 내주게** 하니 글자가 5% 커졌고, 칸 사이 간격 20→12px 로 지도에 8px 를 돌려줬다.
> ③ ⚠️ **세로 여백을 늘려도 폭에 묶인 요소는 안 커진다** — 지도는 358×296(칸 358×430)으로 **폭이 한계**였다. 높이를 키웠다면 빈 공간만 늘었을 것이다. 그래서 `.mr-top` 은 오히려 442→420 으로 줄여 죽은 여백을 걷고 표에 돌려줬다.
> ④ **급지 문구 = 코발트(#2e6bff)** — 오너 질문 "코발트냐 서울로고 파랑이냐"에 대한 결론. ⓐ 계정이 정한 강조 1축이고 이미 쓰이고 있다 ⓑ 종이 대비 **4.31** 로 글자 기준(4.2) 통과 — 서울로고 파랑(#2e86d0)은 **3.70** 이라 깎아야 하고 깎으면 로고 색이 아니게 된다 ⓒ 로고 색을 바로 옆 글자에 쓰면 엠블럼과 글자가 섞여 둘 다 약해진다. `CARD_CHECKLIST` §2 색 절에 기준으로 올렸다.
> **검증**: designQa error 0 · 스모크 110/110 · audit-head 위반 0장 · doctor 필수 24항.

> **07-31 (10) 확정 — 3건 오너 확정 + 확정 절차의 단축어화 + 학습 정리**
> ① **확정**: 「서울 최고가 34평」·「25평」(각 캐러셀 2장) · 「출근길 지하철 속도」(1장). 커버 없이, 2026.01~07월 반영본.
> ② **`scripts/confirm.mjs` 신설 — 오너가 "확정할게" 하면 한 줄로 끝난다.** 재생산 → 검수(error 면 중단) → 확정 기록 → 픽셀 기준값 → §0 검사 5종 → 커밋. 순서가 늘 같고 판단이 없는 일은 사람이 외울 일이 아니다.
> ③ ⚠️ **「픽셀 불변」의 범위를 갈랐다 — 고정물 / 정기물.** 실거래·증시처럼 매달 다시 그려지는 카드를 `pixel-baselines` 에 넣으면 다음 수집일에 doctor 가 통째로 빨간불이 된다. **정기물의 약속은 "픽셀이 안 바뀐다"가 아니라 "같은 데이터면 같은 픽셀"(결정성)**이고, 확정은 그 판본 md5 를 `sets.json` 에 증거로 남기는 것으로 한다. 판정은 `confirm.mjs` 가 **워크플로에서 갱신 데이터셋 목록을 읽어** 자동으로 가른다(손으로 적으면 낡는다). → estate-84·estate-59 는 정기물, metro-speed 는 고정물로 갈렸고 기준값에 1건 등록됐다.
> ④ **학습 정리 4곳**: `company/CEO.md`(오너 판단 원칙 12건) · `company/teams/{design,qa}.md`(무엇을 배웠나) · `prompts/{design,qa}.md`(그래서 어떻게 손을 움직이나) · `research/DECISION_LOG.md`(커버 제외·게시물 단위·집계 창 결정). `CARD_CHECKLIST` §0 에 확정 스크립트, §5 에 고정물/정기물 표를 넣었다.
> ⑤ **게시물 단위**: 부동산 2건은 **평형별 캐러셀 2장 = 게시물 2개** 유지. 4장 개별 안도 캡션까지 써 두었으나, 이 카드의 행동유도가 "저장하고 우리 구 찾기"라 상하위를 쪼개면 저장 유인이 준다.
> ⑥ **스킬 갱신 재배포** — 확정 단축어·`audit-head`·자동 갱신 배관·학습 4곳 표를 `wirit-cards` 스킬에 반영해 오너에게 다시 전달했다(사본: `docs/WIRIT_CARDS_SKILL.md`).

> **08-01 완성본을 폰에서 볼 수 있게 + 관제탑 배포가 막혀 있던 원인 제거**
> ① 🚨 **관제탑이 배포되지 않고 있었다.** `sim-tower.mjs` 가 옛 탭 이름(`today`·`board`)을 손으로 들고 있었는데 화면이 `publish` 하나로 합쳐지면서 **30초 대기 후 예외로 죽었고**, 그 단계가 `tower-deploy` 를 통째로 막았다. 화면은 고쳤는데 나가질 않고 있었다 — 오너가 본 404 의 배경이다.
> ② **검사가 스스로 낡아 배포를 막지 않게** 고쳤다: 탭 목록을 **화면에서 읽고**, 직접 `page.click/fill` 대신 2.5초 안에 실패를 알리고 넘어가는 wrapper 를 쓴다. 첫 낡은 선택자에서 멈추지 않고 **전부 모아 보고**한다. 데이터가 있어야만 그려지는 요소는 `optional` 로 두어 조용한 날에 배포가 막히지 않게 했다. 사라진 기능(지시함)의 검사는 걷어냈다 — 없는 기능을 두드리면 진짜 결함이 그 속에 묻힌다.
> ③ **완성본 폴더에 `index.html` 이 없어 11건 전부 404 였다.** Cloudflare 정적 자산은 index 없는 폴더를 목록으로 안 보여준다. `stage-public-cards.mjs` 가 **폴더마다 모바일 갤러리 페이지**(카드 세로 나열 + 캡션 + 복사 버튼)와 **전체 목록 `/published/`** 를 만든다. 다시 그리지 않는다 — 커밋된 그때 그 JPEG 를 감싸기만 한다.
> ④ **`tower-deploy` 감시 경로에 `stage-public-cards.mjs`·`build-archive.mjs`·`published/**` 를 추가**했다. 빠져 있어서 완성본 화면을 고쳐도 배포가 안 돌았다.
> **검증**: 스모크 110/110 · 시뮬 결함 0건·콘솔 오류 0건(이전에는 시뮬이 죽었다).

> **07-26 세션 요약**: **관제탑 대규모 개편 — "현황 보고판"에서 "지휘 도구"로.** 전수 검사 후 3단계 전부 반영.
> ① **정리·통합**: 연결 상태 바(한 줄) → **제목 행 우측 뱃지 + 팝오버**로 축소. 개발용 워크플로 버튼(마이닝·로고취득·평균연봉) 제거. 파이프라인 첫 열의 **컨텐츠 마이닝 패널 폐지** → 소재 고르기는 `💡 소재` 탭 **단일 창구**로 일원화, 칸반은 '기획안'부터. 미연결 시 조작 UI를 **아예 잠근다**(`.locked` — 눌렸다 실패하느니 안 눌리고 이유를 말한다).
> ② **소재↔파이프라인 배관**(핵심): 소재 카드에 **[▶ 진행]** 버튼 신설 → `research/ideas.json`에 `stage:1`이 붙고 파이프라인 '기획안' 열에 티켓으로 뜬다. `Idea`에 `stage`·`reason`·`at` 추가, `buildState`가 stage≥1 소재를 티켓으로 승격(렌더 카드가 이미 있으면 중복 생성 안 함).
> ③ **학습 루프**: 반려·보류에 **이유를 받는다**(원터치 칩 5종 + 자유 입력). 이유는 소재 카드와 `decisions-inbox.md`에 남고, 다음 [새 소재 발굴] 요청에 최근 반려 이유 5건이 **"피할 것"으로 실려** 나간다. 취소하면 상태를 바꾸지 않는다.
> ④ **결정함(🔔 오늘) 첫 화면**: 접속 즉시 "내가 결정할 것"만. 없으면 **"오늘 결정할 것 없음"**을 분명히 표시(앱을 닫아도 된다는 뜻 — 매일 5분 운영). 6건 상한 + 더 보기.
> ⑤ **발행 세트 도입**(`data/review/sets.json`): `data/content`에 실험·중간 렌더가 섞여 결정함이 **27건**으로 불어나던 문제 → 오너가 한 번에 승인하는 단위를 명시. 등록 세트만 발행 후보(**27 → 5건**), 미등록은 `실험` 칩으로 파이프라인에만. **캡션 없으면 결정함에 안 올린다**(올릴 글이 없으면 결정할 게 없다). KPI 숫자도 같은 기준으로 통일.
> ⑥ **발행 승인 화면**: 카드 **전 장 캐러셀** + **캡션 전문** + **자동검수 판정**을 한 화면에. 검수가 pass가 아니면 승인 시 재확인. 액션 바를 스크롤 밖 진짜 하단 바로 분리(본문 겹침 해소).
> ⑦ **모바일**: 칸반 가로 스크롤 → **세로 아코디언**(빈 단계는 접힘), 버튼 엄지 크기, 탭 가로 스크롤.
> ⑧~⑪ **자동화 연결**: `pipeline-tick.yml`(푸시 + 매일 07:00 KST) — 승인된 소재 → **작업지시서 자동 생성**(`research/work-orders/{id}.md`, 최근 반려 이유 동봉) / **발행 대기열**(`data/publish-queue.md`) 점검(캡션·검수 BLOCK 확인) / **성과 회수**(`data/performance.md` — 도달·**저장**·좋아요·댓글) / **알림**(2026-07-31부터 텔레그램).
> ⑨ ~~**알림은 카카오톡 "나에게 보내기"**(무료·단방향) 채택~~ **→ 2026-07-31 텔레그램으로 대체**(아래 세션 로그). 당시 판단은 — 오너 질문("텔레그램보다 카카오톡은?")에 대한 결론. 알림톡은 사업자등록·건당 과금이라 제외, 승인은 어차피 관제탑 웹에서 하므로 단방향으로 충분. 시크릿 없으면 조용히 건너뛰어 배포를 깨지 않는다. → 가이드: [docs/guides/telegram-notify.md](./docs/guides/telegram-notify.md)
> **검증**: 관제탑 스모크 **35항목**(구조·읽기전용·쓰기·배관·학습·충돌재시도·결정함) 전부 통과, 파서 셀프테스트 24/24, 게이트 9/9. 데스크톱·모바일 스크린샷 확인.
> **→ 남은 오너 액션**: (i) **텔레그램 알림 설정** — 봇 만들고 시크릿 2개, 3분 ([가이드](./docs/guides/telegram-notify.md)), (ii) 토허제/국장 카드 **발행 승인**, (iii) DART·토허제 원문 대조로 `verified=true` 승격, (iv) Anthropic 크레딧 소액 충전(LLM 감각검수 ON), (v) IG 토큰 발급 시 실제 업로드·성과 자동 회수 가동.

> **07-26 (2) 관제탑 재설계·간소화**: 오너 전수 검토 반영. ① **409 저장 유실 근본 수정** — 승인 2건 연타 시 한 건이 실제로 유실됐음(확인). 재시도만으론 부족해 **모든 저장소 쓰기를 직렬화**(`GH.serial`) + 재시도 5회/백오프 3.2초. 유실된 국장 성적표는 대기열에 복구. ② **지표 명칭**: 결재 대기 / 작업중 / 소재 풀 / 데이터 자산 (누르면 해당 화면으로). ③ **이미지 전략** — 카드 PNG 장당 870KB인데 목록엔 40px로 보였음(화면 무게의 대부분이 안 보이는 픽셀). 보이는 크기만큼만 싣도록 축소(목록 200px·상세 900px, Chromium 캔버스), 큰 판본은 캡션 있는 발행 후보에만 → **8.0MB → 650KB**. ④ **파이프라인 = 흐름 레일 + 단계별 세로 목록**(7열 칸반 폐지, 가로/무한 세로 스크롤 제거). 급한 것부터 정렬, 빈 그룹 자동 접힘, **결재 대기만 그림+무게**. 승인대기를 '결재 대기'(캡션 有)와 '캡션 대기'로 분리 → **지표=레일=그룹=탭 배지 숫자 일치**. 실험 렌더 17건은 접힌 서랍으로. ⑤ **소재 보드 3버튼 간소화** — 승인·보류·반려 폐지, **▶진행 / ✎수정 / 🗑삭제**. 삭제 시 사유를 적으면 학습 신호로 남고(다음 발굴에 "피할 것"으로 실림), 비우면 그냥 삭제. 수정 폼에 항목 이름 부착. 진행·완료 소재는 보드에서 분리(파이프라인·보관함으로). ⑥ **작업 진행 가시화** — 누르면 버튼 잠금 + "진행 중" 표시, 하단 작업 표시줄, 저장소 작업(Actions) 감시(7초/45초), 끝나면 알림 + **자동 반영**(수동 새로고침 버튼 제거). ⑦ **회사 = 조직도 우선** — 일의 흐름(발굴→기획→제작→검수→발행)에 팀 배치, 팀을 눌러야 원칙·업무기준이 열림. CEO 원칙은 접힌 섹션. ⑧ **보관함 신설** — `scripts/build-archive.mjs`가 완성 작업물을 **소재 분류대로 자동 폴더링**해 `data/archive/index.json` 생성(카드·캡션·검수·PNG 경로 색인). ⑨ 디자인 시스템 재작성: Pretendard(카드와 동일·별도 파일), 색 절제(코발트=결정 필요, 빨강=막힘), 탭 이모지 제거, 제목의 HTML·마크다운 잔재 제거. ⑩ **발행 승인이 파이프라인에 반영** — `data/publish-queue.md`를 읽어 발행됨으로 올리고 되돌리기 제공. **검증: 스모크 54항목·파서 24·게이트 9 전부 통과.**

> **07-26 (3) 관제탑 정합성 점검**: ① **조직도를 CEO 정점 계통도로 재작성** — CEO → 총괄(오케스트레이터) → 5개 본부(발굴·기획·제작·검수·발행). 카드 나열이 아니라 보고선을 그린다. 팀을 눌러야 원칙·업무기준이 열림. ② **보관함 실데이터 연동** — 카드 실물 썸네일 + **캡션 전문**(복사 버튼) + 검수 요약 + **저장소 원본 링크**(카드 JSON·PNG·캡션·검수). ③ **[중단·삭제]가 안 먹던 버그 수정**(오너 보고) — 결정이 브라우저에만 남아 재빌드 때 되살아났다. `data/pipeline-state.json`에 기록하고 buildState가 읽어 반영. **수정지시·발행취소도 같은 문제**였음 → 수정지시는 pipeline-state의 `revise[]`에, 발행취소는 `publish-queue.md`에서 해당 줄을 실제로 삭제. 버튼 이름 '이 건 중단' → **'중단·삭제'**. ④ **CSS `display`가 `[hidden]`을 덮던 버그** — 팀 상세가 전부 펼쳐져 있었고 작업 표시줄도 항상 보였다. `.tpanel[hidden]`·`.jobbar[hidden]` 명시 + 전 셀렉터 감사. ⑤ **결정함 노이즈 제거** — 미선택 소재 38건을 매일 띄우던 것을 **새로 발굴된 소재** 또는 **파이프라인이 빈 경우**로 한정. ⑥ **전 버튼 시뮬레이션 도입**(`scripts/sim-tower.mjs`) — 모든 탭·지표·드로어 액션·소재/회사/보관함 버튼을 실제로 눌러 오류를 잡는다. 배포 파이프라인에 편입. **검증: 스모크 59항목 + 시뮬 전 버튼 통과.**

> **07-23 세션 요약**: **국장(증시) 성적표 캐러셀 완성·발행 대기 + 브랜드 기준 2건 갱신**. ① **신규 세트 2장**: `index-cover@1`(어그로 커버 — 시그니처 색반전 타이포 `세계 수익률 1위`[빨강·취소선]→`세계 꼴찌`[블루 초대형], 파이낸셜뉴스 2026-07-20 근거) + `index-2026@1`(코스피·코스닥 연중 추이 — **메인 포커스=고점 대비 하락률**). 코스피 6,748.0(7/21)·고점 9,114.6(6/22)·**고점대비 −26.0%(29일 만에)**·연초4,309.6→고점 +111.5%. 코스닥 753.3·고점1,226.2(4/27)·**−38.6%(85일 만에)**·+29.7%. 수집기 `krMarketCli`(야후 파이낸스 차트 API, 코스피^KS11·코스닥^KQ11 — Stooq는 러너 봇차단) → `data/datasets/kr-market-2026.json`. 차트 SVG: 연초·고점·현재 3점 주석+날짜축, **상승 곡선화살표(빨강)·하락 곡선화살표(코발트 굵게)**, 고점 라벨 상단밴드+흰 헤일로로 겹침 해소, `preserveAspectRatio="none"` 왜곡은 컨테이너 비율=viewBox로 방지. ② **커버 배경 = 추상 캔들차트 사진**(일본 전광판→교체): `photo-fetch.yml`에 **후보 다장(count) 모드** 추가(cand/에 10장 받아 눈검증 후 승격) → Pexels #0(Tima Miroshnichenko) 채택(`stock-crash.jpg`, 손·외국어 없음). ③ **기준 2건 갱신(BRAND.md 반영)**: **(a) 제목 기본 폰트=태백체**(운영자 업로드, 이전 Wanted Sans). OTF는 Chromium OTS 거부(CFF+세로메트릭)→ `font_pipeline.py`+`font-convert.yml`(Actions, opentype-sanitizer·otf2ttf)로 **OTF→WOFF2 변환** 후 사용. **(b) 최외곽 얇은 흰색 테두리 기본**(캡처 시 경계 또렷 — 흰 3px + 안쪽 잉크 12px). ④ **숫자 표기 규칙**: 증시 카드 전 수치 **소수1 고정**(−26.0% 등), 하락 뱃지에 **경과일**(NN일 만에) 병기. ⑤ **업로드 캡션**(`data/review/captions/index-2026.txt`): 코드 린트 통과(태그5·금지어0·기간·출처·후킹·수치일치). **금지어에 '수익률' 포함** 확인 → 캡션은 '오르며/상승'으로 우회. 전 카드 결정성(동일 해시)+designQa 통과. **발행 대기 4호(국장 성적표)**. → 남은 오너 액션: (i) LLM 감각검수 ON은 여전히 Anthropic 크레딧 소액 충전 대기, (ii) 발행 승인 게이트.

> **07-22 세션 요약**: **부동산 실거래 랭킹 발행 세트 완성**(발행 대기 3호). ① P1 수집기(국토부 실거래) 6개월치(2026.1~6, 25개 구 150파일) 캐시 → **'대장 엔진'**(밴드필터 먼저→아파트별 최고가; `build-map-rank`·`build-estate-cover` 공통). ② **신규 템플릿 2종**: `map-rank@1`(서울 자치구 코로플레스 지도+순위표 2장 — 페이지별 언급 구만 색상·나머지 회색, 좌측 4줄 제목+**서울시 공식 심볼마크**, 평형 뱃지, '최고가' 빨강, 1·2·3위 메달), `estate-cover@1`(다크 스카이라인 사진 커버 — 질문형+정답 가리기+3줄 제목'2026년 상반기 기준'+골드 강조). ③ **자산 취득**: 서울 심볼마크 SVG(위키미디어 MediaWiki API 해석→Actions `svg-asset-fetch.yml`), 커버 사진(Pexels `seoul-apart-night`, `photo-fetch.yml`). ④ **캡션 확정**(84·59㎡): 해시태그 5개·면책문구 제외·기간 구체표기. ⑤ **전 팀 학습 반영**(design·marketing·asset-hub·editing·qa teams+prompts, CEO 원칙 3건, PATTERN_LIBRARY '우리 동네는?' 공식). 검증값: 84㎡ 1위 서초 래미안원베일리 71.5억(25위 도봉 11.5억, 6.2배)·59㎡ 1위 49.5억. 전 카드 결정성(동일 해시)+designQa 통과. **캐러셀 3장**(커버→1~13위→14~25위). ⑥ **자동 검수 루프 착수**(오너 "최소 개입·에이전트끼리 검수"): `packages/pipeline/src/review/` — 결정성·레이아웃·**캡션 린트**·**캡션↔카드 수치 대조**를 묶어 pass/revise/**block**(발행 차단) 판정. 코드 검수는 키 없이 동작(실증: 잘못된 캡션 자동 BLOCK — 태그16·면책어·"70억"≠카드71.5억), LLM 검수(디자인·카피·적대적 렌즈)는 `ANTHROPIC_API_KEY` 있을 때 추가. `review.yml`+루브릭(`docs/REVIEW_RUBRIC.md`)+오너 가이드(`docs/guides/anthropic-key.md`). **→ 검수 루프 CI 실전 검증 완료(run #3): 크롬 설치·콘텐츠 재생성·코드 검수 3장 전부 통과(결정성·레이아웃·캡션·수치). ✅ ANTHROPIC_API_KEY 등록·인증 성공 확인(llm.available:true, 모델 claude-sonnet-5). ⚠️ 단 계정 크레딧 잔액 0 → LLM 4렌즈 미실행(코드가 우아하게 스킵, info 레벨). #1 레퍼런스 벤치마크 렌즈 추가 완료. 리포트: data/review/estate-84.json. → 다음 오너 액션(마지막): console.anthropic.com Billing에서 크레딧 소액 충전($5)하면 감각 검수 ON. 이후 승인 게이트(텔레그램)·IG 발행 순.**

> **07-21 세션 요약**: 지하철 표정속도 **캐러셀 최종 확정 = 2장**(p1 후킹 커버 + p2 2단 표, p3 마무리 장 폐기). 커버=Pexels 감성 사진+정답 가리기+대형 후킹 제목(98px)+하단 잉크 바(워드마크 40px)+`👉 …다음장에서` CTA / p2 2단 정보표=노선 뱃지·급완 태그·km/h 셀·워터마크·최고(코발트)·최저(레드) 하이라이트. 신규 템플릿 `metro-2col`·`metro-cover-photo`, 노선 뱃지 시스템(`metro-lines.json`), **사진 자동취득 다중소스**(Pexels/Pixabay/Wikimedia, Actions). **전 팀 기본 업무 기준 문서화**(각 teams/*.md·CEO.md). **발행 캡션 확정**(data/content/2026-07-20/captions.md ✅ 확정 발행본, "KTX 저리가라"→"도심 도로보다 빠름" 안전문구 교체). **✅ 수치 검증 완료(verified✓, 07-21)**: 헤드라인 서울–청량리 29.3=연합뉴스(2016) 정확일치, 천안–서울 급행 68.6=공식 급행시간표(85분) 오차 내, 서울교통공사 1~8호선 표정속도=공식 공개데이터 정확일치, 신분당 48.3=법령맥락 보도. 코레일·9호선은 동일기준 신뢰(원자료 개별대조 추후). 검증이력은 data/datasets/metro-speed-2026.json meta.verification.

> ⚠️ **우선순위 재정렬 (2026-07-19)**: 시스템의 척추는 **리서치·기획(무엇을 만들지 결정)** — [docs/RESEARCH_WORKFLOW.md](./docs/RESEARCH_WORKFLOW.md)가 로드맵보다 우선. R0(운영자 수동)로 시작해 결정 로그를 쌓고 점진 자동화(R1→R3). M4(자동 수치수집)·M0/M5 발행부는 정기 고정물(증시)에만 먼저 적용하는 부수 트랙.
> **다음 액션**: **평균연봉 Top10 카드 디자인 확정(2026-07-20)** — 제목행 가운데정렬·제목 표폭 자동정렬·금은동 메달·좌우대칭·세로 꽉채움, 로고 10/10 실로고(Tier C 가동), `designQa` 통과·결정성 확인. 남은 선택지 — ① **평균연봉 카드 발행 승인**(오너의 DART 1~2개사 원문 대조 후 — 아래 참고), ② 관제탑 라이브 실동작(⚠️저장소 **Settings→Pages=GitHub Actions** 미설정으로 배포 404 — 파일을 본인 브라우저로 직접 열어 토큰 연결하거나 Pages 설정 필요), ③ 남은 키(ECOS·공공데이터) 발급 → 환율·부동산 카드.
> ⚠️ **평균연봉 카드 발행 전 확인 필요**: DART API(1차 출처)에서 코드로 직접 추출했고 값이 업계 통념과 정성적으로 합치하나, 세션이 웹 열람 불가라 DART 원문 화면 대조는 못 함(오보 0건 원칙 — "1~2개사 원문 대조 후 verified=true" 미이행). 운영자가 DART 사이트에서 1~2개사 대조 확인 후 발행 승인 권장.

## 마일스톤 현황

| # | 마일스톤 | 상태 | 비고 |
|---|---|---|---|
| — | 설계 문서 작성 (로드맵, 아키텍처, 에이전트, 템플릿, 소스, 운영, 실행전략) | ✅ 완료 | 2026-07-18 |
| M0 | 준비 — 계정·API 키 발급 | 🟡 가이드 완료·운영자 수행 대기 | 가이드: [docs/guides/M0-setup.md](./docs/guides/M0-setup.md). 운영자가 키 6종 발급 후 M1 |
| M1 | 레포 스캐폴딩 + 렌더러 코어 | ✅ 완료 | JSON→PNG 렌더러 동작. 결정성·스키마검증 확인. 미리보기: templates/dummy-card/preview.png |
| M2 | 템플릿 1호 ranking-table | ✅ 완료 | **시안 A 확정**(2026-07-18). Pretendard 번들, 5/10/15행·긴이름 검증 |
| M3 | 템플릿 2호 market-daily (증시) | ✅ 완료 | 국기·1년차트·상승레드/하락코발트·종목그리드. **이미지 로딩 파이프라인 확립**(국기로 증명). vs-compare는 M3b로 이월 |
| M4 | 수집기 P0 (증시·환율·금리) + Actions cron | 🟡 코드완료·실데이터 검증 대기 | 파서 셀프테스트 15/15 통과. 실제 수집은 Actions(네트워크 개방)+ECOS키 필요 |
| M5 | 첫 무인 콘텐츠: 증시 데일리 E2E 자동 발행 | 🟡 데이터→카드 자동화 완료 | `packages/pipeline` raw→콘텐츠JSON→PNG 검증(간밤의 미국 증시). 남은 것: 발행(IG토큰) + 실데이터(Actions) |
| M6 | 편집 에이전트 + 팩트체크 | ⬜ 대기 | |
| M7 | 리서치·기획 에이전트 | ⬜ 대기 | |
| M8 | 오케스트레이터 + 모바일 승인 게이트 | 🟡 승인 게이트 가동 | 관제탑 모바일 승인 + `pipeline-tick.yml`(승인→작업지시서·발행대기열·성과·알림). 알림은 **텔레그램**으로 전환(2026-07-31) — 오너 3분 작업 대기 |
| M9 | 운영 대시보드 | ✅ **C단계 가동** | 관제탑이 저장소를 직접 되쓴다. 결정함 중심 재설계 + 소재↔파이프라인 배관 + 승인→실행 자동화(2026-07-26, [설계 §4c](./docs/CONTROL_TOWER.md)). Cloudflare Workers 상시 운영·비밀번호 문 |
| M10 | 디자인 QA + 콘텐츠 뱅크 | 🟡 디자인 자동검수 가동 | `designQa`(정렬·여백·넘침·행겹침·**패딩침범 padcross**) — `pnpm --filter @wirit/renderer qa`, error 시 발행 차단. 뱅크는 대기 |
| M11 | 인사이트 수집 + 주간 성과 리포트 | 🟡 오너 수동 기입 | `data/performance.md`(도달·저장·좋아요·댓글) — 수동 발행이므로 **오너가 인스타 인사이트를 보고 직접 기입**한다. 발행 사실 자체는 `published/index.json` 이 갖는다 |
| M12 | 운영 안정화 + 자동화 수위(L0~L3) | ⬜ 대기 | |

## Phase 0 결정 대기 항목 (운영자 결정 필요)

- [x] **니치 확정** (2026-07-18): **부동산·주식·경제 코어(60~70%) + 범용 통계·랭킹 트래픽(25~35%) 하이브리드** — 상세는 [docs/CONTENT_STRATEGY.md](./docs/CONTENT_STRATEGY.md)
- [x] **주간 발행 캘린더 초안** (2026-07-18): CONTENT_STRATEGY.md §4 — 아침/저녁 증시 무인 슬롯 + 낮 메인 슬롯
- [x] **계정명·브랜드 확정** (2026-07-18): **wirit (위릿), @wirit_note** — 로고·잉크네이비 팔레트 확정, 상세는 [docs/BRAND.md](./docs/BRAND.md). 계정 개설 완료 (프로페셔널 전환은 M0에서)
  - 남은 소작업: 로고 원본 `assets/brand/logo.png` 업로드 ([방법](./assets/brand/README.md)), 하락 색상(코발트 제안) M3 시안에서 확정

**→ Phase 0 결정 사항 전부 완료. 다음 작업: M0 발주.**

## 발급된 키/계정 (이름만 기록 — 값은 절대 기록 금지)

| 항목 | 상태 |
|---|---|
| 🟢 금융감독원 DART (`DART_API_KEY`) — 평균연봉·실적, 무료 | ✅ 발급·등록 완료 (2026-07-20). 평균연봉 10개사 수집 성공 |
| 🟢 한국은행 ECOS (`ECOS_API_KEY`) — 환율·금리, 무료 | ⬜ M0 4단계 |
| 🟢 공공데이터포털 (`DATA_GO_KR_API_KEY`) — 부동산, 무료 | ⬜ M0 3단계 |
| 🟢 Brandfetch (`BRANDFETCH_API_KEY`) — 로고, 무료·선택 | ⬜ M0 6단계 (선택) |
| 🔵 Instagram 프로페셔널 계정 (@wirit_note 전환) | ⬜ M0 1단계 (발행 단계) |
| ~~Meta 앱 + 인스타 토큰~~ | ⛔ 불필요 — 2026-07-27 오너 결정으로 수동 발행 |
| 🔵 Anthropic (`ANTHROPIC_API_KEY`) — AI 직원, 소액결제 | ⬜ M0 7단계 (AI 단계) |
| GitHub Secrets 등록 | ⬜ M0 8단계 |
| 🟢 텔레그램 알림 (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`) — **무료·만료 없음** | ⬜ 오너 3분 작업 ([가이드](./docs/guides/telegram-notify.md)). 2026-07-31 카카오톡에서 전환 — 카카오는 알림용 키가 등록된 적이 없어 계속 조용히 건너뛰고 있었고, refresh token 을 2개월마다 갱신해야 해 손이 계속 갔다 |
| ~~Telegram 봇 토큰~~ | ❌ 미채택 — 카카오톡으로 대체(2026-07-26) |

> M0 가이드 확장(2026-07-19): 키를 🟢지금(무료·데이터/로고) / 🔵나중(발행·AI) 두 묶음으로 분리. DART가 최우선(정확한 연봉).

> 방식 결정: 인스타 발행은 **"Instagram API with Instagram Login"**(2024.7~) 사용 — 페이스북 페이지 불필요, 내 계정만 대상이라 앱 심사 없이 개발 모드로 동작. 토큰 60일 유효 + M5에서 자동 갱신 구현 예정.

## 세션 로그

| 날짜 | 세션 작업 | 결과 |
|---|---|---|
| 2026-07-18 | 설계 문서 일체 작성, 실행 전략(세션 운영 방식) 수립 | 문서 9종 커밋 |
| 2026-07-18 | 니치 확정(코어+트래픽 하이브리드), 콘텐츠 전략 문서화 | CONTENT_STRATEGY.md 추가 |
| 2026-07-18 | 브랜드 확정: wirit(@wirit_note), 잉크네이비 팔레트 | BRAND.md, assets/brand/ 추가 |
| 2026-07-18 | M0 셋업 가이드 작성 (계정·키 발급), 발행 방식 확정, .gitignore | M0-setup.md 추가, 운영자 수행 대기 |
| 2026-07-18 | M1 렌더러 구축: 모노레포 + JSON→PNG(Playwright) + 스키마검증 + dummy-card | 렌더 성공, 해시 동일(결정성), 불량데이터 반려 확인 |
| 2026-07-18 | M2 ranking-table 템플릿 3시안(A/B/C) + Pretendard 폰트 번들 | 5/10/15행·긴이름 렌더 검증. 시안 선택 대기 |
| 2026-07-18 | M2 시안 A 확정. 푸터 얇게. 실물사진 1급요소 문서화 | 운영자 A 선택 |
| 2026-07-18 | M3 market-daily(증시) 템플릿 + 국기 271종 번들 + lineChart 헬퍼 | 국기·차트·등락색 검증. 이미지 로딩=임시파일 goto 방식으로 확립 |
| 2026-07-18 | 다크 푸터, 실제 기업로고(5종), 폰트시스템(Pretendard/Wanted/BlackHan) | 로고·푸터 확정 |
| 2026-07-18 | 폰트확정(헤드라인·숫자=Wanted, 본문=Pretendard), 우상단 원형로고, 코발트 테두리, 푸터 1.5배 | 아이덴티티 강화. 15행·결정성 재확인 |
| 2026-07-18 | 로고 3단계 폴백(실제로고>브랜드칩>모노그램). 건설사·SK 브랜드칩 채움 | 웹 로고 자동수집 불가(프록시 차단) → 컬러칩 대체. 상승/하락색 유지 |
| 2026-07-18 | 테두리 잉크네이비 확정 | 로고·푸터와 통일 |
| 2026-07-18 | M4 수집기 코드(us-market/kr-rates) + 파서 셀프테스트 + Actions cron | 셀프테스트 15/15. 실데이터는 Actions에서(빌드환경 API 차단) |
| 2026-07-18 | 헤더 개편(뱃지제거, 태극기 제목 왼쪽, 부제 로고라인, 제목 크게/2줄) | 두 카드 통일 |
| 2026-07-18 | M5 pipeline: raw→콘텐츠JSON→PNG 자동생성(간밤의 미국 증시) | 셀프테스트 15/15, 픽스처 데이터로 카드 자동생성 확인 |
| 2026-07-19 | 공용 상단 캡션 표준화(날짜·부제 좌상단 옅게) | 두 카드 통일 |
| 2026-07-19 | **우선순위 재정렬**: 리서치·기획 중심(수동 R0 우선). RESEARCH_WORKFLOW + 결정로그·브리핑 틀 | 운영자 방향. 자동수집/발행은 부수트랙 |
| 2026-07-19 | R1 소재 보드 자동수집기: 주제별 구글뉴스·트렌드 RSS → research/briefs/{date}-auto.md, Actions 매일 07:00 커밋 | RSS 파서 셀프테스트 22/22. 오늘 첫 보드는 세션 웹검색으로 생성 |
| 2026-07-19 | 관제탑(Control Tower) 설계: 에이전트 판단+근거 시각화, 3단계(파일→정적HTML→웹) | docs/CONTROL_TOWER.md. 목업 제작 여부 운영자 확인 대기 |
| 2026-07-19 | 관제탑 목업 아티팩트 발행(칸반+티켓 타임라인+지시전달함, 소재 9건 인터랙티브) | https://claude.ai/code/artifact/76b89dd4-9e07-45c6-b0fb-abc666c00eb2 |
| 2026-07-19 | **조직 개편 v2 (운영자 승인)**: 9팀 닫힌 루프 — 트렌드분석·자료허브 신설, 6축 채점, 검수 6항, 사진정책, KPI | AGENTS.md 전면 개정. 다음: 트렌드분석 v1 → 자산허브 → 관제탑 v2 |
| 2026-07-19 | 트렌드분석팀 v1: 급상승 검색어 자동수집 → 소재보드 수요신호(📈) 통합 | 셀프테스트 28/28. 실수집은 Actions에서. 스크랩 워크플로우 가동(PATTERN_LIBRARY) |
| 2026-07-19 | 자산 허브 스캐폴딩: ASSET_HUB 규칙, 카탈로그(로고5·국기271·사진·지도·데이터셋), validate-assets 검증기+CI | 무결성 검사 통과·위반탐지 확인 |
| 2026-07-19 | **회사 체제 가동**: company/(CEO 판단원칙 20건 시드 + 9팀 사원카드) + CLAUDE.md 학습 프로토콜 + 관제탑 v2(탭·KPI·6축·아이디어 입력) | 4단계 완료. 목업 v2 재배포 |
| 2026-07-19 | **관제탑 B단계(정적 관제탑) 구축**: `packages/dashboard-static` — 상태 빌더(저장소 산출물→tower-state.json) + HTML 생성기(목업 룩에 데이터 주입, 썸네일 base64) + 파서 셀프테스트 23종 + 데일리 워크플로 재생성 배선 | 실데이터 검증: 후보9·승인대기2(아파트 실렌더 썸네일)·원칙27·팀9·자산6. 스크린샷 4종 확인 |
| 2026-07-19 | **관제탑 편집 기능 + Pages + 아티팩트**: 회사 탭에서 CEO 원칙·9팀 원칙/프롬프트를 직접 수정보완(지시 전달함 or GitHub 직접편집 링크). `prompts/` 팀별 시스템프롬프트 초안 9종 신설. Pages 배포 워크플로+가이드. 아티팩트 발행(비공개) | 편집→지시전달함 동작 검증. 아티팩트: https://claude.ai/code/artifact/fd253d9e-976f-477c-8dd0-c42bb75c71a1 |
| 2026-07-19 | **컨텐츠 마이닝 패널**(파이프라인 1열 개편): 화면 1/3 폭, 주제 비중(부동산30·경제20·주식15·기타35), [마이닝 실행]→실시간 수집 요청, 후보 목록(주제·출처·사유+선정/탈락, 스크롤), 지식 채팅창(붙여넣기), 선정/탈락 학습 루프(트렌드분석·리서치) | 헤드리스 상호작용 검증. 정적 한계는 지시 전달함으로 실동작, C단계서 백엔드 직결 |
| 2026-07-19 | **마이닝 1회 실행**(세션 웹 리서치): 비중대로 후보 10건 수집 → `research/briefs/2026-07-19-mining.md` (부동산3·경제2·주식2·돈부자2·정책1, 각 출처·선정사유). 브리핑 파서에 `사유:` 추출 추가 | 관제탑 마이닝 패널에 10건 표출. 수치는 신호(1차 출처 재확인 전제) |
| 2026-07-19 | 초봉 카드 데이터 정직화: 동일값 공동순위(rank 필드) + "밴드(정밀 아님)" 명시. 오보 원칙(공동순위·밴드표기·미검증집계 정밀카드 금지) CEO·QA 학습기록 | 오너 지적("같은 연봉인데 순위 다름", "정확한 금액이야?") |
| 2026-07-19 | 로고 자동취득 실전 가동: `packages/collectors` Wikimedia 취득기(검색어 다중화·라이선스 검증) + `scripts/lib/logo-resolver.mjs`(simple-icons). `asset-fetch.yml` 3회 실행 → SK하이닉스·한화에어로·HMM·HD현대일렉 실로고 자동 취득·커밋 | 오탐 1건(삼성전자→SamsungApps) 눈검증으로 제거, png/svg 확장자 버그 수정. 초봉카드 로고 7/10 |
| 2026-07-20 | M0 키 발급 가이드 확장(🟢지금/🔵나중 분리, DART·Brandfetch 단계 추가) → **운영자 DART_API_KEY 발급·등록** → `dart-salary.yml` 실행 | 1차 실행 경로버그(pnpm exec가 INIT_CWD 미설정) 수정 후 2차 성공. **평균연봉 10개사 1차출처 수집**(1억1,432만~1억7,795만, 동률 없음) → avg-salary.json. 로고 해결기가 허브(위키미디어 취득분) 우선 재사용하도록 개선 → 카드 실로고 7/10. 승인대기 4건으로 관제탑 갱신 |
| 2026-07-20 | 평균연봉 카드 완성(오너 3건 요청): 제목 어그로화("2026 대기업 평균연봉 Top10", 2025 기준 유지) · 직원수 "0.0만명" 통일(정렬) · **로고 Tier C(Brandfetch) 신설**(도메인 조회, 후보 배열 순차시도)로 남은 3개사(포스코인터·삼성바이오·HD한국조선) 실로고 확보 | **로고 10/10 달성**, 눈 검증 완료(slug "hd"→"hd-ksoe" 구체화). 파서 셀프테스트 50/50 |
| 2026-07-20 | **관제탑 라이브 모드(C단계 진입)**: GitHub API 직접 호출 레이어 — 연결 바(토큰), 마이닝/로고/평균연봉 워크플로 실행 버튼, 자료 인박스→`research/INBOX.md` 커밋(대량 업로드), 소재 선정/탈락→`research/decisions-inbox.md` 기록, 워크플로 상태 실시간. 클립보드 폴백 수정 | 헤드리스 검증(연결 UI·워크플로 버튼·오류 0). Pages+저장소전용토큰 필요([가이드](./docs/guides/control-tower-live.md)). 아티팩트는 CSP로 읽기전용 유지 |
| 2026-07-21 | **지하철 운행속도 세트 확정·검증·발행준비**: 커버(제목98·워드마크40·잉크바·👉CTA)+2단표 2장, p3 폐기. 캡션 확정(안전문구 교체). **verified✓**(연합뉴스·서울교통공사 공공데이터·법령 대조) | 발행 대기 2호 |
| 2026-07-21 | **전 팀 기본 업무 기준 동기화**: 이번 세션 지시(2장 캐러셀·커버 스펙·정답 가리기·안전 카피·1차 검증/verified 게이트·빌더 재계산 검증)를 CEO §B/§C + design·qa·marketing·planning·research·orchestrator·editing·trend-analysis 전 팀 문서와 PATTERN_LIBRARY·DATA_SOURCES에 반영 | 자동화 대비 기준 정합 |
| 2026-07-21 | **인스타 잘림 대응 → 지하철 카드셋 1:1 정사각(1080×1080) 전환**: 4:5가 업로드 시 센터크롭돼 제목·푸터 잘림. `.wirit-card` 높이 1350px→뷰포트 100%로 바꿔 비율을 템플릿 `config.json`에서 제어(metro-2col·metro-cover-photo에 1:1 config 추가). 평균연봉 등 4:5 카드는 무변화(픽셀동일 확인). 결정성·QA 통과 | 오너 "사이즈 바뀌어야" |
| 2026-07-21 | **뱃지 규칙 확정(배경 분기)**: 표(종이)=`wirit.` 잉크 워드마크만+콘텐츠 상향, 커버(사진)=흰 박스 뱃지 유지. design·CEO 기준 반영 | 오너 "표는 글씨만, 커버는 뱃지로" |
| 2026-07-21 | **비율 4:5로 복귀(최종)**: 1:1로 바꿨다가, 인스타가 4:5를 피드·그리드 모두 온전 표시함 확인(apt_lap 그리드)→ metro config 제거해 4:5 기본 복귀. 잘림은 업로드 '펼치기' 미실행 탓. `.wirit-card` 높이=뷰포트 100% 개선은 유지(비율 config화). 결정성·QA 통과. **뱃지 규칙은 유지** | 오너 "원래대로 돌리자, 4:5로 보임" |
| 2026-07-21 | **부동산 콘텐츠 대량 기획**: 시리즈 뱅크 39종 등록(바이럴13+실거래자동화26). GTX-A EP.1(4장)·대장도감 No.01 강남구(3장) 제작·검증. 실거래 연동 가이드(MOLIT·건축물대장·K-apt·청약홈·R-ONE·인허가·NEIS·좌표·카카오) | 오너 발제·확정 |
| 2026-07-21 | **P1 국토부 실거래 수집기 가동·실증**: molitCli(파서 방어적·상세→기본 엔드포인트 폴백·서비스키 정규화) + molit-collect.yml. **실데이터 검증**: 강남구 2026.5 유효 438건 수집→대장 산출(절대가1위 에테르노청담 218억·국평대장 현대14차 66억). 셀프테스트 71/71. 캐시=`data/datasets/molit/`. ⚠️403 원인=상세 미승인/전파지연→기본 엔드포인트 폴백으로 해결 | 오너 "P1 수집기 구축" |
| 2026-07-23 | **국장 성적표 세트 제작**: `krMarketCli`(야후 파이낸스, 코스피·코스닥)→`kr-market-2026.json`, `index-2026@1`(고점대비 하락률 메인) + `index-cover@1`(색반전 어그로 커버). 차트 주석(3점·날짜축·상승/하락 곡선화살표), 고점라벨 겹침 해소(상단밴드+흰 헤일로) | 오너 발제, 다수 반복 다듬기 |
| 2026-07-23 | **커버 원안 복원 + 수치 규칙**: 커버를 어그로 재설계했다가 오너 지적으로 원안 구성(부제목·제목 상단·CTA 하단 가운데·줄간격) 복원. 증시 수치 **소수1 고정**, 하락 뱃지 **경과일(NN일 만에)** 병기 | 오너 "임의 변경 말고 기존 기준대로" |
| 2026-07-23 | **커버 배경 교체(일본 전광판→추상 캔들차트)**: `photo-fetch.yml`에 후보 다장(count) 모드 추가 → Pexels 10장 눈검증 → #0 `stock-crash.jpg` 승격(카탈로그 등록) | 오너 "한국·추상 주식 사진으로" |
| 2026-07-23 | **제목 폰트=태백체 + 최외곽 흰색 테두리(기준 갱신)**: 태백 OTF는 Chromium OTS 거부 → `font_pipeline.py`+`font-convert.yml`(Actions, ots·otf2ttf)로 WOFF2 변환 후 적용. `--font-title`=태백체(전 카드), 카드 흰 3px+잉크 12px 프레임 기본. BRAND.md 반영. p2 제목 78→64px(태백 자폭 넓음) | 오너 폰트 업로드+"흰 테두리 기본" |
| 2026-07-23 | **국장 캡션 작성**: `data/review/captions/index-2026.txt` — 코드 린트 통과(태그5·금지어0·수치일치). '수익률'이 캡션 금지어라 '오르며/상승'으로 우회 | 오너 "업로드 캡션 만들어줘" |
| 2026-07-25 | **토허제 지도 마감 + 아이덴티티 노출 기준 확정**: 지도 경계 흰색 통일·표 18행 채움·인사이트 텍스트화·제목 1줄(nowrap)·**한강 도식선**(교량 좌표 단순화, 표시 전용·클리핑)·**서울 시경계**(evenodd 클립으로 내부선 없이 외곽선만)+'서울특별시' 라벨. **뱃지/아이디 배치 기준 = 슬롯 A/B 고정 + C(그래픽 내부 스탬프) 조건부 + D(캐러셀 마지막)** — BRAND.md §4a-3 / CEO §C / design 팀 반영 | 오너 6+3항목 지시. QA error 0·2회 렌더 동일·서울 신고가 카드 해시 불변 |
| 2026-07-25 | **토허제 지도 2차 다듬기**: 한강을 **손으로 안 그리고 도출** — 한강 이북 구 ∪ 이남 구가 **공유하는 폴리곤 정점**을 뽑아 경도순 연결(선이 정확히 구 경계 위) + 양 끝단(시계 구간)만 강서·하남 북쪽 링 정점으로 연장. 서울 경계선 약화, '서울특별시' 라벨 삭제. 인사이트↔푸터 여백. **아이디 위치 = 푸터 삭제 후 지도 좌측 스탬프 1개**(중복 금지 원칙 반영). 순위표 = 상위 15 + ··· + **꼴찌 3곳(38~40위)** + 우측 브래킷 주석 '셋 다 합쳐 18건' | 오너 6항목. QA 0·결정성 OK·서울 카드 해시 불변 |
| 2026-07-25 | **토허제 지도 최종 레이아웃**: 표 16위까지(꼬리 삭제), 제목 56px 한 줄·끝 이모지 제거·상단 여백 92px로 로고 뱃지와 30px 이격(겹침 방지), 행 높이·폰트 확대로 표 균형, 인사이트 여백 정리. **아이디 스탬프를 지도 SVG 뷰박스 좌표로 이동**(광명 아래·수원 팔달 좌측 / 노원 우측 2곳) → 지도 경계를 절대 벗어나지 않음 | 오너 지시. 실측(Playwright)으로 행 넘침 확인 후 확정. QA 0·결정성 OK·서울 카드 해시 불변 |
| 2026-07-25 | **토허제 신고가 지도 제작 완료(발행 대기) + 제작 기록 문서화**: 캡션까지 확정(린트 PASS). 이 게시물의 전 과정(소재 재정의·경기 실거래 확장·동탄구 경계 합성·서울 데이터 오염 사고·한강/시경계 도출·오너 피드백 5라운드 전체·최종 스펙·캡션 근거)을 [research/production-logs/2026-07-25-tohuh-singoga-map.md](./research/production-logs/2026-07-25-tohuh-singoga-map.md)에 정리. 팀 기준(qa·research·marketing·planning·editing) + DECISION_LOG·PATTERN_LIBRARY·TEMPLATES §7·DATA_SOURCES §2-1/2-2 동기화 | **오너 확인 필요**: 토허제 지정 목록 1차 출처 대조 후 verified 승격 → 발행 |
| 2026-07-25 | **토허제 지도 = [월간] 정기물 등록 + 재사용 오더 확정**: "토허제 기준으로~/토허제 지도~" 오더 = 기존 카드 그대로 재사용(월·데이터만 교체). IDEAS.md 월간 정기물·CEO §D·planning 반영. **소재 관제탑 생성기를 저장소로 이관**(`scripts/build-idea-board.mjs`) — 스크래치패드는 세션과 함께 사라지므로. 보드에 제작 완료 8건 그룹 신설 + 07-23 트리아지 결정 시드 주입(총 59건) | 헤드리스 스모크 통과(오류 0·복사 동작·결정 반영) |
| 2026-07-26 | **관제탑 상시 호스팅 배선 완료(배포 대기)**: Cloudflare Workers 배포 워크플로(`tower-deploy.yml`) — 저장소 산출물 → **카드 재생성·렌더(썸네일 복구)** → `_site` 조립 → wrangler 게시. **문 잠그기 방식 교체**: Cloudflare Access가 대시보드 버그(`use_clientless_isolation_app_launcher_url`)로 반복 실패 + workers.dev 보호 여부 불확실 → **워커 서버 측 비밀번호 문**(`packages/tower-worker`)으로 대체. 게이트 셀프테스트 9/9 통과 | 오너: Cloudflare 가입·프로젝트·토큰·GitHub Secrets 등록 완료. **남은 것 = `TOWER_PASSWORD` 시크릿 추가 후 워크플로 실행** |
| 2026-07-26 | **관제탑 C단계(직접 실행) + 소재 보드 통합**: 소재를 `research/ideas.json` 단일 원천으로 이관하고 관제탑 `💡 소재` 탭에 마이닝과 합침. 승인·보류·거부·수정·삭제·추가가 **누르는 즉시 저장소에 커밋**(sha 재확인·800ms 디바운스). 결정/원칙/자료는 decisions-inbox·INBOX로 직행. **우회 장치 전수 제거**(지시 전달함·요약 복사·초기화·죽은 CSS·decisions 배열). 미연결 시 조작을 아예 막아 사라질 변경 방지. 별도 ideas.html 폐기→`/#ideas` 리다이렉트, 생성기 삭제 | 스모크 17/17 · 파서 셀프테스트 24/24 · 배포 전 자동 실행 배선 |
| 2026-07-26 | **관제탑 전면 개편(기능·레이아웃·자동화)**: 결재 게이트를 소재→기획→검수→발행 전 구간으로 재설계. ① 오너 결정을 **저장소에 영구 기록**(`data/pipeline-state.json` 중단·수정요청, `publish-queue.md` 승인, `ideas.json` 단계·사유) ② 화면이 **배포를 안 기다리고** 저장소를 직접 다시 읽음(`refreshFromRepo`) ③ 저장 충돌(409)로 승인이 **소실되던 버그** — 모든 쓰기를 한 줄로 직렬화 + 5회 재시도 ④ 파이프라인을 흐름 레일 + 묶음 목록으로(가로 스크롤·무한 스크롤 제거), 소재 보드 버튼 3개로 간소화(진행/수정/삭제, 삭제 코멘트=사유) ⑤ **작업 표시줄**로 모든 실행 진행상황 시각화(중복 클릭 방지) ⑥ 회사 탭 = **CEO 중심 실제 계통도**, 본부 클릭 시 원칙 ⑦ **보관함**(주제별 자동 폴더링, 저장소 실파일만 링크) · **성과 탭** 신설 ⑧ 자유 입력 **지시함**(링크·메모·지시를 알아서 분류해 저장) ⑨ 화면 무게 8.0MB→870KB(표시 크기로만 이미지 축소) | 파서 셀프테스트 · 스모크 **70/70** · **전 버튼 시뮬레이션** · **실제 사이트 확인 25/25**(run #39) |

| 2026-07-26 | **"시킨 일이 진짜로 돌아가게" — 오너 질문 3건의 근원 수정**: ① **409 저장 실패의 진짜 원인 = GitHub 읽기 캐시**. 브랜치 이름으로 읽으면 방금 바뀐 파일도 몇 초간 옛 sha를 준다 → 그 sha로 저장하면 계속 409. 게다가 재시도가 "sha가 그대로면 건너뛴다"고 짜여 있어 **5회를 한 번도 시도하지 않고** 소진했다. → `git/ref`로 지금 커밋을 확인하고 그 커밋으로 읽는다(캐시 무력) + 매 회 반드시 저장 시도 ② **브라우저 옛 상태가 저장소를 덮던 문제** — 화면 상태를 통째로 localStorage에 저장·복원해 중단·삭제한 건이 되살아났다(동기화는 깃발을 더하기만 함) → 상태의 유일한 사실은 저장소, 브라우저엔 메모만 ③ **'접수'와 '실행'을 구분** — `data/requests.json` 요청 대장 + `process-requests.mjs`. '오늘' 탭 **내가 시킨 일**에 접수 시각·경과·담당(자동/사람)·언제 되는지. 사람 몫은 지시문 복사로 넘기고, 자동은 [지금 처리 돌리기]. 수정 지시는 재작업 지시서로 자동 전개 ④ **"OO 자료 찾아줘"가 실제로 실행** — `collect-signals --query`(뉴스 키워드 검색) 신설, 관제탑이 지시문에서 검색어를 뽑아 즉시 수집 실행. `ingest-signals`가 그날 보드를 전부 읽게(지시 수집 파일이 정렬에서 밀려 무시되던 버그) ⑤ **소재 발굴을 막던 또 하나** — digest가 수집 **전에** 자산 허브 검사를 돌다 17건 실패로 죽어 수집 자체가 실행 안 됐다 → 무관한 위생 검사는 경고로 강등 + 카탈로그 정리 | 스모크 **81/81**(캐시 재현·요청 현황 검사 신설) · 전 버튼 시뮬 0건 · 수집기 71/71 · 자산 허브 이상 없음 |

| 2026-07-26 | **"제작된 카드는 어딨는거야?" — 제작→화면 단절의 구조적 수정**: ① **빌더 명세화** — 배포의 카드 재생성 목록이 워크플로 YAML에 하드코딩돼 있어, 새 카드를 만들어도 목록에 손으로 추가하지 않으면 실사이트에 카드가 영영 안 떴다('월급 34평'이 그랬음) → `data/review/builders.json` 단일 원천 + `scripts/rebuild-cards.mjs`, **스모크 [0]절이 세트↔빌더 정합을 검사해 빠지면 배포를 막는다** ② **원본 내려받기 통로 신설** — 오너가 수동 업로드하려 해도 원본을 받을 곳이 없었다(화면 이미지는 900px 축소본) → `/download/`(비밀번호 문 안쪽)에 전 세트 원본 JPEG+캡션, 결재 화면에 [N장 JPG]·[캡션 복사] 버튼 ③ **정직한 단계 표시** — 제작중 화면이 "만드는 중입니다"라고 거짓말했다. 제작은 자동이 아니라 작업 세션(사람) 몫 → "자동으로 진행되지 않습니다" 명시 + **[제작 지시문 복사]**(채팅에 붙여넣으면 즉시 제작 시작) ④ 발행 세트에 estate-cover 빌더 2종 등록, `/cards/`(공개·발행용)와 `/download/`(문 안·오너용) 분리 | 스모크 **98/98**(세트↔빌더·카드 전달 검사 신설) · 시뮬 0건 · 게이트 14/14 |

| 2026-07-26 | **관제탑 단독 제작(클로드 없이) 개통**: 오너 질문 "대시보드에서만 작업할 수는 없어?"에 대한 구현. ① **[다시 제작] 버튼** — 보관함·결재 화면에서 빌더 있는 세트를 누르면 `produce-card.yml`(Actions)이 **데이터 재계산 → 렌더 → 자동검수 → 배포 반영**까지 무인으로 돈다(5~8분). 정기물(토허제·증시·실거래 랭킹·월급34평 등 6세트) 전부 해당 ② 빌더 없는 세트엔 거짓 버튼 대신 **"작업 세션 필요"**를 명시 ③ `scripts/produce-card.mjs` 로컬 실증(tohuh-rank 재생산 → 검수 PASS) ④ 요청 대장에 `auto:"machine"`(자동 제작 5~8분) 종류 추가 | 스모크 **102/102**([J. 관제탑 단독 제작] 신설 — 버튼이 워크플로를 실제로 거는지까지) |

| 2026-07-27 | **모바일 연결 + 토큰 권한 누락 수정**: 오너가 폰에서 연결하려다 발견. ① **연결 안내가 `Contents`만 요구**하고 있었다 — 관제탑은 워크플로를 dispatch 하므로 **`Actions = Read and write`가 반드시 필요**한데 빠져 있어, 안내대로 만든 토큰은 "연결됐는데 소재 발굴·카드 제작 버튼이 안 먹는" 상태가 된다 → 권한 2개 명시 ② **연결 검사가 `/user`만 물어봐서** 권한 부족을 못 잡았다(존재 vs 동작 함정 재발) → 연결 시 **저장소 접근 + Actions 권한을 실제로 확인**하고, 모자라면 무엇이 안 되는지 그 자리에서 말한다 ③ 토큰은 기기별 저장이라 **기기마다 한 번씩** 붙여넣어야 함을 화면에 명시 + 휴대폰 단계 안내(접이식) ④ 가이드 [docs/guides/mobile-connect.md](./docs/guides/mobile-connect.md) | 스모크 **106/106**([K. 연결 안내·검사] 신설 — 권한 부족 토큰을 실제로 넣어 경고가 뜨는지까지) |

| 2026-07-27 | **오보 수정: 서울 카드에 경기도가 섞여 있었다 + 정직한 진행 표시**: ① **「서울 아파트 실거래 랭킹」에 과천시·성남시분당구·화성시동탄구 등 경기 11곳이 들어가 있었다**(오너 발견). 원인 = `build-map-rank.mjs`가 실거래 캐시를 **지역 구분 없이 전부** 읽음 → 7/21 만들 땐 서울 캐시뿐이라 맞았는데, **7/22 경기 캐시를 수집한 순간 같은 코드가 조용히 다른 결과를 냈다**. 서울(11xxx)만 읽도록 수정 후 재생산 — 25개구 복구·검수 PASS(커버 71.5억/49.5억은 원래 서울 값이라 불변) ② **재발 방지 검사 신설** `scopeChecks.ts` — 제목이 '서울'인데 항목에 서울 밖이 있으면 **BLOCK**. 캡션 없어도 돈다 ③ **거짓 버튼 제거** — 지시서가 이미 만들어진 요청에 [지금 처리 돌리기]는 아무 효과가 없었다 → 효과 있을 때만 표시 ④ **정직한 문구** — 검수대기 "자동 검수를 도는 중입니다"(거짓) → "지금은 아무도 손대고 있지 않습니다", 수정지시 담당 "작업 세션(사람)" → "**오너가 시켜야 함** · 여기서 저절로 진행되지 않습니다" ⑤ 모바일에서 버튼 글자가 세로로 깨지던 것 수정 | 스모크 106/106 · 시뮬 0건 · 검수 15/15 · estate-84/59 재생산 PASS |

| 2026-07-27 | **[밀린 일 한번에 넘기기] + 운영법 문서화**: 오너 질문 "무한히 카드 제작을 하려면?"에 대한 답. ① **밀린 일 묶음 버튼** — 요청 대장 미완료 + 파이프라인 제작 대기(1~3단계) + 캡션 없는 발행 세트를 모아 **지시문 하나로 복사**. 하루 한 번 채팅에 붙여넣으면 그날 치가 끝난다(건마다 복사할 필요 없음) ② **운영법 가이드** [docs/guides/how-to-order.md](./docs/guides/how-to-order.md) — 핵심 원리는 "**대화 1회 = 영구 자산 1개**". 빌더 1개 = 매달 버튼 한 번으로 카드 1장 → **빌더 개수 = 생산 능력** ③ **재발행 가능 조건 명문화** — 자동 갱신 소스(국토부 매월 5일·증시 평일)로 만든 카드만 [다시 제작]으로 최신판이 나온다. 기사에서 손으로 뽑은 수치는 1회성 → **주문 시 "매달 낼 것/한 번만"을 밝히면 제가 수집기까지 함께 만든다** | 스모크 **113/113**([L. 밀린 일 묶음] 신설 — 복사 내용까지 확인) · 시뮬 0건 |
| 2026-07-27 | **소재 보드 재편 — 주제별 → 발행 주기별 (오너 지시)**: 보드를 볼 때 답이 나와야 하는 질문이 "이건 무슨 분야지?"가 아니라 **"이건 언제 또 내야 하지?"**였다. ① **칸을 주기로 재편** — 정기(데일리/주간/월간/분기/연간) · 일회성(시의성/일반) · 분류 대기. 칸마다 "왜 이 주기인지" 한 줄 설명 부착 ② **소재마다 `feed` 뱃지** — `자동`(수집기가 이미 돈다 → [🔁 다시 제작] 한 번이면 최신판) / `수동`(자료를 사람이 다시 넣어야 함). **정기물로 잡을 수 있는지가 여기서 갈린다** ③ **주제는 `topic` 필드로 분리** — 보관함 폴더링은 계속 주제(부동산·증시·경제·돈·연봉·교통·생활·생활·통계)로 한다. cat을 폴더명으로 쓰던 `build-archive.mjs` 수정 ④ **52건 → 45건** — 시의성 소멸 1(7/23 대토론회), 중복 1(분기 대장 갱신 → 완성 소재에 흡수), 뉴스 제목 그대로였던 자동수집 신호 5건 삭제. 사유 전부 `decisions-inbox.md`에 기록, 동탄 급등 각도는 '경기·신도시 대장'으로 흡수 ⑤ **새 소재는 '🆕 분류 대기'로** — 정기냐 일회냐는 오너 판단이라 기계가 못 고른다. `pickCat`이 첫/마지막 칸을 기본값으로 쓰던 습관 제거(예전에 새 소재가 '제작 완료' 칸에 처박히던 원인) ⑥ **화면에서 주기 변경** — ✎ 수정에 주기 선택칸 추가. 이게 없으면 분류 대기를 비울 방법이 없다 ⑦ **작업지시서에 주기·자료갱신 표기 + 정기물이면 `builders.json` 등록을 필수 항목으로** — 등록 안 하면 매번 세션을 불러야 하는 일회성이 된다 | 스모크 **120/120**([M. 소재 정리] 신설) · 시뮬 0건 · 실사이트 검사 +6 |
| 2026-07-27 | **발행 자동화 폐지 + 완성본 저장소 (오너 지시)**: ① **인스타 자동 업로드를 접었다** — 배포 워크플로에서 발행 단계 제거. 오너가 직접 올린다 ② **"내가 올렸다"를 적을 칸 신설** — 이게 없어서 시스템이 오너에게 **거짓 보고**를 했다("발행 0건"인데 오너는 이미 올리고 계셨다). 자동화하지 않기로 한 일일수록 기록 통로가 먼저 필요하다. 결재 화면 stage 5에 **[✅ 인스타에 올렸습니다]** 추가 → 발행 대기열이 `[x]`로 체크되고 발행일이 남는다 ③ **완성본 저장소 `published/`** — 누르면 `publish-archive.yml`이 돌아 `published/{발행일}-{label}/` 에 **그날 픽셀 그대로의 JPG + 캡션 + meta(출처·검수판정)**를 커밋. 카드는 매번 다시 그리므로 실거래가 갱신되면 지난달 카드 숫자가 바뀐다 — 나간 물건만은 굳혀야 증거가 된다 ④ **결정함에 '올릴 차례' 신설** — 승인만 해 놓고 잊지 않게. 같은 카드가 두 갈래 티켓으로 중복되던 것도 수정 ⑤ **성과 탭 = 발행 이력(완성본) + 수치 기록** 분리 — 수치가 비었다고 "발행 0건"으로 읽히지 않게 ⑥ **공개 경로 `/cards/` 폐쇄** — 인스타 Graph API가 공개 URL로만 이미지를 가져가기에 열어 뒀던 문. 자동 발행을 접었으니 쓸 상대가 없다 → 워커·스테이징·셀프테스트에서 제거, 전 경로가 비밀번호 뒤로 ⑦ `publish-instagram.mjs`는 **동작 검증된 채로 보관**(CI에서만 분리) — 다시 켤 땐 워크플로 단계·공개 스테이징·워커 예외 3곳을 함께 되살린다 | 스모크 **130/130**([N. 발행 완료] 신설) · 시뮬 0건 · 게이트 16/16 · 파서 24/24 · 실사이트 +6 |
| 2026-07-31 | **Cowork 창구 실측 — 이전 가능 확인 + 백지 clone 구멍 2건 수정 (오너 질문 "코워크로 옮겨도 되나")**: ① **doctor 18항 통과 · 발행본 md5 3장 32자리 전부 일치** — 픽셀 불변이 창구를 넘어서도 지켜짐을 실측으로 확인. Chromium 은 `/opt/pw-browsers/chromium` 사전설치(`playwright install` 금지), 폰트는 `templates/_shared/fonts/` 번들이라 clone 만으로 따라옴 ② **백지 clone 에서만 드러나는 구멍 2건** — `packages/dashboard/fonts/` 와 `data/out/` 은 gitignore 대상이라 갓 clone 한 환경엔 없다. 문서(§4)대로 `dashboard-static all` → `smoke-tower` 로 가면 **94/97**. 기존 환경은 예전 산출물이 남아 있어 통과하던 것 — **정말 백지에서 복제될 때만 드러나는 구멍**이었다 ③ **`scripts/render-sets.mjs` 신설** — sets.json 카드를 `data/out/{날짜}/` 로 렌더(관제탑 썸네일 원천). rebuild-cards 는 검수용 임시 렌더만 해서 PNG를 남기지 않는다 ④ **HANDOFF §4 에 관제탑 생성 명령 정정** — 폰트 복사는 `build-tower-site.mjs` 가 한다(`dashboard-static all` 은 안 한다). 새 환경 절차와 실패 시 대조표를 §4·§10·§12 에 명시 ⑤ **pnpm-lock 동기화** — 워크스페이스에 있으나 lock 에 없던 `packages/tower-worker` 등록(지금은 무의존성이라 무해하나, 의존성이 생기면 CI 의 `--frozen-lockfile` 이 깨진다) | 백지 clone 재현 **97/97** · doctor 18항 · 검수 15장 error 0 · 파서 24/24 |
| 2026-07-31 | **알림 카카오톡 → 텔레그램 전환 (오너 지시)**: 카카오 알림 3단계를 안내하던 중 오너가 "멈추고 나중에 텔레그램으로 한번에 구현하자"고 결정. ① **원인 정정** — 앞서 `KAKAO_REST_KEY` 를 `KAKAO_REST_API_KEY` 의 오타로 진단했으나 오진이었다. 그건 [realestate-data-apis.md](./docs/guides/realestate-data-apis.md) 에 적힌 **카카오 로컬 API(좌표 지오코딩) 용 Phase 3 예정분**이다. 지우라고 안내할 뻔했다 — 알림용 키 두 개는 애초에 **등록된 적이 없었고**, 이름이 비슷한 다른 키가 있어 켜 둔 줄 알았던 것이 진상 ② **왜 텔레그램인가** — 둘 다 0원이지만 카카오는 준비 5단계 + refresh token 2개월 갱신이라 손이 계속 간다. 텔레그램은 봇 하나에 **토큰 만료가 없다**(준비 3분). `CLAUDE.md` 기술 결정(승인 게이트 = Telegram/Slack 봇)으로 돌아가는 것이기도 하다 ③ `scripts/notify-telegram.mjs` 신설 — 동작 범위는 카카오와 동일(결정할 일 있을 때만 발송, 시크릿 없으면 조용히 건너뜀, 실패해도 배포 안 깨뜨림). 본문 상한이 200자→4096자라 제목이 잘리지 않는다 ④ `notify-kakao.mjs`·`kakao-notify.md` 삭제, `telegram-notify.md` 신설(3분 가이드), pipeline-tick ⑩단계 교체 ⑤ **로고·종목·지도 API 쪽 카카오는 그대로 뒀다** — 증시 카드의 기업 로고와 좌표 API는 알림과 무관하다 | doctor 18항 · 픽셀 3장 일치 · 스모크 **97/97** · 파서 24/24 · YAML 검증 |

## 다음 세션이 알아야 할 메모

- 운영자는 비개발자. 완료 확인은 항상 "눈으로 볼 수 있는 결과물"로 제시할 것
- **Cowork 창구 사용 가능(2026-07-31 실측)**: 저장소를 clone 하고 `node scripts/doctor.mjs` 만 돌리면 된다(✅ 18항). 발행본 md5 3장 일치 확인. 새 환경에서는 `rebuild-cards` → **`render-sets`** → `build-tower-site` → `smoke-tower` 순서를 지킨다 — 앞의 둘을 건너뛰면 썸네일·폰트가 없어 스모크가 94/97 로 떨어진다(코드 문제 아님)
- **렌더 환경은 한 곳으로 고정한다**: 결정적 렌더의 전제가 "같은 기계"다. 두 창구에서 번갈아 렌더하면 픽셀 기준선이 갈라질 수 있다
- **M1 완료**: 렌더러(`packages/renderer`)가 `pnpm --filter @wirit/renderer render -- --data <json> --out <dir>` 로 동작. 새 템플릿은 `templates/{이름}/`에 html+schema+sample+config 4종.
- **M2 완료**: ranking-table 시안 A 확정(variant 미지정 시 A 기본). B/C variant CSS는 특집용으로 템플릿에 보존. 밀도 자동조절(d-sparse/mid/dense), 긴 이름 말줄임.
- **M3 완료**: market-daily(증시). 국기 이미지(`_shared/flags/` 271종), 1년 라인차트(`lineChart` 헬퍼, 결정적 SVG), 등락색(상승=레드/하락=코발트).
  - **중요 인프라 변경**: 렌더가 이제 템플릿 폴더에 임시 HTML을 써서 file:// 로 goto → 국기·로고·실물사진 등 상대경로 이미지가 정상 로드됨(구 setContent 방식은 이미지 차단). `.render-tmp-*.html`은 gitignore.
  - **확인 요망(운영자)**: 하락=코발트 색이 괜찮은지. 지금은 한국 관례(상승 빨강)+팔레트 정합으로 채택. 별로면 변경 가능.
- **로고**: 아직 이름 첫 글자 모노그램(임시). 로고 라이브러리(`_shared/logos/{slug}.png`) 구축은 별도 작업 — 실제 기업 로고 넣으면 완성도 크게 상승.
- **M4 완료(코드)**: `packages/collectors` — us-market(Stooq)·kr-rates(ECOS). 파서/네트워크 분리, 셀프테스트 15/15. Actions `.github/workflows/collect.yml`(매일 06:30 KST + 수동버튼).
  - **실데이터 검증 대기**: 빌드환경은 외부 API 차단이라 여기선 실수집 불가. **운영자가 M0에서 ECOS_API_KEY를 GitHub Secrets에 넣은 뒤 Actions '데이터 수집' 수동 실행 → 아티팩트로 결과 확인**.
  - Stooq(미국지수)는 키 불필요 → 키 없어도 Actions에서 us-market은 수집됨.
- **관제탑 B단계 완료**: `pnpm --filter @wirit/dashboard-static all` → `packages/dashboard/index.html`(자체 완결, 브라우저로 바로 열림). 생성물은 gitignore(대용량 base64) — 생성기 소스만 커밋. 데이터는 전부 저장소 산출물에서 자동 집계(하드코딩 없음). 승인/보류/수정지시는 localStorage에 쌓여 "지시 전달함 → 요약 복사 → Claude"로 실행. **다음: Pages 상시 배포 토글** 또는 C단계(웹·버튼 실동작, M6~ 후).
- **다음 후보**: M5(수집 데이터 → 증시카드 자동생성 → 발행 E2E). 단, 인스타 발행은 M0의 IG 토큰 필요.
- 하락 색상(코발트) 확정은 M3(market-daily) 시안에서.
- 아직 미완: 로고 원본 `assets/brand/logo.png` 업로드(운영자), M0 키 발급(운영자).

### 상시 기준·인프라 (2026-07-23 추가)
- **포맷 기본값 = 단일 카드 1장** (07-23 확정, 기존 '캐러셀 기본 2장' 대체). 캐러셀은 **3조건 중 하나**일 때만 — ①정답 가리기가 핵심 ②한 장에 물리적으로 안 들어감 ③시리즈 연재. 목표 배분 단일 70~80%. 근거: 공유·저장·발행빈도 우세 + **커버 사진 취득이 무인화(R3) 병목**. 커버 필요 시 **그래픽 커버 우선**(사진은 감성이 핵심일 때만). 단일 후킹 = **"제목이 곧 반전"**. 계약: `docs/TEMPLATES.md §6`
- **랭킹 선정 지표는 집계·견고 지표로** — 개별 항목을 노이즈 비율(직전 대비 %)로 뽑지 말 것. 건수·비율·중위·지도 등으로. (신고가 리포트를 %랭킹→구별 건수 지도로 재정의한 사례)
- **제목 폰트 = 태백체(`--font-title`)** — 전 카드 제목 기본. base.css @font-face는 **WOFF2만** 참조. **새 폰트는 OTF를 `templates/_shared/fonts/`에 두고 `font-convert.yml`(Actions) 실행 → WOFF2 생성 후** base.css에서 참조(OTF 직접 참조 금지 — Chromium OTS가 CFF/세로메트릭 OTF를 거부해 폴백됨). 단일 웨이트 폰트는 @font-face `font-weight:100 900`으로 선언.
  - ⚠️ **폰트 로딩 검증법**: setContent가 아니라 **file:// HTML을 goto**해야 로컬 폰트 로드됨(about:blank는 file:// 하위자산 차단). 렌더러는 이미 goto 방식.
- **카드 최외곽 흰색 테두리 기본**(`.wirit-card::after`: 흰 3px + inset 잉크 12px) — 캡처 경계용. 전 카드 공통.
- **증시 카드 수치 규칙**: 소수1 고정, 하락 뱃지에 경과일(NN일 만에) 병기.
- **사진 다장 취득**: `photo-fetch.yml`에 `count`>1 → `cand/<slug>-i.jpg` 여러 장 받아 **눈검증 후 1장 승격**(정식 파일명+카탈로그 등록, cand/ 정리). 세션은 다운로드 차단이라 취득은 Actions.
- **국내 증시 수집**: `krMarketCli`(야후 파이낸스 차트 API) → `data/datasets/kr-market-2026.json`. `kr-market.yml`(수동+평일 cron). 발행 전 헤드라인 수치(현재·고점) KRX/언론 교차확인 후 verified 승격.
- **캡션 금지어에 '수익률' 포함**(투자 뉘앙스 회피) — 상승/오름세로 우회. 규칙은 `packages/pipeline/src/review/rubric.ts`.

### 관제탑 호스팅 (2026-07-26 배선 완료)
- 주소: `https://wirit-tower.engineerest0.workers.dev/` (관제탑) · `/ideas.html` (소재 보드)
- 배포: `.github/workflows/tower-deploy.yml` — push·매일 06:00 KST·수동. CI가 카드를 다시 그려 **썸네일 포함**
- 문 잠금: `packages/tower-worker` 워커가 서버 측에서 비번 검사. `TOWER_PASSWORD` 시크릿이 **없으면 공개 상태**(경고 출력)
- ⚠️ Cloudflare Access는 쓰지 않는다 — 대시보드 버그로 생성 불가했고 workers.dev 보호 여부도 불확실

### 관제탑 실시간화 (오너 질문 07-25 → 답변 문서화)
- 단계별 안내: `docs/guides/control-tower-realtime.md` (1 고정주소 → 2 자동갱신 → 3 직접저장 → 4 기기동기화 → 5 자동발굴 → 6 웹서비스)
- **호스팅 해법**: `docs/guides/control-tower-hosting.md` — 비공개 저장소는 GitHub Pages가 유료이고 배포본에 썸네일이 빠지는 문제를 **0원으로** 해결(**Cloudflare Pages 무료 + 빌드 시 카드 재생성 + Cloudflare Access**). 저장소는 비공개 유지
  - 썸네일 문제의 본질: PNG를 커밋할 게 아니라 **재료(`data/datasets/` 443파일)가 저장소에 있으니 배포 때 다시 그리면 된다**
- **오너 행동 대기**: ① Cloudflare 가입·Pages 프로젝트·토큰 발급 후 GitHub Secrets 등록(약 15분) ② 저장소 전용 토큰 발급(5분). 이 둘 없이는 3단계 이후가 물리적으로 불가(아티팩트는 외부 통신 차단)
- 권장 범위 = 1~4단계(전부 0원, 오너 실작업 6분). 5·6단계는 답답함을 느낀 뒤 결정

### 소재 보드 (관제탑에 통합됨 · 2026-07-26)
- **단일 원천 = `research/ideas.json`**. 관제탑 `💡 소재` 탭이 이 파일을 읽고, 오너 조작 시 GitHub API로 **되쓴다**
- 소재를 코드에서 추가/수정하지 말 것 — `research/ideas.json`을 고치면 다음 배포에 반영된다
- 별도 보드(`scripts/build-idea-board.mjs`)는 **삭제됨**. `/ideas.html`은 `/#ideas` 리다이렉트만 남음
- 오너가 관제탑에서 누르면 커밋 메시지가 `관제탑: …`로 남으므로, 세션 시작 시 `git log`로 오너 결정을 확인할 것
- 항목을 추가/삭제하면 `research/IDEAS.md`와 생성기 양쪽을 함께 갱신한다(IDEAS.md가 원천)

### 이 게시물(토허제 신고가 지도) 관련 (2026-07-25)
- **제작 기록 전문**: `research/production-logs/2026-07-25-tohuh-singoga-map.md` — 같은 유형 카드를 만들기 전 이 문서부터 읽는다
- **재현 명령**: `node scripts/build-tohuh-rank.mjs 202606 2026-07-23 16` → 렌더 → `qa` → `review --caption`
- **회귀 기준값**: 승인된 서울 신고가 카드 md5 = `5eaa30d25843e8f6447aeac44a7aa534` (공용 템플릿 수정 시 반드시 대조)
- **발행 전 남은 것**: 토허제 지정 목록 1차 출처(서울시·경기도 고시) 대조 → `data/datasets/tohuh-2026.json` verified 승격

### 관제탑 — 고치기 전에 반드시 읽을 것 (2026-07-26, 오류가 반복된 원인)
오너가 "왜 이렇게 못 잡는 오류가 많냐"고 지적한 건들의 **공통 원인 3가지**. 화면을 고칠 땐 이 3층을 다 통과시킨다.
1. **단방향 검사** — "썼다"만 보고 "다시 읽었을 때 반영되나"를 안 봤다 → 삭제·중단이 화면에 그대로 남음.
   → `scripts/smoke-tower.mjs` **[G. 왕복]** 절에서 쓴 것을 다시 읽어 확인한다.
2. **스냅샷 vs 실시간** — 화면은 빌드 시점 사진이라 저장소에 반영돼도 재배포(3분) 전엔 안 보인다.
   → 클라이언트가 `ideas.json`·`pipeline-state.json`을 **직접 읽는다**(`refreshFromRepo`, 로드·탭 복귀·작업 완료 시).
3. **존재 vs 동작** — "버튼이 있다"만 검사해서, 배선이 통째로 빠진 것(연결 뱃지)을 못 잡았다.
   → `scripts/sim-tower.mjs`가 **모든 버튼을 실제로 눌러본다**. 배포를 막는 검사다(오탐은 검사를 고칠 것).
- 마지막 관문 = `scripts/verify-live.mjs` — **인터넷에 올라간 주소**를 열어 25개 표지를 확인한다. 로컬만 통과하고 배포가 실패해 옛 화면이 남는 사고를 막는다.
- 세션에서는 workers.dev가 프록시에 막혀(403) 직접 못 연다 → **실사이트 확인은 CI 로그로 본다**.
- `data/review/sets.json` = 발행 세트 명세(라벨/제목/장수/캡션). 결재함에 뜰지 말지가 여기서 갈린다(캡션 없으면 안 뜸).
- 소재 발굴은 `research-digest.yml` → **`scripts/ingest-signals.mjs`** 가 브리핑(🔥 표시)을 `ideas.json`으로 옮겨야 보드에 뜬다. 브리핑만 쓰면 화면엔 아무 일도 안 일어난다.

### 관제탑 — 오너가 "언제까지 기다려?"라고 물으면 그건 설계 실패다 (2026-07-26)
- **"대기 중"은 금지어.** 모든 요청에 ① 언제 시켰나(경과) ② 누가 하나(자동/사람) ③ 언제 되나를 적는다. 모르면 "모른다"고 쓴다. 검사: 스모크 [H]절
- **요청 대장 = `data/requests.json`.** 관제탑이 요청할 때마다 한 줄 적고, `scripts/process-requests.mjs`가 **사실(파일 존재)로** 완료를 판정한다. 코드가 임의로 '완료'를 켜지 않는다
- **자동으로 되는 것 / 사람이 해야 하는 것을 화면에서 가른다.** 자료 조사 = 자동(키워드 수집 2~3분) · 수정 지시 = 지시서까지 자동, 제작은 사람 · 그 외 = 사람
- **저장 409가 또 나면** `GH.head()` → `git/ref/heads/{브랜치}`로 커밋을 잡고 그 커밋으로 읽는지 확인할 것. 브랜치 이름으로 읽으면 캐시에 당한다
- **워크플로에 무관한 검사를 앞에 두지 말 것.** 자산 허브 검사가 소재 수집을 죽이고 있었다. 오너 요청을 막는 검사는 그 요청과 직접 관련된 것만

### 같은 오류가 반복된 근본 원인 — 총정리 (2026-07-26, 오너 질문 "왜 아직도 해결이 안 됐어?")
겉으로는 매번 다른 버그였지만 뿌리는 5개였고, 각각 **구조(검사·명세)로** 막았다. 새 화면·새 카드를 만들 때 이 표부터 본다.
| 뿌리 | 무슨 일이 생겼나 | 구조적 처방 |
|---|---|---|
| ① 세션과 저장소의 단절 | 세션에서 만든 카드(data/out, gitignore)는 세션이 끝나면 사라지고, 배포는 하드코딩된 목록만 다시 그림 → "만들었다는데 화면에 없다" | 빌더 명세 `builders.json` + `rebuild-cards.mjs`. **스모크 [0]: 캡션 있는 세트에 빌더 없으면 배포 차단** |
| ② 접수와 실행의 혼동 | 버튼이 저장소에 글만 남기고 실행자가 없음 → "언제까지 기다려?" | 요청 대장 + '내가 시킨 일'(누가·언제) + 자동 실행 연결(수집 dispatch). **스모크 [H]** |
| ③ 스냅샷과 실시간의 간극 | 화면은 빌드 시점 사진, localStorage 옛 상태가 사실을 덮음 → "삭제했는데 남아있다" | 상태의 유일한 사실=저장소. `refreshFromRepo` + localStorage엔 메모만. **스모크 [G] 왕복** |
| ④ 존재와 동작의 혼동 | 버튼·요소가 "있다"만 검사, 배선 빠짐·409 캐시를 못 잡음 | **전 버튼 시뮬**(sim-tower) + 캐시 재현 검사 + 실사이트 확인(verify-live) — 전부 배포 차단 |
| ⑤ 사람 몫을 자동인 척 표시 | 제작은 작업 세션(사람) 몫인데 화면이 "만드는 중"이라고 거짓말 → "아무것도 작성을 안 하고 있는데?" | 정직한 문구 + **[제작 지시문 복사]** — 오너가 채팅에 붙여넣으면 그 자리에서 제작 시작. **스모크 [I]** |
- 수동 업로드 통로: 결재 화면 → **원본 내려받기(JPG) + 캡션 복사**. `/download/`는 비밀번호 문 안쪽, `/cards/`는 발행용 공개(승인분만).
- 새 카드 체크리스트: 빌더 스크립트 → `builders.json` → `sets.json` → 캡션 → review PASS. 하나라도 빠지면 스모크가 잡는다.
- **배포 확인은 판번호로**: `_site/version.txt`(빌드 커밋)를 함께 올리고, verify-live가 그 값이 기대 커밋과 같아질 때까지 기다린 뒤 검사한다. "N초 대기"는 금지 — 전파가 느린 날 옛 화면을 검사해 오탐 낸다
- **빌더는 반드시 지역을 필터링한다.** `data/datasets/molit/`는 서울(11xxx)·경기(41xxx)가 섞여 있다. 서울 카드가 `readdirSync(...).filter(f=>f.endsWith(".json"))`처럼 전부 읽으면, **나중에 다른 지역을 수집하는 순간 조용히 오염된다**(2026-07-27 실제 발생). 검수기 `scopeChecks`가 이제 막지만, 새 빌더를 쓸 땐 필터부터 확인할 것
- **관제탑 토큰 권한 = Contents(R/W) + Actions(R/W) 둘 다.** Actions가 없으면 기록은 되는데 실행 버튼(발굴·제작·처리)만 조용히 죽는다. 토큰은 **기기별 저장**(localStorage)이라 PC·폰 각각 붙여넣어야 한다 → 가이드: `docs/guides/mobile-connect.md`
- **수동 실행 전용 워크플로는 한 번 돌기 전엔 GitHub에 등록되지 않는다** → 관제탑 버튼(dispatch)이 404를 받는다. 새 workflow_dispatch 워크플로에는 자기 파일 경로에 대한 push 트리거(등록용 잡)를 함께 넣을 것 — produce-card.yml 참고
- **결재 카드 큰 이미지는 HTML에 base64로 박지 않는다** — `/download/{label}-{n}.jpg` 참조. 임베드하면 화면이 4.4MB로 부푼다(실측). 새 세트가 늘어도 화면 무게가 안 는다

### 발행 — 수동 원칙 (2026-07-27 오너 결정, 다음 세션 필독)
- **발행은 오너가 직접 한다.** 자동 업로드 코드(`scripts/publish-instagram.mjs`)는 동작 검증된 채로 남아 있지만 **CI에서 분리**돼 있다. 다시 켜려면 세 곳을 **함께** 되살려야 한다: ① `tower-deploy.yml` 의 발행 단계 ② `stage-public-cards.mjs` 의 `/cards/` 공개 스테이징 ③ `worker.js` 의 `/cards/` 공개 예외(인스타는 공개 URL로만 이미지를 가져간다). 하나만 켜면 조용히 실패한다.
- **발행 사실의 유일한 원천은 `published/index.json`** 이다. 오너가 [✅ 인스타에 올렸습니다]를 눌러야 생긴다. **`data/performance.md`(수치)가 비었다고 '발행 0건'으로 읽지 말 것** — 그 오독이 실제로 오너에게 거짓 보고를 냈다(2026-07-27).
- `published/` 는 gitignore 대상이 **아니다**(발행분은 그날 픽셀 그대로 커밋). 세트당 약 3MB씩 쌓이므로 저장소 용량을 가끔 확인할 것.

### 클로드 없이 되는 것 / 안 되는 것 — 확정 답변 (2026-07-26 오너 질문)
**관제탑 버튼만으로 되는 것(구현 완료)**: 소재 발굴·등록·삭제·진행, 수정지시(지시서 자동 생성), **기존 카드 재생산(제작)**, 발행 승인, 원본 내려받기+캡션 복사, **발행 완료 기록 + 완성본 보관**, 성과 기록 열람.
**조건부 — 오너 작업 1개면 되는 것**: ① **새 소재의 무인 제작**(작업지시서를 읽고 AI가 코드·캡션 작성) = Actions 안에서 Claude API를 돌리는 러너(M6). 크레딧은 **2026-07-27 충전 완료** → LLM 감각검수 4렌즈 가동 확인(years-to-buy BLOCK 판정: 부제-표 불일치·색 이탈 실검출). M6 러너 자체는 **아직 없다 — 세션 2~3회 분량**.
**하지 않기로 한 것**: **인스타 자동 업로드** — 2026-07-27 오너 결정. 발행은 오너가 직접 하고, 관제탑은 [✅ 인스타에 올렸습니다]로 그 사실을 받아 적어 `published/` 에 완성본을 보관한다. IG 토큰은 필요 없다.
**원천적으로 대시보드가 못 하는 것(사람 고유)**: 결제(크레딧·요금제), 계정·토큰 발급, verified 승격(1차 출처 눈 대조 — 오보 0 정책상 사람 확인), 디자인 취향 결정.

### 무한 제작 운영법 — 세션마다 이걸 목표로 (2026-07-27)
- **저와의 대화 1회 = 영구 자산 1개.** 새 카드 유형을 만들면 빌더로 남아 관제탑 버튼만으로 영원히 재생산된다. **세션마다 "새 유형 3~5개"가 기본 목표.**
- **주문 받을 때 반드시 물을 것: "매달 낼 것인가, 한 번만인가."** 매달이면 **데이터 수집기까지 함께** 만들어야 재발행이 버튼이 된다. 기사에서 손으로 뽑은 수치로 만들면 [다시 제작]해도 같은 숫자만 나온다(1회성).
- 자동 갱신 소스 = 국토부 실거래(매월 5일) · 국내증시(평일) · 미국증시·금리(평일) · DART(수동). 이 위에 얹은 카드만 정기물이 된다.
- 오너 운영 루틴(5분): 오늘 탭 → 결재 대기 처리 → [밀린 일 한번에 넘기기] → 정기물은 [🔁 다시 제작]
- **소재 보드는 주기별로 서 있다**(2026-07-27 재편): 정기 데일리 2 · 주간 5 · 월간 10 · 분기 5 · 연간 6 / 일회성 16 / 분류 대기 1 = **45건**. `feed:"auto"`인 소재만 버튼으로 재발행된다 — `manual`을 정기물로 잡았으면 **수집기부터 만드는 게 그 소재의 첫 작업**이다(chungyak·lotto=청약홈, pop=주민등록 API).
- 새 소재를 등록할 땐 `cat`(주기)·`topic`(주제)·`feed`(자동/수동)를 반드시 채운다. `cat`은 주제가 아니다 — 주제를 넣으면 보드가 다시 뒤섞인다.
- 가이드 원문: `docs/guides/how-to-order.md`

### 2026-07-30 세션 — year-bars@1 신설 + 전월세 카드 5장
- **새 템플릿 `year-bars@1`** — 연도별 변화율 발산 막대. 연도별 변화율은 순위가 아니므로 순위표에 넣으면 안 된다(연도 옆에 1·2·3이 붙어 시간축이 순위로 오독된다). 계약·규칙 = `docs/TEMPLATES.md §8`
- **`map-choropleth@1` 확장** — 선택 필드 `colorMid`/`midAt`/`legendMid` 추가(발산 배색). 기존 대장APT 지도 **md5 회귀검사 통과**. 제목 자동 축소는 **줄이는 방향으로만** — 키우면 확정된 기존 카드 픽셀이 바뀐다. §9
- **새 세트 2개**: `wolse-flip`(3장 — 연도별 + 자치구 1~13위 + 14~25위) · `rent-gu-map`(2장 — 월세 지도 + 전세 지도). 빌더·세트·캡션 전부 등록됨
- **캡션도 빌더가 쓴다.** 손으로 적으면 다음 달 갱신 때 카드는 새 숫자, 캡션은 옛 숫자가 된다. 문장은 사람이 쓰고 숫자만 계산값을 끼운다
- **지도 코드는 `scripts/lib/seoul-map.mjs` 한 곳** — 투영·무게중심·면적이 두 빌더에 복사돼 있던 것을 합쳤다. 복사하면 투영을 한 번 고칠 때 세 곳을 고쳐야 하고, 빠뜨리면 같은 서울인데 모양이 다른 지도가 섞인다

#### 이번에 드러난 뿌리 2개 — 다음 세션 필독
| 뿌리 | 무슨 일이 생겼나 | 처방 |
|---|---|---|
| ⑥ **규칙으로 지역을 자르면 조용히 틀린다** | "코드가 530으로 시작하면 서울 자치구"로 잘랐더니 R-ONE 530 대역에 함께 있는 **경기 시·구 24곳이 섞여** 서울 순위 1위가 영통구(수원)로 나왔다. ①과 같은 사고를 규칙으로 다시 만든 것 | 규칙 대신 **명단**(`SEOUL_GU_CODES` 25곳). 수집기가 25곳 전부의 계열 길이를 검사하고 하나라도 없으면 파일을 쓰지 않는다. 데이터셋 `meta.groups.seoulGu` 로 내보내 **빌더는 절대 다시 자르지 않는다** |
| ⑦ **사람이 확인한 사실을 자동화가 지운다** | 예정 수집이 한 번 돌면서 `meta.verified: true`·교차확인 메모·단위 확정 기록을 전부 지웠다. 카드 빌더는 verified 를 요구하므로 **수집 성공이 곧 카드 제작 중단**이 됐다 | ① 수집기가 최신월이 같으면 확인 기록을 **이어받는다**(새 달이면 내리고 이유를 남긴다) ② 대조 스크립트가 **판정까지 직접 기록**한다 — 손으로 올리지 않는다 ③ 수집 워크플로가 대조를 이어 돌린다 |

- **색은 정보다 — 카드마다 바꾸지 않는다.** 오름=레드(`--wirit-red`), 내림=코발트(`--wirit-cobalt`). 처음엔 "월세는 빨강 지도 / 전세는 파랑 지도"로 만들었는데, 전세는 0을 가로지르는 자료여서 **−5.0%인 금천구가 옅은 파랑 = 소폭 상승처럼** 읽혔다. 0을 가로지르는 자료엔 반드시 발산 배색
- **표시값이 같으면 순위도 같게.** 소수 첫째 자리까지만 보여주면 송파 14.9193 / 동작 14.8916 이 둘 다 "+14.9%"인데 18·19위가 된다 — 독자에겐 계산 오류로 보인다. 공동순위로 묶고 다음 순위를 건너뛴다
- **끝나지 않은 기간은 그래픽 자체가 말해야 한다.** 값 옆 "(6월까지)"만 적으면 그래픽만 잘라 썼을 때 경고가 사라진다 → `partial: true` = 사선 막대

#### 2026-07-30 2차 — 오너 수정 지시 반영
- **`year-bars@1` → `@2` 재설계**: 가로 발산 막대 → **가로축=연도 세로 막대**. 시간은 가로로 흐른다. 같은 세션 산출물이고 발행 이력이 없어 폴더를 그대로 쓰고 버전만 올렸다(§8)
- **2026년 연간 추정 표기** — 상반기 실적 × (12 ÷ 지난 개월수). **원자료에 없는 숫자**여서 네 곳에서 실측과 분리한다: 점선 테두리 + 사선 칠 + "하반기 추정" 꼬리표 + 실측/추정 경계 세로선. 하나만 빠뜨리면 추정치가 사실로 읽힌다
- **지도 2장을 토허제 40곳으로 확대** — 서울 25개 자치구 전역 + 경기 15곳. 서울 25곳만 그리던 `rent-gu-map` 세트는 40곳 버전에 완전히 포함되므로 **삭제**했다(죽은 세트를 결재 화면에 남기지 않는다)
- **경기 지역 매칭은 명단으로** (`GG_CODE` 15줄) — 이름으로 짐작해 집으면 동명 지역이 섞인다. 지정 목록(`tohuh-2026.json`)에 있는데 매칭표에 없는 곳이 생기면 빌더가 던진다
- **화성은 시 전체 기준** — 허가구역은 동탄 일대인데 R-ONE 동탄구 계열이 2026-01 분구 이후 6개월치뿐이라 2020-07 비교가 불가능하다. 동탄 값을 쓴 척하는 것이 오보이므로 카드·캡션에 "시 전체 기준"을 적었다
- **`framedBy`** — 지도 화면(bbox)은 **색칠 대상만으로** 잡는다. 경기 전체 bbox 로 그렸더니 40곳이 중앙에 뭉쳐 서울 라벨을 읽을 수 없었다. 잘려도 되는 건 배경이다
- **`hideLegend`** — 범례를 빼도 `footnote`(기준·한계 한 줄)는 남긴다. 범례는 편의고 기준 표기는 정직의 문제다
- 기존 대장APT 지도 **md5 회귀검사 재통과**(템플릿을 두 번 고친 뒤에도 픽셀 동일)

#### 2026-07-30 3차 — 토허제 지도 표준화 + 카드 1 A안 확정
- **📌 토허제 지도는 새로 그리지 않는다.** 「2026년 6월 신고가」 카드의 지도를 `scripts/lib/tohuh-map.mjs` 로 뽑아 **모든 토허제 카드가 같은 모듈을 쓴다**(한강·서울 외곽선·40곳 라벨 충돌 회피·동탄구 합성·지도 안 스탬프). 계약·표기 기준 전문 = `docs/TEMPLATES.md §10`
- **지역 표기 기준도 데이터에 있다** — `data/datasets/tohuh-2026.json` 의 `label`(표: 행정구·시까지) / `mapLabel`(지도: 짧게) / `rebCode`(R-ONE 코드) / `subCodes`(신설 구 합성). **손으로 다시 만들지 않는다.** 이번에 GG_LABEL·FULL 같은 손표를 두 번 만들었다가 버렸다 — 기준이 이미 있는데 못 찾아 다시 만든 것이 낭비였다
- **월세 카드 = `singoga-map@1`** 지도+표 1장. 기준 2025년 6월 → 2026년 6월. 전세 카드는 오너 지시로 폐기
- **카드 1은 A안(에디토리얼) 확정** — 제목 1줄 자동 축소 + 헤어라인, 우상향 곡선(막대 뒤 배경 레이어), 추정치 카드(점선·사선 = 추정 막대와 같은 언어), 플롯 빈 사분면에 요약카드 2장 + 워터마크. 막대 두께 84%→92%, 음수 영역 1.75→1.45 로 줄여 꽉 채웠다
- **`labelWidth` 는 호출자가 준다** — 기본 140(신고가 "성남 수정 26"), 상승률 카드는 176("성남 수정 +6.9%"). 기본값을 바꾸면 확정된 신고가 카드 픽셀이 변한다

#### 이번에 지킨 선 — 제목이 표와 모순되면 제목을 바꾼다
오너가 「토허제 40곳 **전체** 월세 폭등중」을 요청했는데, 1년 기준으로 **과천시가 −0.2%** 다.
그 숫자는 카드 표 40위 줄에 그대로 보인다 — 제목에 '전체'를 쓰면 **같은 카드 안에서 제목과 표가 서로 모순**된다.
그래서 '전체'는 `upAll` 일 때만 붙이고 아니면 뺐고, 하단에 「40곳 중 39곳 상승」을 적었다.
데이터가 허락하면 자동으로 '전체'가 붙는다. **문구보다 사실이 먼저다.**

#### 2026-07-30 4차 — 디자인 검수 자동화 + 두 카드 재정비
- **📌 검수가 카드 재생성에 붙었다.** `rebuild-cards.mjs` 가 빌드 후 `sets.json` 의 모든 카드를 렌더해 실측한다. 검수 실패는 배포를 막지 않지만 `::warning` 으로 크게 소리 낸다
- **`designQa` 에 겹침 검사 2종 추가** — 오너 지적("이건 자동 검수 했어야지")의 구조적 처방
  - `badgeclip` : 제목·상단 요소가 **우상단 로고 뱃지와 겹침**. 카드 밖으로 안 나가니 기존 `overflow` 검사에 안 걸렸다
  - `textclip` : **글자끼리 겹침**(값·라벨·하단 설명·푸터). 표가 길어져 하단 설명을 덮는 게 가장 흔한 불량이다
  - 글자 상자는 **Range 로 글리프 박스**를 잰다. 패딩 상자로 재면 뱃지 자리를 비워 둔 `.wirit-topcap` 이 오탐 난다
  - 공통 검사(넘침·겹침)를 순위표 전용 검사 **앞으로** 옮겼다 — 예전엔 `rows` 가 없는 카드는 넘침 검사조차 안 돌았다
- **카드 1(year-bars@2)** 제목 가운데·뱃지 아래 / 헤어라인·요약카드 삭제 / 지수 카드(최저↔현재 사상 최대)와 추정 배지를 **플롯 좌측 빈 칸**으로 / 워터마크는 0선 아래 빈 칸 / 실측 +4.6 과 추정 +9.2% 둘 다 표기 / HEADROOM 1.42→1.10 으로 막대 확대
- **카드 2(tohuh-rent-map)** 지수 → **금액**으로 전환. 주값 = 1년간 오른 월세(만원), 보조값 = 현재 월세. 기간 = 최근 1년(2025.06→2026.06). '생략' 표시 삭제 → 상위 11곳(검수가 정한 한계)

#### 배운 것
- **표 행 수는 눈대중으로 정하지 않는다.** 보조값이 붙어 행이 2줄이 되면 들어가는 줄 수가 달라진다 (16행 넘침 → 12행 2px 겹침 → **11행 통과**). 검수를 돌려 한계를 찾고 그 이유를 코드 주석에 적었다
- **`process.exit(0)` 은 스크립트 맨 마지막에 둔다.** 중간에 있으면 뒤에 붙인 단계가 조용히 안 돈다 — 검수 블록을 붙였는데 한 줄도 실행되지 않았다
- **단위 변환은 데이터가 확정한 값만 쓴다.** 천원→만원을 ÷100 으로 잘못 나눠 송파 월세가 23만원으로 나왔다. 빌더가 `meta.unit.avgWolse === "천원"` 을 확인하지 않으면 던지게 했다

#### 2026-07-30 5차 — 지수 곡선 겹치기 + 지도 2줄 라벨
- **카드 1: 막대 + 지수 곡선 이중 표현** (오너 제안). 막대 = 연간 변화율(%), 회색 선 = 지수 수준. **축이 다르므로 곡선에는 눈금을 넣지 않고** 시작·끝 값만 찍는다 — 두 축 눈금을 나란히 두면 "선이 막대를 앞질렀다" 같은 착시가 생긴다. 기준시점(2026.01=100)을 카드에 밝힌다
- **장식 곡선(화살표)은 제거**했다. 실제 데이터 곡선이 들어오는데 장식 곡선을 같이 두면 독자가 어느 선이 사실인지 구분하지 못한다
- 최저 지수 카드 삭제(지수 수준은 곡선이 직접 보여준다) · 추정 배지 + 범례만 좌측에
- **카드 2: 지도 라벨 2줄**(`twoLine`) — 행정구 / 값. 좁은 도형에서 1줄보다 훨씬 잘 들어간다. 2줄이면 폭이 절반쯤이라 `labelWidth` 도 184 → 118 로 줄인다
- 제목·최상단 문구 오너 문구로 · 표 10위까지 + `spread`(행 간격을 펴서 표가 본문 높이를 채운다)
- 신고가 카드 md5 회귀 재통과(지도 모듈에 2줄 옵션을 더한 뒤에도 픽셀 동일)

#### 2026-07-30 6차 — 오너 3차 지시 반영 (두 카드 마감)
- **카드 2**: 제목 자동 확대(`fitTitle` — 폭 936px 이분탐색, 줄이는 게 아니라 키우는 쪽도 이 게이트로) · 표 8위로 줄이고 `spread` 여백 · 머리글 3열 "순위/지역/월세 상승분" 가운데 정렬(`head.c`) · 최하단 "최근 1년(2025.06~2026.06) 아파트 평균 월세 · 동탄구는 화성시 기준" · 푸터 끝 마침표 삭제 · 지도 라벨 `placement:"nearest"`(행정구 중앙 최근접 빈 자리)
- ⚠️ **Handlebars 숫자 인덱스는 `{{head.c.[0]}}` 브래킷 필수** — `{{head.c.0}}` 은 파싱 오류("Expecting 'ID', got 'NUMBER'")
- **카드 1**: 곡선 바닥을 첫 플러스 연도(2021) 막대 상단 위로 재매핑(끝점 Y_TOP 앵커라 2026 은 고정) · 곡선 z-index 3(막대 앞) + 회색(--wirit-gray) · **매년 6월 말 지수 회색 레이블**(90.94 시작점도 뱃지→동일 레이블, 첫해는 시작점이 대표) · 좌측 강조 카드 2장(est=레드 점선·사선 / idx=잉크 — 각 그래프 색 연동, "역사상 최고 경신"은 `isPeakNow` 계산으로만 표기) · 회색 범례문·"단위 %" 삭제
- 지도 `placement` 는 opt-in — 기본 "down" 유지로 **발행본(신고가 카드) md5 572b4238… 회귀 재확인**
- 일괄 검수 15장 + 스모크 132 통과

#### 2026-07-30 7차 — 지도 라벨 소속 판정 + 카드 1 문구 정리
- **지도 라벨이 옆 구로 날아가던 원인 = 겹침 회피가 소속보다 앞섰던 것.** "빈 자리를 먼저 찾는" 방식은 중랑·서대문·금천·수원 팔달을 남의 구 위로 밀어냈다. 이제 후보 격자를 전부 훑어 **거리 + 도형 밖 벌점 + 겹침 면적 벌점**을 한 점수로 겨룬다(`placement:"nearest"`)
  - 겹침 벌점이 **연속값**(0/1 아님)이라 좁은 구는 "살짝 겹치고 제자리"를 택한다 — 오너가 수원 팔달에 허용한 그 동작이 규칙으로 들어갔다
  - 도형 안 판정은 무게중심이 아니라 **투영된 링 전체의 점-포함 판정**이다. 오목한 구(중랑·성동)는 무게중심이 옆 구에 떨어진다
- 카드 2: 기준 시점(`asOfNote`)을 제목 아래·표 오른쪽·지도 위에 **제목의 절반 크기**로 — 크기는 fitTitle 이 실측한 제목 크기에서 계산한다(고정값이면 제목이 커질 때 비율이 깨진다) · 하단 주석 위아래 여백 확보
- 카드 1: 제목 '월세 폭등' 레드(이 카드에서 레드 = 오름, 뜻과 어긋나지 않는다) · 지수 레이블 3개(2016·2020·2026 — 11개는 회색 띠가 돼 곡선을 가렸다) · 강조 카드 2장을 **좌우 배치**(좌 지수 / 우 상승률) · **카드 안 글자는 전부 같은 크기**, 강조는 색·굵기로만((추정)만 예외로 작게)
- ⚠️ **오너 메모의 9.6% 는 반영하지 않았다.** 계산값은 +9.2%(6개월 실측 +4.6% × 2). 수치는 코드가 뽑은 값만 쓴다
- 카드에서 산술 가정 한 줄이 빠진 만큼 **캡션에 추정 근거를 명시**했다 — 근거가 어디에도 없으면 추정치가 실측으로 읽힌다
- 발행본 신고가 카드 md5 572b4238… 재확인 · 일괄 검수 15장 · 스모크 132/132

#### 2026-07-30 8차 — 카드 1 강조 카드 확대 + 워터마크 추가
- 강조 카드 확대: 글자 31→39px · 폭 66→78% · 패딩 확대. 더 키우면 3줄로 접혀 카드가 곡선까지 내려온다
- **한국어 줄바꿈은 `word-break: keep-all` 로 못박는다.** 기본값이면 어절 중간에서 끊긴다 — "역사상 최 / 고 103.9" 로 잘렸다. 지금은 "월세지수 역사상 / 최고 103.9"
- `stamp2` — **지수 곡선 아래·막대 위** 빈 칸에 워터마크 하나 더(오너 지시). 초년도(2016~2020)는 막대가 0선 아래에만 있어 0선 바로 위가 통째로 빈다. 막대가 위로 자라는 데이터에서 켜면 데이터를 덮으므로 카드가 켤 때만 켠다
- 추정치는 **9.2% 확정**(오너 승인) — 6개월 실측 +4.6% × 2

#### 2026-07-30 9차 — 정사각 카드 + 표 열 맞춤
- 카드 1 강조 카드를 **정사각형**(aspect-ratio 1:1) 2장으로. 3줄 고정: 라벨 2줄 + 큰 레드 숫자 1줄
  - **라벨은 빌더가 줄 단위로 넘긴다.** 자동 줄바꿈에 맡기면 폰트·데이터가 바뀔 때 4줄이 돼 정사각형을 넘친다
  - 값 줄은 **스크립트가 실측해 줄인다**(최소 34px). CSS 값만 믿으면 자릿수가 늘 때(+12.4%) 점선 밖으로 삐져나간다 — 첫 렌더에서 "(추정)"이 테두리를 넘었다
- 좌측 워터마크를 곡선·막대에서 더 띄워 좌상단으로(오너 지시)
- **카드 2 표 열 어긋남 수정** (오너: "지역 위치가 너무 오른쪽"). 원인은 머리글과 데이터 행이 **서로 다른 그리드**인데 값 열을 둘 다 `auto` 로 둔 것 — 머리글은 "월세 상승분" 폭, 행은 "+38만원" 폭으로 각자 계산했다. 값 열을 고정 폭(176px)으로 못박고 정렬을 데이터에 맞췄다(순위 가운데 / 지역 왼쪽 / 값 오른쪽)
  - `.sm-h3`(head.c 를 준 카드에만 붙는 클래스)로 스코프 — 전역으로 바꾸면 발행본 열 폭이 달라진다
- 발행본 md5 572b4238… 재확인 · 일괄 검수 15장 · 스모크 132/132

#### 2026-07-30 10차 — 카드 1 마감 (2번 카드는 오너 확정)
- **"AI티" 제거**: 강조 박스에서 큰 라운드(24px) · 옅은 회색 채움 · 사선 무늬를 걷어냈다. 남긴 것은 종이 바탕 + 각진 괘선. **두 박스의 차이는 선 하나** — 실선 = 실측 / 점선 = 추정. 장식이 아니라 정보다
- (추정)을 값 아래 독립된 줄로 → 좌우 카드의 숫자가 **같은 크기**. 크기는 두 카드가 함께 들어가는 하나의 값을 스크립트가 찾아 동시에 적용한다(각자 줄이면 어긋난다)
- "하반기 추정" 알약형 1줄 → **정사각 2줄 뱃지**, 추정 구간 아래쪽으로. 1줄 뱃지는 막대보다 넓어 삐져나온 쪽이 막대 왼쪽 경계를 타고 오르는 곡선과 겹쳤다
- 지수 레이블 3개 → **원형 뱃지**(곡선과 같은 회색). 소수 한 자리 — 둘째 자리까지면 6글자라 원을 넘치고, 카드의 103.9 와 자릿수가 엇갈린다
- **`+1.7` 을 막대 안 흰 글씨로.** 어느 칸을 그렇게 할지는 **빌더가 곡선과의 실측 거리로 정한다**(`valueInside`) — "2021번 칸"이라고 박아두면 데이터가 바뀔 때 엉뚱한 칸이 흰 글씨가 된다
- **검수에 `barclip` 신설** — 지수 원형 뱃지 ↔ 막대 겹침. 오너 규칙("막대와 겹치지 않게")을 코드가 지킨다. 반쯤 겹친 값(-115%)으로 되돌려 **실제로 잡히는지 확인**했다(가로 20px·세로 28px 검출)
- 확정된 2번 카드 md5 ec4cad52… · 발행본 572b4238… 둘 다 불변 확인

#### 2026-07-30 11차 — 제작 기준 재편 + 에이전트 교육 (오너 지적: "지난 기준을 다시 말하게 했다")
**진단**: 기준이 없어서가 아니었다. 이번에 다시 말하게 된 항목 중 절반은 **이미 CEO.md·학습 로그에 있던 것**이다
(표 머리글 열 맞춤은 07-20 에 배웠고 07-30 에 재발). 원인은 기준이 **긴 학습 로그에 흩어져 있어 작업 전에 아무도 훑지 않은 것**.

**처방 세 가지 — 문서만 고치면 또 반복된다**
1. **단일 창구 신설: `docs/CARD_CHECKLIST.md`** — 짧고, 순서가 있고, 자동/수동이 구분된다.
   §0 내보내기 3줄 · §1 검수가 막는 것 · §2 **재발 금지 항목** · §3 자동/수동 가르는 기준 · §4 기술 함정 · §5 발행본 md5 · §6 세션 종료
   CEO.md·TEMPLATES.md 는 *왜*의 원천, 체크리스트는 *무엇을 확인하는가*로 역할을 갈랐다
2. **좌표로 잴 수 있는 원칙은 검수 코드로 승격** — 문서에만 적힌 원칙은 지켜지지 않는다
   - `colalign` 신설: **머리글 ↔ 데이터 행 열 어긋남**(07-20 · 07-30 재발 항목). 되돌려 검증 = 15px 검출
   - `footergap`(warn) 신설: 푸터 바로 위 요소가 푸터에 붙음(<14px) — 흐름 배치라 `rowclip` 이 못 잡던 자리
   - (앞서) `badgeclip` · `barclip` 도 같은 이유로 신설
   - 자동 항목 전체: overflow · padcross · badgeclip · textclip · barclip · colalign · numalign · rowclip · footergap · rightskew · truncate
3. **읽게 만드는 배선** — `CLAUDE.md` 세션 시작 4번 항목에 체크리스트 필수 열람 추가,
   `TEMPLATES.md` 머리에 배너, `prompts/design.md`·`prompts/qa.md`(런타임 에이전트 프롬프트)에 세부 지침,
   `company/CEO.md` §C 6건 + §D 4건, `company/teams/design.md`·`qa.md` 학습 로그 갱신
   (관제탑 회사 탭은 CEO.md·teams/*.md 를 파싱하므로 코드 수정 없이 화면에 반영된다)

**부수 정리**: `TEMPLATES.md` §8 의 낡은 필드 설명 수정(`lede` 폐지·제목 nowrap) — 낡은 기준이 남아 있으면 그게 다음 혼선의 씨앗이다

#### 2026-07-30 12차 — 하반기 추정 꼬리표: 상자 제거
- `.yc-tag` 에서 뱃지(상자)를 없애고 **글자만** 남겼다(2줄 유지 — 1줄은 막대보다 넓어져 곡선과 겹친다)
- ⚠️ **상자를 없애면 흰 글씨는 쓸 수 없다.** 사선 바탕이 옅은 분홍이라 흰 글씨가 사라진다 → 진한 레드(#a5121f).
  "상자 안 흰 글씨"는 상자가 있을 때만 성립하는 조합이었다
- 발행본 572b4238… · 확정 2번 카드 ec4cad52… 둘 다 불변

#### 2026-07-30 13차 — 두 카드 오너 확정 + 캡션 정리
- **확정 md5 기록**(`docs/CARD_CHECKLIST.md` §5): `wolse-flip-p1` = `87ef2d00…` · `tohuh-rent-map` = `ec4cad52…`
  발행본(`tohuh-rank` = `572b4238…`)과 **같은 규칙**을 적용한다 — 확정 이후 픽셀 불변
- **발행 세트를 단일 카드로**: `wolse-flip` 세트에서 p2·p3(자치구 25곳 표)를 뺐다.
  기준 시점이 p1(연간 변화율)과 달라(2020-07 임대차2법 이후 누적) 한 캐러셀에 섞으면
  독자가 두 기준을 헷갈린다. **빌더는 그대로 두었으니 별도 소재로 낼 때 되살릴 수 있다**
- **캡션 2건 재작성** (둘 다 빌더가 생성 — 손으로 적으면 다음 갱신에 카드와 어긋난다)
  - 공통: 첫 두 줄에 숫자·규모를 다 넣는다(인스타는 그 뒤를 '더 보기'로 접는다) · 해시태그 5개 · 중복 제거
  - 카드 1: 자치구 문단 삭제(다른 카드 내용) → 연도 나열 + 추정 근거 + 전세 대조로 압축.
    지수 시작월·기준월(=100)도 **데이터에서 읽는다**(손으로 적으면 통계 개편 때 거짓이 된다)
  - 카드 2: 40곳 순위는 유지(저장 유도 가치) 하되 한 줄을 짧게 · 중복 항목("가장 많이"/"서울 1위") 제거
  - ⚠️ **하드코딩 수치 제거**: "송파 금액 +20.2% / 지수 +10.6%" 가 손으로 박혀 있었다 →
    1위 지역의 금액·지수 변화율을 그때그때 계산해 넣는다. 다음 달 공표분에서 거짓이 될 문장이었다

#### 2026-07-30 14차 — 두 카드 발행 대기 등재
- `data/publish-queue.md` 에 `- [ ]`(승인됨·업로드 전)로 2건 추가 → 관제탑에서 **stage 5 · "업로드 대기"** 로 표시
- 자동 검수 리포트 생성(`pnpm --filter @wirit/pipeline review`) — 둘 다 **PASS**(결정성✓ · QA에러 0 · 캡션 린트 0)
  - ⚠️ 캡션 린트가 "출처 표기 없음"을 잡았다: 린트는 **낱말 '출처'** 를 찾는다. `📊 한국부동산원…` 만으로는 통과 못 한다 → `📊 출처 · …` 로 수정
- **중복 티켓 정리**: 옛 제목의 소재 카드(`wolse-flip-year`·`rent-gu-map`)가 남아 있어 카드 하나가 관제탑에 **티켓 둘**로 보였다. 티켓 매칭이 제목 기준이라 제목을 발행 세트와 맞춰 병합했다(옛 제목은 `titleWas` 로 보존 — 결정 이력을 끊지 않는다)
- 자치구 25곳 표(`gu-wolse-25`)는 stage 4 → 3 으로 되돌렸다. 발행 세트에서 뺀 물건이 '결재 대기'에 서 있으면 안 된다
- 확정 md5 불변 확인: `87ef2d00…` · `ec4cad52…`

#### 2026-07-30 15차 — 관제탑 구조 논의 (실행 보류)
- 오너 진단: 관제탑은 사실상 소재 보드 뷰어. 지시·소재 등록은 채팅이 훨씬 편하다 → 구조 원인 분석과 축소안을 제시했고 오너가 **보류** 선택
- 축소안은 `company/CEO.md` 확인 대기에 기록 — "관제탑 축소 진행해줘" 지시가 오면 그 안대로 실행
- ⚠️ 다음 세션 주의: 보류 상태이므로 관제탑 기능을 임의로 지우지 말 것

#### 2026-07-30 16차 — 관제탑 표준 축소 실행 (오너 승인)
- **탭 7 → 5**: 발행(기본) · 소재 · 보관함 · 회사 · 자산. **지운 것**: 결정함(오늘)·파이프라인 칸반·지시함(자유 입력)·요청 대장·성과 탭
- **새 '발행' 탭** = 관제탑의 고유 임무만: ①결재 대기(카드+캡션 실물 확인 → [🚀 발행 승인]) ②올릴 차례(JPG 다운로드 → [✅ 인스타에 올렸습니다]) ③발행 이력(성과 탭 흡수). KPI 도 결재 대기 · **올릴 차례** · 소재 풀 · 데이터 자산으로
- 결재 화면에서 수정지시·반려 버튼 제거 — "수정할 게 있으면 **채팅에**"로 안내(접수만 되는 버튼은 메모일 뿐)
- 소재 보드: 지시함 제거·단일 컬럼, "새 소재·지시는 채팅에 붙여주세요" 명시. ▶진행은 '골랐다' 기록으로 유지(제작은 채팅)
- 삭제된 JS 배관: 칸반·결정함·요청 대장(whoWhen/handoff/workOrderText)·지시함(classifyAsk/logRequest)·dropTicket/reviseTicket/promoteIdea — **약 2만 자 감량**
- 스모크 재작성: "지운 화면이 되살아나지 않았는가"를 명시적으로 검사(결정함·칸반·지시함·요청 대장·성과 없음) — **97/97 통과** · verify-live(배포 검증)도 5탭 기준으로 갱신 · dashboard-static selftest 24/24
- 문서: `docs/CONTROL_TOWER.md` 머리에 축소 배너 · `company/CEO.md` §D 에 원칙 승격("관제탑은 보고·기록하는 곳")
- ⚠️ 남긴 백엔드: pipeline-tick·requests.json 등 자동 정리 워크플로는 그대로다(화면만 없앰). 되살릴 일이 생기면 git 이력에 있다

#### 2026-07-30 17차 — 인수인계 문서 + 환경 자가진단 (오너 질문: "Cowork로 넘길 수 있니?")
- **`docs/HANDOFF.md` 신설** — 저장소를 처음 붙인 세션이 읽는 단일 입구.
  30초 오리엔테이션 → 환경 확인 → 세션 시작 순서 → 공장 구조 → 명령 → 새 카드 흐름 → 절대 금지 → 학습 프로토콜 → 현재 상태 → 창구 이전(§12)
- **`scripts/doctor.mjs` 신설 — 문서로는 알 수 없는 것을 실제로 만들어 확인한다.**
  빌더 실행 → 카드 JSON → 렌더 → **발행본 md5 대조** → 자동 검수. 이 사슬이 끝까지 가면 그 환경은 공장이 맞다
  - 등급: A 필수(카드 제작 능력) / B 선택(API 키 — 없어도 기존 데이터로 카드는 나온다) / C 선택(배포)
  - **❌면 "카드 픽셀을 건드리지 마세요"라고 분명히 말한다** — 렌더가 안 되는 환경에서 카드를 고치면 검수가 사람 눈으로 돌아가고, 그게 오보 0이 깨지는 지점
  - ⚠️ 진단기 자신의 오진을 한 번 잡았다: `playwright-core` 는 renderer 패키지 의존성이라 루트에서 import 하면 못 찾는다 → 멀쩡한 환경을 불합격 판정했다. `createRequire(renderer/package.json)` 로 해석
- **`data/review/pixel-baselines.json` 신설** — 발행본·확정본 md5 의 **기계가 읽는 단일 원천**.
  CARD_CHECKLIST §5 표는 사람이 읽는 사본이고, 새 카드 확정 시 둘 다 고친다
- 입구 배선: `CLAUDE.md` 머리 · `README.md` · `CARD_CHECKLIST.md` §5 에서 HANDOFF/doctor 를 가리키게
- 검증: doctor 18항 통과(3장 전부 픽셀 동일) · 카드 검수 15장 · 스모크 97/97 · selftest 24/24

#### 검수기가 잡아 준 것 (이번 세션)
| 발견 | 검사 |
|---|---|
| 추정 배지 설명글이 큰 숫자 아래로 깔림 | `textclip` |
| 표 16행이 하단 설명·푸터를 덮음 → 11행이 한계 | `textclip` |
| 곡선 범례가 −1.5 값 라벨과 겹침 | `textclip`(leaf 에 `.yc-lleg` 추가한 뒤) |
| 제목 가운데 정렬 시 로고 뱃지와 겹침 | `badgeclip` |
- **검수 leaf 목록에 없는 요소는 안 잡힌다.** 새 요소를 만들면 `designQa` 의 `LEAF` 에 추가한다 — 곡선 범례가 목록에 없어서 한 번 놓쳤다

- **2026-07-31** — 정비사업 지도 카드(`jeongbi-board`) **발행 완료**. 뉴시스 인포그래픽을 위릿 포맷으로 재구성: 지도는 번호 원만, 단지명은 하단 8칸으로 내림. 브랜드 로고 14종 확보(오너 제공 9종) + 누끼·크기 정규화, 색은 로고 픽셀에서 추출. 좌표 24/24 카카오 지오코딩. 제목 A안 채택, 캡션 작성 완료. md5 `57783108`.
- **2026-07-31** — 이 카드를 만들며 나온 작업 기준을 문서에 반영했다: `company/CEO.md`(일하는 순서·캡션·출처), `docs/CARD_CHECKLIST.md` §2(로고·색·지도·레이아웃 체크 16항), `docs/BRAND.md`(지도형 카드 워터마크 3개), 팀 학습 로그 3건(design·qa·asset-hub). 발행 캡션과 미채택 3안은 `data/review/captions/` 에 이유와 함께 남겼다.
- **2026-07-31** — 발행본 `jeongbi-board` 머리 규격 복구(오너 지적: 완성본과 윗부분이 다름). 카드 패딩 44→72, 코너 뱃지 공용 규격 복귀, 지도 상한 620px·카드 아래 여백 28px, 출처에서 매체명 제외. md5 `ea241b6e`. `designQa` 에 **brandhead** 검수항 추가(warn) — 기존 카드 index-2026·maprank·estate-cover 가 같은 방식으로 어긋나 있어 규격에 맞출지 예외로 둘지 정해야 한다.

#### 2026-07-31 (2) — 카드 2장 확정 + 검수 구멍 3개 + 죽어 있던 수집 파이프라인 복구

**만든 것**
- `brand-rank` (오너 확정) — 아파트 브랜드 순위 조사 5곳 1~5위 비교. `brand-rank-grid@1` 신설.
  조사마다 1위가 다르다(자이·래미안·힐스테이트)는 것이 이 카드의 문장. 발표 없는 자리는
  비우지 않고 '발표 없음'이라 적는다. 시점이 조사마다 달라 **푸터에 기준일을 적지 않는다.**
  md5 `91d8e2ab`.
- `kospi-record` (오너 확정) — 2026-07-31 증시 하루 기록 5줄 + 코스피 추이. `record-grid@1` 신설.
  **주인공은 '역대 1위' 같은 문구, 수치는 증거**(오너 지시)라 문구를 태백체로 크게 두고 수치를 회색으로 내렸다.
  md5 `5c242f77`. ⚠️ 데이터셋 `verified=false` — 수급·시총은 보도 인용이라 **KRX 대조 후 발행**.

**🐞 죽어 있던 파이프라인 (오너가 Actions 화면을 보내 준 덕에 발견)**
`selftest` 가 로고 도메인맵의 항목 수를 `=== 3` 으로 검사하고 있었다. 브랜드 로고를 추가하자
테스트가 깨졌고, 워크플로는 수집 **전에** 셀프테스트를 돌리므로 **증시 수집이 통째로 죽었다.**
데이터셋이 07-22 에 멈춘 것을 나는 "세션 외부망 차단" 탓으로만 설명하고 있었다.
→ 개수 대신 성질 검사 3개로 교체(85 통과). 수집 성공 → 7/23~30 채워짐 → 차트 점선 사라짐.
→ 덤: 보도로 적어 둔 7/29·7/30 종가가 수집값과 **소수점까지 일치**. 빌더가 매번 이 대조를 한다.
→ `kr-market.yml` 에 대기열 푸시 트리거 추가(세션은 GitHub API 가 403 이라 버튼을 못 누른다).

**🕳 3팀 합동 검수(디자인·AS·기획)가 찾은 검수 구멍**
1. **새 템플릿의 글자가 통째로 겹침 검사 밖에 있었다** — `designQa` 의 `LEAF` 에 record-grid
   요소를 추가하지 않았다. "문제 없음"은 통과가 아니라 **아무것도 안 쟀다**는 뜻이었다.
   `footerGap` 의 '푸터 위 요소' 목록도 마찬가지 → 오너가 두 번 말한 "최하단 문구 여백"이
   새 카드에는 적용되지 않고 있었다.
2. 오너 지적 3건 중 `aicard` 하나만 코드가 되고 나머지는 글로만 남아 있었다.
3. 최고점 뱃지가 꼭짓점 직후 되돌림 구간을 덮고 있었다("꼭짓점 아래는 비어 있다"는 가정).

**새 검수항 3종** (모두 일부러 어긋나게 되돌려 잡히는 것을 확인)
| 코드 | 잡는 것 |
|---|---|
| `titlegap`(warn) | 제목 아래 간격 28px 미만 — 제목을 키우면 아래 여백이 함께 줄어든다 |
| `aicard`(warn) | 라운드 20px+ 옅은 회색 채움이 3개 이상 반복 — "AI티" 금지의 코드판 |
| `hierarchy`(warn) | 기록 문구가 수치의 1.15배 미만 — 주인공이 뒤집혔다는 신호 |

**학습 반영** — CEO.md 원칙 8건, CARD_CHECKLIST §2 7항 + §5 표 2행,
팀 학습 로그(design·qa), **런타임 프롬프트 5종**(design·qa·editing·orchestrator·research).

**다음 세션이 알아야 할 것**
- `kospi-record`·`brand-rank` 둘 다 **오너 확정이지만 발행 게이트 전**이다. 1차 출처 대조가 남았다.
- 레거시 카드(index-2026·maprank·estate-cover)가 `brandhead` 를 어긴 상태로 남아 있다 —
  규격에 맞출지 예외로 둘지 오너 결정 대기.
- 포레나 브랜드 로고 미확보(현재 카드에는 불필요).
